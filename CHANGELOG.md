# Changelog

## v2.0.0 - 2026-08-02

- 使用 React 18 与 TypeScript 重建前端工程；移除旧 `web/app.js` 手写 DOM 运行时。
- 建立 18 条正式路由与清晰层级：日志中心为一级页面，GitHub 助手位于常用工具二级页面，返回按钮只由父路由元数据控制。
- 保留系统、Docker、文件、SSH、安全、审计、APT、备份、Passkey、TOTP 与 GitHub 工作流的真实 API 能力。
- 修复 Passkey `flow_id`、WebAuthn snake_case 映射、资源状态解包、模板条件链、自闭合组件解析、移动抽屉和表单作用域等运行问题。
- 修复 Web / Agent 防火墙协议、UFW 参数顺序、真实命令输出、添加后即时刷新与移动规则卡片。
- 将根目录 `VERSION` 设为版本唯一真值，并统一 README、CHANGELOG、SECURITY、前端包、Go 链接参数和 Release 校验。
- 使用固定版本的 React 18/ReactDOM 运行时和 TypeScript 离线构建，正式成品不依赖 CDN，也不让旧前端混入。
- 完成 Go 测试与静态分析、AMD64/ARM64 构建、11 种视口 × 18 条路由的 198 次 Chromium 渲染回归、51 组字段级交互契约、154 个可见控件真实性审计及 36 个页面视图无障碍检查。

## v1.0.0 - 2026-08-02

- 完成稳定版发布前清理：移除历史版本命名、重复样式、无引用函数、调试残留和旧版兼容覆盖层。
- 将前端渲染函数收敛为单一正式实现，统一页面、卡片、表单、状态、弹窗、移动导航与响应式规则。
- 固化 NTP 与内核优化的真实状态展示；读取失败时显示明确原因，不再以“未知”掩盖后端错误。
- 统一 GitHub 版本建议和 Release 流程，稳定版默认使用正式 Release，而不是预发布状态。
- 增加 v1.0.0 发行契约与前端卫生检查，阻止旧版本标记、占位交互、调试语句和未同步嵌入资源进入发布包。
- README、Security Policy、Roadmap、安装器和 GitHub Actions 示例统一到 v1.0.0。
- 从清理后的源码重新执行前端嵌入、单元测试、Race 检查、静态分析、浏览器级响应式审计和 AMD64/ARM64 构建。
## v0.9.10-beta - 2026-08-02

- 按统一产品信息架构重构桌面端与移动端真实页面，桌面使用深色侧栏和高密度管理布局，手机端使用紧凑卡片与五项主导航。
- 重做概览页的运行时间、负载、CPU、内存、磁盘、Swap、系统信息、Docker 状态与快捷操作，移除臃肿和错位的信息块。
- NTP 状态改为识别 systemd-timesyncd、Chrony、NTPsec、NTP 和 OpenNTPD，并显示服务单元、启用状态、同步状态、服务器和时区。
- 内核优化页面显示当前 LukePanel 预设、实际拥塞控制、队列算法、swappiness 与 BBR 状态，预设按钮明确标记当前方案。
- 重构文件管理列表与操作面板、日志审计筛选和记录卡片、GitHub、SSH、Docker 与安全页面的移动端密度和长文本处理。
- 统一 Visual Viewport 弹窗定位与滚动锁，二次验证、Compose 编辑器和输入对话框在 iOS 键盘出现时保持可用。
- 增加 sysctl 预设与 systemd 属性解析测试，并继续要求所有可见交互绑定真实处理器和服务端接口。
- README 与 UI 规范更新为面向公开用户的产品、部署、安全和贡献说明。

## v0.9.9-beta - 2026-08-02

- 重新建立全站统一的响应式设计系统，覆盖所有页面、卡片、表格、表单、对话框和底部弹层。
- 将正式 LukePanel 图标应用到侧栏、登录流程、浏览器图标、Apple 主屏幕图标和 PWA 清单。
- 统一移动端间距、按钮布局与长文本处理，删除互相覆盖的旧样式并消除页面级横向溢出。
- 按 iOS 实时 Visual Viewport 重构密码二次验证，键盘弹出后输入框、标题和操作按钮仍保持可见。
- 增加前端交互契约测试，阻止没有真实事件处理器的按钮和表单进入发行版本。
- 修复审计记录“复制”按钮没有实际处理器的问题；GitHub 助手开关改为准确的“显示/隐藏入口”。
- 重写 README，使其成为面向所有用户的安装、功能、安全、运维和开发文档。
- 统一安装器与发行包中的 Agent systemd 权限，确保 APT 能切换到 `_apt` 用户执行下载。

## v0.9.8-beta - 2026-08-01

- 统一移动端响应式规则，清理 v0.9.6/v0.9.7 相互覆盖的临时 CSS，避免同一页面出现尺寸和定位冲突。
- 二次验证始终贴合 iOS 实际可视区域底部；键盘弹出后不再跳到页面顶部、隐藏标题或遮挡操作按钮。
- 弹窗、Compose 编辑器和输入对话框统一使用 Visual Viewport 与滚动锁，关闭后恢复原页面位置。
- 重构 APT 软件源移动端卡片，解析来源和域名，完整配置折叠展示，长地址不再撑破页面。
- 修复手机端单个页头按钮只占半行、通用操作按钮溢出、长文本换行及子页面底部留白问题。
- Agent 安装单元显式保留系统管理所需权限，修复 APT 切换 `_apt` 用户时 `seteuid 42` 失败。

## v0.9.7-beta - 2026-08-01

- GitHub Device Flow 改为零配置登录：用户只需在 GitHub Device 页面输入一次性代码并授权，LukePanel 后台自动轮询并完成登录。
- OAuth Token 不再出现在前端流程中，也不会返回给浏览器；Token 登录仅保留在折叠的高级方式中。
- 未配置项目专用 OAuth Client ID 时自动使用 GitHub CLI 的公开设备授权入口，同时继续支持环境变量和构建参数覆盖。
- 进一步修复 iPhone 二次验证与键盘工具栏重叠、全屏编辑弹窗高度、子页面底部留白及软件升级卡片布局。
- 完整继承 v0.9.6-beta 的安装参数、全文件系统管理、TOTP 登录、审计一致性和移动端修复。

## v0.9.6-beta - 2026-08-01

- 修复 Release 未注入 OAuth Client ID 时设备登录入口被静默隐藏；现在可在页面临时填写公开 Client ID 并直接生成设备授权码。
- 修复 iPhone 子页面底部导航遮挡、Compose 弹窗被 Safari 工具栏挤压、二次验证与键盘重叠，以及软件升级卡片留白过大。
- 重做 iPhone 表单和安全页布局，修复横向溢出、底部导航遮挡以及 Safari 聚焦输入框自动放大。
- 二次验证改为独立密码弹窗，不再重绘整页；支持 iOS 键盘、回车提交和原页面位置保持。
- 登录页移除 TOTP 说明，密码校验成功且账号已开启 TOTP 时进入独立验证码步骤。
- 首次安装支持交互或命令行自定义管理员用户名、密码和监听端口；升级保留已有设置。
- GitHub Device Flow 改为项目级 Client ID，普通用户无需创建 OAuth App 或填写 Client ID；保留内存 Token 登录备用入口。
- 文件管理开放 `/` 完整文件系统；虚拟文件系统保持只读，敏感文件改为二次验证后可操作。
- 删除历史错误上传到仓库根目录的 `server/audit*.go` 残留，并统一版本与 Release 构建。

## v0.9.5-beta - 2026-08-01

- 修复 JSONL 审计日志与可选 SQLite 索引之间的即时一致性，写入后立即读取不再返回空结果。
- 启动时同步完成审计索引检查与必要重建，轮转日志和旧版无时间字段记录均可读取。
- 修复 GitHub ZIP 助手误剥离 `internal/`、`.github/` 等合法仓库顶层目录的问题。
- 增加 focused ZIP 与 dot-directory 导入回归测试，避免 Hotfix 被上传到错误路径。
- GitHub Actions 在无外部 Go Module 依赖时关闭无效缓存，消除缺少 `go.sum` 的警告。
- 临时兼容并隔离错误上传到仓库根目录的 `server/audit*.go`，不再参与 Go 构建。

## v0.9.0-beta - 2026-08-01

- 核心单机管理功能进入 Beta 冻结阶段，后续 v0.9.x 主要修复真实环境兼容性。
- Docker 新增 Docker Hub 搜索、Compose 新项目向导、固定诊断命令、Volume 占用扫描及备份恢复；容器日志按需实时刷新并支持暂停。
- APT 下载、升级、软件安装/删除与 Docker 镜像构建改为 Agent 后台任务，页面断线后可继续查询状态。
- 新增 UFW 可恢复规则管理、NTP 开关和完整软件源管理。
- 新增 Fail2ban 状态、失败/封禁统计、单 IP 解封和 SSH 白名单维护；当前访问 IP 不允许被误移除。
- 新增 Passkey / WebAuthn、可信设备、可恢复 IP 允许列表和 Telegram 登录通知。
- 审计新增可选 SQLite 索引、组合筛选、分页和长期检索，JSONL 继续作为持久来源。
- 新增完整面板备份与恢复，覆盖账号、安全配置、审计、快照、文件备份和回收站，并保留本机运行 Secret；支持固定 systemd 计划任务和最近 7 份保留策略。
- GitHub 助手新增 Actions Job 日志、Release Assets、PR 安全合并与最新 Head SHA 检查。
- 新增 Linux 用户与 sudo 管理、Markdown 安全预览、文件收藏/最近访问和文件所有者修改。
- 完成 v0.9 移动端表单、任务进度、备份恢复和安全中心适配。

## v0.8.0-alpha - 2026-08-01

- 新增通用配置快照中心，支持查看内容、恢复和删除；SSH、DNS、主机、Compose 与 APT 等关键操作前自动创建快照。
- APT 管理由只读检查升级为完整安全流程：模拟预检、只下载、执行升级、软件包搜索、安装和删除，并检测 dpkg/apt 锁。
- APT 升级失败时保留完整日志、快照编号并尝试执行 `dpkg --configure -a` 与依赖修复。
- 新增主机设置页面：主机名、时区、系统 DNS、Swap 创建/删除和固定 sysctl 优化预设。
- Docker Compose 支持多配置文件在线编辑、`docker compose config -q` 校验、保存前快照、失败回滚和可选立即部署。
- Docker 镜像新增可视化构建，限制构建上下文在文件管理授权目录内。
- 文件管理新增授权目录递归搜索、图片/PDF 预览、ZIP 内容浏览及 ZIP/TAR.GZ 在线压缩。
- SSH 新增端口、Root 登录、TCP 转发、Agent 转发和 X11 转发可视化设置；修改端口时保留新旧端口，验证后再关闭旧端口。
- GitHub 助手新增直接创建 Release，保留可选启用和无默认仓库策略。
- 补充 v0.8 移动端表单、快照卡片、Compose 编辑器、媒体预览和文件搜索结果布局。

## v0.7.0-alpha - 2026-08-01

- 登录页在未开启 TOTP 时不再渲染验证码输入；仅在密码验证成功且账号已启用 TOTP 后动态显示。
- Docker 未安装时新增 Debian/Ubuntu 官方软件源快捷安装入口。
- Docker 容器编辑改为全可视化环境变量、端口和挂载行，并补充网络模式、特权模式等高级项。
- GitHub 助手移至常用工具并改为可选功能，默认关闭且不预设任何所有者、仓库或分支。
- 改进 GitHub Device Flow 登录引导、Client ID 校验和未选择仓库的空状态。
- SSH 新增密码登录开关；关闭前要求存在公钥、明确确认，并通过独立配置、`sshd -t` 校验和失败回滚防止锁死。
- SSH 新增 ED25519 密钥生成、可选私钥口令、自动写入公钥及一次性私钥下载。
- Swap 卡片明确区分“未启用”“已启用但未使用”和真实使用比例，避免把 0% 误判为读取失败。
- 新增管理员用户名修改；修改后保留当前会话并退出其他设备。
- 新密码必须输入两次并通过实时强度检查；服务端拒绝常见密码、用户名衍生密码、重复字符和简单序列。
- 新增主机安全体检与安全分，并可一键启用 Fail2ban 和 Debian/Ubuntu 自动安全更新。
- Fail2ban 自动忽略内网、当前访问 IP 和已有 SSH 会话来源 IP，降低误封管理员的风险。
- 常用工具新增固定模板系统诊断，不提供任意 Shell。
- 重构移动端页面标题与操作区、表单、日志和弹窗布局，减少标题被挤成多行和按钮竖排。

## v0.6.0-alpha - 2026-08-01

- Docker 容器卡片新增 CPU、内存、网络与块设备 I/O 按需实时统计，离开页面或进入后台后自动停止采集。
- 新增 Docker 清理空间预览、安全/深度清理，并默认保护存储卷。
- 新增 Docker 网络与存储卷创建，输入参数在 Agent 端再次校验。
- 新增安全计划任务，可按小时、每天或每周重启 systemd 服务、Docker 容器或执行安全 Docker 清理。
- 计划任务使用独立 systemd service/timer，不接受任意 Shell 字符串。
- 文件编辑新增历史版本列表、文本差异对比和单文件恢复；恢复前自动保存当前版本。
- GitHub 助手新增分支创建与 Pull Request 创建，形成“新建分支 → 上传 ZIP → PR 合并”的新手工作流。
- 新增 TOTP 身份验证器、一次性恢复码、恢复码重建和关闭流程；恢复码只存哈希且使用后立即失效。
- 用统一的面板弹窗替换浏览器 alert、confirm 和 prompt，危险操作、输入与错误反馈在移动端保持一致。
- 新增 Docker、文件版本、TOTP 与计划任务回归测试，并修复文件下载响应重复关闭问题。

## v0.5.0-alpha - 2026-08-01

- 所有非首页页面新增明确返回按钮，并按信息架构返回上一级而不是退出站点。
- 修复 SPA 深层路径被静态文件服务器 301 重定向到根目录的问题；刷新 `/files`、`/docker`、`/security` 等页面不再回到概览。
- 当前路由同时保存在浏览器会话中；反向代理异常回落到根路径或重新登录后仍可恢复原页面。
- 登录成功不再强制跳回概览，而是回到刷新前或登录前的页面。
- 登录、修改密码和二次验证密码框统一增加英文键盘提示，关闭自动大写、自动纠错与拼写检查。
- 用自定义二次验证弹窗替换浏览器 prompt，错误密码可在原弹窗重试。
- 移动端“我的与安全”新增顶部退出按钮和独立“退出当前账号”区域。
- 新增 `uninstall.sh` 和本机 `lukepanel-uninstall` 命令。
- 默认卸载只移除程序与 systemd 服务并保留数据；`--purge` 可彻底删除配置、审计、备份和用户。
- Release 附件新增 `install.sh` 与 `uninstall.sh`，并纳入 SHA256 校验。
- 新增 SPA 深层路径和静态资源回归测试。

## v0.4.0-alpha - 2026-08-01

- 修复概览 Docker 卡片需要手动读取且留白过大的问题，改为进入概览后自动同步，并每 10 秒低频更新。
- Swap 改为明确显示“已用百分比 · 已用/总量”，避免 0 B 被误解为读取失败。
- 修复 hardened systemd 环境缺少 AF_NETLINK 导致网络接口页面整体报错；同时保留 sysfs 降级读取。
- 修复移动端页面顶部安全区、存储页“显示全部”按钮竖排及操作区挤压。
- SSH 用户选择改为“用户名（N 个密钥）”，改善移动端可读性。
- 新增非 Compose Docker 容器结构化编辑和安全重建回滚。
- Compose 管理容器禁止直接重建，并引导打开对应 Compose YAML。
- 文件管理新增保留目录结构的文件夹上传。
- 新增 ZIP 安全解压，限制文件数量、单文件/总大小，拒绝路径穿越和符号链接。
- 新增 GitHub OAuth Device Flow 网页登录，Token 仅保存在当前会话内存。
- 新增 GitHub ZIP 差异预览、Commit 和 Push，无需服务器安装 Git。
- GitHub 推送采用非强制分支更新；远端分支变化时自动拒绝覆盖。
- Release 构建补充嵌入最新前端步骤，避免 Release 二进制携带旧页面。
- 增加 Docker 重建、嵌套上传、ZIP 路径安全和 GitHub ZIP 规范化测试。

## v0.3.0-alpha - 2026-08-01

- 系统概览改为 SSE 实时推送，每 2 秒更新数值而不重建页面。
- 页面后台、离开概览或退出登录后自动断开实时流，失败时回退兼容刷新。
- 修复移动端底部导航遮挡服务卡片和最后一项的问题。
- 服务页默认仅显示运行中服务，新增异常/全部筛选和搜索。
- 存储页默认隐藏 Docker overlay、netns、BPF、重复绑定挂载等虚拟项目。
- 修复长挂载路径和设备路径溢出卡片的问题，并增加复制挂载路径。
- 文件管理新增面包屑跳转、复制路径、复制、移动、权限修改和多文件上传。
- 回收站新增列表、恢复到原路径/新路径、永久删除和跨文件系统支持。
- 默认文件根目录增加 `/root` 与 `/usr/local`，自定义策略不会被自动扩权。
- 新增 Docker Compose 项目识别及拉取、启动、停止、重启、下线。
- 新增 SSH 状态、登录用户和 authorized_keys 公钥管理，修改前自动备份。
- 日志审计新增搜索、单条复制、当前列表一键复制和导出。
- 审计 JSONL 达到 20MB 后自动轮转，保留 3 个历史文件。
- 新增 GitHub 新手助手：仓库状态、Tag/Release/Actions 查看、安装命令复制。
- 支持一次性 Fine-grained Token 创建 Tag 和重试失败的 Actions，Token 不持久化。
- Release Actions 增加手动运行入口，并支持幂等更新已有 Release 附件。
- 增加授权根目录写操作保护和相关测试。
- 文件编辑备份限制为 500MB / 500 份，SSH 公钥备份最多保留 100 份。

## v0.2.0-alpha - 2026-08-01

- 修复系统概览不会自动刷新的问题。
- 概览默认每 5 秒刷新，后台暂停，回到前台立即同步。
- 新增实时网络上传和下载速率。
- 新增 root Agent 与 Unix Socket 权限隔离。
- 新增 systemd 服务管理和服务日志。
- 新增进程排行、SIGTERM 和 SIGKILL。
- 新增网络接口、监听端口和存储挂载信息。
- 新增 systemd timer 和 APT 更新检查。
- 新增 Docker 容器列表、启停、重启、删除和日志。
- 新增镜像拉取/删除、网络与存储卷基础管理。
- 文件管理新增上传、下载、新建、重命名、编辑、备份和回收站。
- 新增 Ping、DNS、TCP 和 HTTP 工具。
- 新增系统日志、操作审计和会话管理。
- 高风险操作新增密码二次验证及 5 分钟授权窗口。
- 安装器支持从 v0.1 平滑升级并自动迁移配置。

## v0.1.0-alpha - 2026-08-01

- 建立纯 Go 标准库单二进制后端。
- 建立零运行依赖的响应式前端。
- 支持移动端底部导航和桌面端侧栏。
- 增加登录、会话、CSRF、限速与修改密码。
- 增加 CPU、内存、磁盘、负载、网络和运行时间概览。
- 增加 Allowed Roots 只读文件目录浏览。
- 增加 JSONL 审计记录。
- 增加 systemd 服务、安装器和 GitHub Actions。
