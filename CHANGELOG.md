# Changelog

## v0.7.0-alpha - 2026-08-01

- 登录页在未开启 TOTP 时不再渲染验证码输入；仅在密码验证成功且账号已启用 TOTP 后动态显示。
- Docker 未安装时新增 Debian/Ubuntu 官方软件源快捷安装入口。
- Docker 容器编辑改为全可视化环境变量、端口和挂载行，并补充网络模式、特权模式等高级项。
- GitHub 助手移至常用工具并改为可选功能，默认关闭且不预设任何所有者、仓库或分支。
- 改进 GitHub Device Flow 登录引导、Client ID 校验和未选择仓库的空状态。
- SSH 新增密码登录开关；关闭前要求存在公钥、明确确认，并通过独立配置、`sshd -t` 校验和失败回滚防止锁死。
- SSH 新增 ED25519 密钥生成、可选私钥口令、自动写入公钥及一次性私钥下载。
- Swap 卡片明确区分“未启用”“已启用但未使用”和真实使用比例，避免把 0% 误判为读取失败。
- 新增管理员用户名修改；修改后保留当前会话并退出其他设备。
- 新密码必须输入两次并通过实时强度检查；服务端拒绝常见密码、用户名衍生密码、重复字符和简单序列。
- 新增主机安全体检与安全分，并可一键启用 Fail2ban 和 Debian/Ubuntu 自动安全更新。
- Fail2ban 自动忽略内网、当前访问 IP 和已有 SSH 会话来源 IP，降低误封管理员的风险。
- 常用工具新增固定模板系统诊断，不提供任意 Shell。
- 重构移动端页面标题与操作区、表单、日志和弹窗布局，减少标题被挤成多行和按钮竖排。

## v0.6.0-alpha - 2026-08-01

- Docker 容器卡片新增 CPU、内存、网络与块设备 I/O 按需实时统计，离开页面或进入后台后自动停止采集。
- 新增 Docker 清理空间预览、安全/深度清理，并默认保护存储卷。
- 新增 Docker 网络与存储卷创建，输入参数在 Agent 端再次校验。
- 新增安全计划任务，可按小时、每天或每周重启 systemd 服务、Docker 容器或执行安全 Docker 清理。
- 计划任务使用独立 systemd service/timer，不接受任意 Shell 字符串。
- 文件编辑新增历史版本列表、文本差异对比和单文件恢复；恢复前自动保存当前版本。
- GitHub 助手新增分支创建与 Pull Request 创建，形成“新建分支 → 上传 ZIP → PR 合并”的新手工作流。
- 新增 TOTP 身份验证器、一次性恢复码、恢复码重建和关闭流程；恢复码只存哈希且使用后立即失效。
- 用统一的面板弹窗替换浏览器 alert、confirm 和 prompt，危险操作、输入与错误反馈在移动端保持一致。
- 新增 Docker、文件版本、TOTP 与计划任务回归测试，并修复文件下载响应重复关闭问题。

## v0.5.0-alpha - 2026-08-01

- 所有非首页页面新增明确返回按钮，并按信息架构返回上一级而不是退出站点。
- 修复 SPA 深层路径被静态文件服务器 301 重定向到根目录的问题；刷新 `/files`、`/docker`、`/security` 等页面不再回到概览。
- 当前路由同时保存在浏览器会话中；反向代理异常回落到根路径或重新登录后仍可恢复原页面。
- 登录成功不再强制跳回概览，而是回到刷新前或登录前的页面。
- 登录、修改密码和二次验证密码框统一增加英文键盘提示，关闭自动大写、自动纠错与拼写检查。
- 用自定义二次验证弹窗替换浏览器 prompt，错误密码可在原弹窗重试。
- 移动端“我的与安全”新增顶部退出按钮和独立“退出当前账号”区域。
- 新增 `uninstall.sh` 和本机 `lukepanel-uninstall` 命令。
- 默认卸载只移除程序与 systemd 服务并保留数据；`--purge` 可彻底删除配置、审计、备份和用户。
- Release 附件新增 `install.sh` 与 `uninstall.sh`，并纳入 SHA256 校验。
- 新增 SPA 深层路径和静态资源回归测试。

## v0.4.0-alpha - 2026-08-01

- 修复概览 Docker 卡片需要手动读取且留白过大的问题，改为进入概览后自动同步，并每 10 秒低频更新。
- Swap 改为明确显示“已用百分比 · 已用/总量”，避免 0 B 被误解为读取失败。
- 修复 hardened systemd 环境缺少 AF_NETLINK 导致网络接口页面整体报错；同时保留 sysfs 降级读取。
- 修复移动端页面顶部安全区、存储页“显示全部”按钮竖排及操作区挤压。
- SSH 用户选择改为“用户名（N 个密钥）”，改善移动端可读性。
- 新增非 Compose Docker 容器结构化编辑和安全重建回滚。
- Compose 管理容器禁止直接重建，并引导打开对应 Compose YAML。
- 文件管理新增保留目录结构的文件夹上传。
- 新增 ZIP 安全解压，限制文件数量、单文件/总大小，拒绝路径穿越和符号链接。
- 新增 GitHub OAuth Device Flow 网页登录，Token 仅保存在当前会话内存。
- 新增 GitHub ZIP 差异预览、Commit 和 Push，无需服务器安装 Git。
- GitHub 推送采用非强制分支更新；远端分支变化时自动拒绝覆盖。
- Release 构建补充嵌入最新前端步骤，避免 Release 二进制携带旧页面。
- 增加 Docker 重建、嵌套上传、ZIP 路径安全和 GitHub ZIP 规范化测试。

## v0.3.0-alpha - 2026-08-01

- 系统概览改为 SSE 实时推送，每 2 秒更新数值而不重建页面。
- 页面后台、离开概览或退出登录后自动断开实时流，失败时回退兼容刷新。
- 修复移动端底部导航遮挡服务卡片和最后一项的问题。
- 服务页默认仅显示运行中服务，新增异常/全部筛选和搜索。
- 存储页默认隐藏 Docker overlay、netns、BPF、重复绑定挂载等虚拟项目。
- 修复长挂载路径和设备路径溢出卡片的问题，并增加复制挂载路径。
- 文件管理新增面包屑跳转、复制路径、复制、移动、权限修改和多文件上传。
- 回收站新增列表、恢复到原路径/新路径、永久删除和跨文件系统支持。
- 默认文件根目录增加 `/root` 与 `/usr/local`，自定义策略不会被自动扩权。
- 新增 Docker Compose 项目识别及拉取、启动、停止、重启、下线。
- 新增 SSH 状态、登录用户和 authorized_keys 公钥管理，修改前自动备份。
- 日志审计新增搜索、单条复制、当前列表一键复制和导出。
- 审计 JSONL 达到 20MB 后自动轮转，保留 3 个历史文件。
- 新增 GitHub 新手助手：仓库状态、Tag/Release/Actions 查看、安装命令复制。
- 支持一次性 Fine-grained Token 创建 Tag 和重试失败的 Actions，Token 不持久化。
- Release Actions 增加手动运行入口，并支持幂等更新已有 Release 附件。
- 增加授权根目录写操作保护和相关测试。
- 文件编辑备份限制为 500MB / 500 份，SSH 公钥备份最多保留 100 份。

## v0.2.0-alpha - 2026-08-01

- 修复系统概览不会自动刷新的问题。
- 概览默认每 5 秒刷新，后台暂停，回到前台立即同步。
- 新增实时网络上传和下载速率。
- 新增 root Agent 与 Unix Socket 权限隔离。
- 新增 systemd 服务管理和服务日志。
- 新增进程排行、SIGTERM 和 SIGKILL。
- 新增网络接口、监听端口和存储挂载信息。
- 新增 systemd timer 和 APT 更新检查。
- 新增 Docker 容器列表、启停、重启、删除和日志。
- 新增镜像拉取/删除、网络与存储卷基础管理。
- 文件管理新增上传、下载、新建、重命名、编辑、备份和回收站。
- 新增 Ping、DNS、TCP 和 HTTP 工具。
- 新增系统日志、操作审计和会话管理。
- 高风险操作新增密码二次验证及 5 分钟授权窗口。
- 安装器支持从 v0.1 平滑升级并自动迁移配置。

## v0.1.0-alpha - 2026-08-01

- 建立纯 Go 标准库单二进制后端。
- 建立零运行依赖的响应式前端。
- 支持移动端底部导航和桌面端侧栏。
- 增加登录、会话、CSRF、限速与修改密码。
- 增加 CPU、内存、磁盘、负载、网络和运行时间概览。
- 增加 Allowed Roots 只读文件目录浏览。
- 增加 JSONL 审计记录。
- 增加 systemd 服务、安装器和 GitHub Actions。
