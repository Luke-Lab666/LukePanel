{
  "name": "lukepanel-frontend",
  "private": true,
  "version": "2.0.5",
  "description": "LukePanel React 18 management console",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "bash ../scripts/build-frontend.sh",
    "test:static": "python3 ../tests/static/validate.py"
  },
  "engines": {
    "node": ">=20"
  },
  "lukepanel": {
    "framework": "React",
    "react": "18.2.0",
    "build": "offline-vendored"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
