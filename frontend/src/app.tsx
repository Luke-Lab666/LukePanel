/* LukePanel v2.0.0 React 18 frontend. No runtime CDN, no direct DOM templating. */
(() => {
  'use strict';

  const { useCallback, useEffect, useMemo, useRef, useState } = React;

  type Dict<T = unknown> = Record<string, T>;
  type RouteDef = { path: string; title: string; subtitle: string; level: 1 | 2; parent?: string; nav?: boolean; icon: string };
  type ToastKind = 'success' | 'error' | 'warning' | 'info';
  type ToastItem = { id: number; kind: ToastKind; title: string; detail?: string };
  type ModalState = { title: string; content: any; size?: 'small' | 'medium' | 'large'; destructive?: boolean } | null;
  type Identity = { username?: string; csrf_token?: string; session_id?: string };
  type ConfirmFn = (title: string, message: string, actionLabel?: string, destructive?: boolean) => Promise<boolean>;

  const VERSION = window.__LUKEPANEL_VERSION__ || 'v2.0.0';
  const ROUTES: RouteDef[] = [
    { path: '/', title: '概览', subtitle: '服务器状态与关键指标', level: 1, nav: true, icon: 'dashboard' },
    { path: '/system', title: '系统', subtitle: '主机、服务、网络与维护', level: 1, nav: true, icon: 'system' },
    { path: '/system/services', title: '服务管理', subtitle: 'systemd 服务状态、控制与日志', level: 2, parent: '/system', icon: 'services' },
    { path: '/system/processes', title: '进程管理', subtitle: '进程资源、筛选与信号操作', level: 2, parent: '/system', icon: 'process' },
    { path: '/system/network', title: '网络状态', subtitle: '接口、地址与监听端口', level: 2, parent: '/system', icon: 'network' },
    { path: '/system/storage', title: '存储空间', subtitle: '文件系统、挂载点与容量', level: 2, parent: '/system', icon: 'storage' },
    { path: '/system/tasks', title: '计划任务', subtitle: '计划任务与 systemd timers', level: 2, parent: '/system', icon: 'tasks' },
    { path: '/system/updates', title: '软件管理', subtitle: 'APT 更新、软件包与软件源', level: 2, parent: '/system', icon: 'package' },
    { path: '/system/host', title: '主机设置', subtitle: '主机名、时区、DNS、Swap 与内核参数', level: 2, parent: '/system', icon: 'server' },
    { path: '/system/snapshots', title: '备份与快照', subtitle: '配置导出、导入与恢复点', level: 2, parent: '/system', icon: 'backup' },
    { path: '/docker', title: 'Docker', subtitle: '容器、镜像、Compose、网络与卷', level: 1, nav: true, icon: 'docker' },
    { path: '/files', title: '文件管理', subtitle: '浏览、编辑、上传与归档', level: 1, nav: true, icon: 'files' },
    { path: '/tools', title: '常用工具', subtitle: '网络诊断与安全维护工具', level: 1, nav: true, icon: 'tools' },
    { path: '/tools/github', title: 'GitHub 助手', subtitle: '仓库、Actions、Release 与导入', level: 2, parent: '/tools', icon: 'github' },
    { path: '/ssh', title: 'SSH 管理', subtitle: '登录方式、用户、密钥与端口', level: 1, nav: true, icon: 'ssh' },
    { path: '/audit', title: '日志中心', subtitle: '系统日志与操作审计', level: 1, nav: true, icon: 'audit' },
    { path: '/security', title: '安全中心', subtitle: '防火墙、Fail2ban 与登录防护', level: 1, nav: true, icon: 'security' },
    { path: '/settings', title: '我的', subtitle: '账户、认证、会话与面板设置', level: 1, nav: true, icon: 'settings' }
  ];
  const ROUTE_MAP = new Map(ROUTES.map(route => [route.path, route]));
  const LEGACY_REDIRECTS: Dict<string> = {
    '/services': '/system/services', '/processes': '/system/processes', '/network': '/system/network',
    '/storage': '/system/storage', '/tasks': '/system/tasks', '/updates': '/system/updates',
    '/host': '/system/host', '/snapshots': '/system/snapshots', '/github': '/tools/github'
  };
  const PRIMARY_ROUTES = ROUTES.filter(route => route.nav);
  const MOBILE_ROUTES = ['/', '/system', '/docker', '/files', '/settings'];

  const ICONS: Dict<string[]> = {
    dashboard: ['M4 13h6V4H4v9Z', 'M14 20h6v-9h-6v9Z', 'M14 4h6v3h-6V4Z', 'M4 17h6v3H4v-3Z'],
    system: ['M4 4h16v16H4z', 'M8 8h8', 'M8 12h5', 'M8 16h3'],
    services: ['M12 3v3', 'M12 18v3', 'M3 12h3', 'M18 12h3', 'M6.6 6.6l2.1 2.1', 'M15.3 15.3l2.1 2.1', 'M17.4 6.6l-2.1 2.1', 'M8.7 15.3l-2.1 2.1', 'M12 16a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z'],
    process: ['M4 7h16', 'M7 4v6', 'M4 17h16', 'M17 14v6', 'M10 12h4'],
    network: ['M5 12.6a10 10 0 0 1 14 0', 'M8.5 16a5 5 0 0 1 7 0', 'M12 20h.01', 'M2 9a15 15 0 0 1 20 0'],
    storage: ['M4 6c0-1.1 3.6-2 8-2s8 .9 8 2-3.6 2-8 2-8-.9-8-2Z', 'M4 6v6c0 1.1 3.6 2 8 2s8-.9 8-2V6', 'M4 12v6c0 1.1 3.6 2 8 2s8-.9 8-2v-6'],
    tasks: ['M6 3v3', 'M18 3v3', 'M4 8h16', 'M5 5h14a1 1 0 0 1 1 1v14H4V6a1 1 0 0 1 1-1Z', 'M8 12h3', 'M8 16h5'],
    package: ['M4 7 12 3l8 4-8 4-8-4Z', 'M4 7v10l8 4 8-4V7', 'M12 11v10'],
    server: ['M4 4h16v6H4z', 'M4 14h16v6H4z', 'M8 7h.01', 'M8 17h.01'],
    backup: ['M4 4v6h6', 'M5.5 15a8 8 0 1 0 1-8.5L4 10', 'M12 8v5l3 2'],
    docker: ['M4 11h16c0 5-3 9-8 9s-8-4-8-9Z', 'M7 8h3v3H7z', 'M11 8h3v3h-3z', 'M15 8h3v3h-3z', 'M11 4h3v3h-3z', 'M20 9c1-1 2-1 3-1'],
    files: ['M3 6h7l2 2h9v12H3z', 'M3 6V4h7l2 2'],
    tools: ['M14 6a4 4 0 0 0-5 5L3 17l4 4 6-6a4 4 0 0 0 5-5l-3 3-3-3 3-4Z'],
    github: ['M12 2a10 10 0 0 0-3 19.5c.5.1.7-.2.7-.5v-2c-2.8.6-3.4-1.2-3.4-1.2-.5-1.2-1.2-1.5-1.2-1.5-1-.7.1-.7.1-.7 1.1.1 1.7 1.1 1.7 1.1 1 1.7 2.7 1.2 3.4.9.1-.7.4-1.2.7-1.5-2.2-.3-4.5-1.1-4.5-5A4 4 0 0 1 6 8.2 3.7 3.7 0 0 1 6.1 5s.9-.3 3 1.1a10.5 10.5 0 0 1 5.5 0c2.1-1.4 3-1.1 3-1.1a3.7 3.7 0 0 1 .1 3.2 4 4 0 0 1 1.1 2.8c0 3.9-2.3 4.7-4.5 5 .4.3.7 1 .7 2v3c0 .3.2.6.7.5A10 10 0 0 0 12 2Z'],
    ssh: ['M4 5h16v14H4z', 'm8 9 3 3-3 3', 'M13 15h3'],
    audit: ['M6 4h12v16H6z', 'M9 8h6', 'M9 12h6', 'M9 16h4'],
    security: ['M12 3 4 6v5c0 5.2 3.4 8.4 8 10 4.6-1.6 8-4.8 8-10V6l-8-3Z', 'm9 12 2 2 4-4'],
    settings: ['M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8Z', 'M4.9 19.1 7 17', 'M17 7l2.1-2.1', 'M4.9 4.9 7 7', 'M17 17l2.1 2.1', 'M2 12h3', 'M19 12h3', 'M12 2v3', 'M12 19v3'],
    back: ['m15 18-6-6 6-6'], refresh: ['M20 11a8 8 0 1 0-2.3 5.7', 'M20 4v7h-7'], menu: ['M4 7h16', 'M4 12h16', 'M4 17h16'],
    close: ['M6 6l12 12', 'M18 6 6 18'], search: ['M11 18a7 7 0 1 1 0-14 7 7 0 0 1 0 14Z', 'm20 20-4-4'],
    plus: ['M12 5v14', 'M5 12h14'], play: ['m8 5 11 7-11 7Z'], stop: ['M7 7h10v10H7z'], restart: ['M20 11a8 8 0 1 0-2 5', 'm20 4v7h-7'],
    trash: ['M4 7h16', 'M9 7V4h6v3', 'm7 7 1 13h8l1-13', 'M10 11v5', 'M14 11v5'], edit: ['m4 20 4-1 10-10-3-3L5 16l-1 4Z', 'm13 5 3 3'],
    download: ['M12 3v12', 'm7 10 5 5 5-5', 'M5 21h14'], upload: ['M12 21V9', 'm7 14 5-5 5 5', 'M5 3h14'],
    chevron: ['m9 18 6-6-6-6'], check: ['m5 12 4 4L19 6'], warning: ['M12 3 2 21h20L12 3Z', 'M12 9v5', 'M12 18h.01'],
    terminal: ['m5 7 4 4-4 4', 'M11 17h8'], user: ['M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z', 'M4 21a8 8 0 0 1 16 0'],
    key: ['M21 2 13.6 9.4', 'M15 4l5 5', 'M11 13a5 5 0 1 1-7-7 5 5 0 0 1 7 7Z'], copy: ['M8 8h12v12H8z', 'M4 4h12v4', 'M4 4v12h4'],
    info: ['M12 22a10 10 0 1 0 0-20 10 10 0 0 0 0 20Z', 'M12 10v7', 'M12 7h.01'], more: ['M5 12h.01', 'M12 12h.01', 'M19 12h.01']
  };

  function Icon({ name, size = 20, className = '' }: { name: string; size?: number; className?: string }) {
    const paths = ICONS[name] || ICONS.info;
    return <svg className={`icon ${className}`} width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{paths.map((path, index) => <path key={index} d={path} />)}</svg>;
  }

  function asObject(value: unknown): Dict<any> { return value && typeof value === 'object' && !Array.isArray(value) ? value as Dict<any> : {}; }
  function listOf<T>(value: unknown, ...keys: string[]): T[] {
    if (Array.isArray(value)) return value as T[];
    const object = asObject(value);
    for (const key of keys) if (Array.isArray(object[key])) return object[key] as T[];
    return [];
  }
  function boolText(value: unknown, yes = '已启用', no = '未启用'): string { return value ? yes : no; }
  function text(value: unknown, fallback = '—'): string { return value === undefined || value === null || value === '' ? fallback : String(value); }
  function number(value: unknown, fallback = 0): number { const out = Number(value); return Number.isFinite(out) ? out : fallback; }
  function formatBytes(value: unknown): string {
    let bytes = number(value); if (bytes <= 0) return '0 B';
    const units = ['B', 'KB', 'MB', 'GB', 'TB', 'PB']; let index = 0;
    while (bytes >= 1024 && index < units.length - 1) { bytes /= 1024; index++; }
    return `${bytes >= 100 || index === 0 ? bytes.toFixed(0) : bytes.toFixed(1)} ${units[index]}`;
  }
  function formatDuration(value: unknown): string {
    let seconds = Math.max(0, Math.floor(number(value))); const days = Math.floor(seconds / 86400); seconds %= 86400;
    const hours = Math.floor(seconds / 3600); seconds %= 3600; const minutes = Math.floor(seconds / 60);
    return [days ? `${days}天` : '', hours ? `${hours}小时` : '', minutes ? `${minutes}分钟` : '', !days && !hours && !minutes ? `${seconds}秒` : ''].filter(Boolean).join(' ');
  }
  function formatDate(value: unknown): string { if (!value) return '—'; const date = new Date(String(value)); return Number.isNaN(date.valueOf()) ? String(value) : date.toLocaleString('zh-CN', { hour12: false }); }
  function query(params: Dict<unknown>): string { const search = new URLSearchParams(); Object.entries(params).forEach(([key, value]) => { if (value !== undefined && value !== null && value !== '') search.set(key, String(value)); }); const out = search.toString(); return out ? `?${out}` : ''; }
  function jsonBody(value: unknown): string { return JSON.stringify(value); }
  function lines(value: string): string[] { return value.split(/\r?\n/).map(item => item.trim()).filter(Boolean); }
  function truncate(value: unknown, length = 80): string { const out = text(value, ''); return out.length > length ? `${out.slice(0, length)}…` : out; }
  function taskTypeLabel(type: unknown): string { return ({ 'service-restart': '重启 systemd 服务', 'docker-restart': '重启 Docker 容器', 'docker-cleanup-safe': '安全清理 Docker', 'panel-backup': '创建面板备份' } as Dict<string>)[String(type)] || text(type); }
  function taskTargetLabel(task: any): string { if (task.type === 'docker-cleanup-safe') return '删除未使用且不影响运行容器的 Docker 资源'; if (task.type === 'panel-backup') return '保存到定时备份目录'; return text(task.target); }
  function taskScheduleLabel(task: any): string { const minute = String(number(task.minute)).padStart(2, '0'); const hour = String(number(task.hour)).padStart(2, '0'); if (task.frequency === 'hourly') return `每小时第 ${minute} 分钟`; if (task.frequency === 'weekly') { const days = ['周日','周一','周二','周三','周四','周五','周六']; return `${days[number(task.weekday)] || '每周'} ${hour}:${minute}`; } return `每天 ${hour}:${minute}`; }
  function copyText(value: string): Promise<void> { return navigator.clipboard?.writeText(value) || Promise.reject(new Error('浏览器不支持剪贴板')); }

  class ApiError extends Error {
    status: number; code: string; command: string; output: string;
    constructor(message: string, status: number, payload: Dict<any>) { super(message); this.name = 'ApiError'; this.status = status; this.code = text(payload.code, ''); this.command = text(payload.command || payload.details?.command, ''); this.output = text(payload.output || payload.details?.output, ''); }
    get detail(): string { return [this.message, this.command ? `执行命令：${this.command}` : '', this.output ? `命令输出：\n${this.output}` : ''].filter(Boolean).join('\n\n'); }
  }
  let csrfToken = '';
  let unauthorizedHandler: (() => void) | null = null;
  let elevationHandler: (() => Promise<void>) | null = null;
  async function parsePayload(response: Response): Promise<any> { const type = response.headers.get('content-type') || ''; if (type.includes('application/json')) return response.json().catch(() => ({})); return response.text().catch(() => ''); }
  async function api<T = any>(url: string, init: RequestInit = {}): Promise<T> {
    const headers = new Headers(init.headers); const method = (init.method || 'GET').toUpperCase();
    if (init.body != null && !(init.body instanceof FormData) && !headers.has('Content-Type')) headers.set('Content-Type', 'application/json');
    if (!['GET', 'HEAD', 'OPTIONS'].includes(method) && csrfToken) headers.set('X-CSRF-Token', csrfToken);
    let response: Response;
    try { response = await fetch(url, { ...init, headers, credentials: 'same-origin' }); }
    catch (cause) { throw new ApiError(cause instanceof Error ? cause.message : '网络连接失败', 0, {}); }
    const payload = await parsePayload(response);
    if (response.status === 401) unauthorizedHandler?.();
    if (!response.ok) { const body = asObject(payload); throw new ApiError(text(body.error, `请求失败（${response.status}）`), response.status, body); }
    return payload as T;
  }
  async function secureApi<T = any>(url: string, init: RequestInit = {}): Promise<T> {
    try { return await api<T>(url, init); }
    catch (cause) {
      if (!(cause instanceof ApiError) || cause.status !== 403 || !cause.message.includes('二次验证') || !elevationHandler) throw cause;
      await elevationHandler(); return api<T>(url, init);
    }
  }
  function errorDetail(cause: unknown): string { return cause instanceof ApiError ? cause.detail : cause instanceof Error ? cause.message : String(cause || '未知错误'); }

  function useRoute() {
    const normalize = useCallback((path: string) => LEGACY_REDIRECTS[path] || (ROUTE_MAP.has(path) ? path : '/'), []);
    const [path, setPath] = useState<string>(() => normalize(window.__LUKEPANEL_TEST_PATH__ || location.pathname));
    useEffect(() => {
      if (location.origin !== 'null' && location.pathname !== path) history.replaceState({}, '', path);
      const onPop = () => setPath(normalize(location.pathname));
      window.addEventListener('popstate', onPop); return () => window.removeEventListener('popstate', onPop);
    }, [normalize, path]);
    const navigate = useCallback((next: string, replace = false) => {
      const target = normalize(next); if (target === path && !replace) return;
      if (location.origin !== 'null') history[replace ? 'replaceState' : 'pushState']({}, '', target); setPath(target); window.scrollTo({ top: 0, behavior: 'auto' });
    }, [normalize, path]);
    return { path, navigate };
  }

  function useResource<T>(loader: () => Promise<T>, deps: any[] = []) {
    const [data, setData] = useState<T | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
    const generation = useRef(0);
    const reload = useCallback(async (silent = false) => {
      const current = ++generation.current; if (!silent) setLoading(true); setError('');
      try { const result = await loader(); if (current === generation.current) setData(result); }
      catch (cause) { if (current === generation.current) setError(errorDetail(cause)); }
      finally { if (current === generation.current) setLoading(false); }
    }, deps);
    useEffect(() => { void reload(); return () => { generation.current++; }; }, [reload]);
    return { data, setData, loading, error, reload };
  }

  function Button({ children, icon, tone = 'default', busy = false, disabled = false, type = 'button', onClick, className = '', title, ariaLabel, ...buttonProps }: { children?: any; icon?: string; tone?: 'default' | 'primary' | 'danger' | 'ghost'; busy?: boolean; disabled?: boolean; type?: 'button' | 'submit'; onClick?: (event: any) => void; className?: string; title?: string; ariaLabel?: string; [key: string]: any }) {
    const resolvedAriaLabel = ariaLabel || buttonProps['aria-label'] || (!children ? title : undefined);
    return <button {...buttonProps} type={type} title={title} aria-label={resolvedAriaLabel} className={`button button-${tone} ${className}`} disabled={disabled || busy} onClick={onClick}>{busy ? <span className="spinner" /> : icon ? <Icon name={icon} size={17} /> : null}{children ? <span>{children}</span> : null}</button>;
  }
  function Badge({ children, tone = 'neutral', title }: { children: any; tone?: 'neutral' | 'success' | 'warning' | 'danger' | 'info'; title?: string }) { return <span className={`badge badge-${tone}`} title={title}>{children}</span>; }
  function Card({ children, className = '' }: { children: any; className?: string }) { return <section className={`card ${className}`}>{children}</section>; }
  function SectionTitle({ title, subtitle, actions }: { title: string; subtitle?: string; actions?: any }) { return <div className="section-title"><div><h2>{title}</h2>{subtitle ? <p>{subtitle}</p> : null}</div>{actions ? <div className="section-actions">{actions}</div> : null}</div>; }
  function Field({ label, hint, children, className = '' }: { label: string; hint?: string; children: any; className?: string }) { return <label className={`field ${className}`}><span className="field-label">{label}</span>{children}{hint ? <small>{hint}</small> : null}</label>; }
  function TextInput(props: any) { return <input {...props} className={`input ${props.className || ''}`} />; }
  function SelectInput(props: any) { return <select {...props} className={`input ${props.className || ''}`}>{props.children}</select>; }
  function TextArea(props: any) { return <textarea {...props} className={`input textarea ${props.className || ''}`} />; }
  function Toggle({ checked, onChange, label, description, disabled = false }: { checked: boolean; onChange: (checked: boolean) => void; label: string; description?: string; disabled?: boolean }) {
    return <label className={`toggle-row ${disabled ? 'is-disabled' : ''}`}><span><strong>{label}</strong>{description ? <small>{description}</small> : null}</span><input type="checkbox" checked={checked} disabled={disabled} onChange={(event: any) => onChange(event.target.checked)} /><span className="switch" /></label>;
  }
  function EmptyState({ icon = 'info', title, description, action }: { icon?: string; title: string; description?: string; action?: any }) { return <div className="empty-state"><span className="empty-icon"><Icon name={icon} size={27} /></span><strong>{title}</strong>{description ? <p>{description}</p> : null}{action}</div>; }
  function ErrorState({ error, retry }: { error: string; retry?: () => void }) { return <div className="error-state"><Icon name="warning" size={22} /><div><strong>加载失败</strong><pre>{error}</pre></div>{retry ? <Button tone="ghost" onClick={retry}>重试</Button> : null}</div>; }
  function LoadingState({ rows = 4 }: { rows?: number }) { return <div className="skeleton-list">{Array.from({ length: rows }, (_, index) => <div className="skeleton-row" key={index}><span /><span /></div>)}</div>; }
  function Progress({ value, tone = 'primary' }: { value: number; tone?: 'primary' | 'warning' | 'danger' }) { const safe = Math.max(0, Math.min(100, value)); return <div className="progress"><span className={`progress-${tone}`} style={{ width: `${safe}%` }} /></div>; }
  function KeyValue({ label, value, mono = false }: { label: string; value: any; mono?: boolean }) { return <div className="key-value"><dt>{label}</dt><dd className={mono ? 'mono' : ''}>{value}</dd></div>; }
  function Terminal({ children, maxHeight = 420 }: { children: any; maxHeight?: number }) { return <pre className="terminal" style={{ maxHeight }}>{children || '暂无输出'}</pre>; }
  function JsonDetails({ data, title = '原始数据' }: { data: unknown; title?: string }) { return <details className="json-details"><summary>{title}</summary><Terminal>{JSON.stringify(data, null, 2)}</Terminal></details>; }

  function PageHeader({ route, navigate, busy, onRefresh, actions }: { route: RouteDef; navigate: (path: string) => void; busy?: boolean; onRefresh?: () => void; actions?: any }) {
    return <header className="page-header">
      <div className="page-heading">
        {route.parent ? <button className="back-button" onClick={() => navigate(route.parent!)} aria-label={`返回${ROUTE_MAP.get(route.parent)?.title || '上级页面'}`}><Icon name="back" size={21} /></button> : null}
        <div><h1>{route.title}</h1><p>{route.subtitle}</p></div>
      </div>
      <div className="page-actions">{actions}{onRefresh ? <Button tone="ghost" icon="refresh" busy={busy} onClick={onRefresh}>刷新</Button> : null}</div>
    </header>;
  }

  function Modal({ state, onClose, returnFocus }: { state: ModalState; onClose: () => void; returnFocus?: HTMLElement | null }) {
    const dialogRef = useRef<HTMLElement | null>(null);
    const onCloseRef = useRef(onClose);
    useEffect(() => { onCloseRef.current = onClose; }, [onClose]);
    useEffect(() => {
      if (!state) return;
      const previousFocus = returnFocus || (document.activeElement instanceof HTMLElement ? document.activeElement : null);
      const dialog = dialogRef.current;
      const focusables = () => Array.from(dialog?.querySelectorAll<HTMLElement>('button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), a[href], [tabindex]:not([tabindex="-1"])') || []).filter(element => element.offsetParent !== null);
      const initial = dialog?.querySelector<HTMLElement>('[autofocus]') || focusables()[0] || dialog;
      const focusTimer = window.setTimeout(() => initial?.focus(), 0);
      const onKey = (event: KeyboardEvent) => {
        if (event.key === 'Escape') { event.preventDefault(); onCloseRef.current(); return; }
        if (event.key !== 'Tab') return;
        const items = focusables();
        if (!items.length) { event.preventDefault(); dialog?.focus(); return; }
        const first = items[0]; const last = items[items.length - 1];
        if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); }
        else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); }
      };
      document.body.classList.add('modal-open'); window.addEventListener('keydown', onKey);
      return () => {
        window.clearTimeout(focusTimer); document.body.classList.remove('modal-open'); window.removeEventListener('keydown', onKey);
        window.requestAnimationFrame(() => window.requestAnimationFrame(() => {
          if (!document.querySelector('.modal') && previousFocus?.isConnected) previousFocus.focus({ preventScroll: true });
        }));
      };
    }, [state, returnFocus]);
    if (!state) return null;
    return <div className="modal-backdrop" role="presentation" onMouseDown={(event: any) => { if (event.target === event.currentTarget) onClose(); }}><section ref={dialogRef} tabIndex={-1} className={`modal modal-${state.size || 'medium'}`} role="dialog" aria-modal="true" aria-labelledby="modal-title"><header><h2 id="modal-title">{state.title}</h2><button className="icon-button" onClick={onClose} aria-label="关闭"><Icon name="close" /></button></header><div className="modal-body">{state.content}</div></section></div>;
  }
  function Toasts({ items, dismiss }: { items: ToastItem[]; dismiss: (id: number) => void }) { return <div className="toast-stack" aria-live="polite">{items.map(item => <article className={`toast toast-${item.kind}`} key={item.id}><Icon name={item.kind === 'success' ? 'check' : item.kind === 'error' ? 'warning' : 'info'} size={20} /><div><strong>{item.title}</strong>{item.detail ? <pre>{item.detail}</pre> : null}</div><button onClick={() => dismiss(item.id)} aria-label="关闭"><Icon name="close" size={16} /></button></article>)}</div>; }

  type PageProps = {
    route: RouteDef; navigate: (path: string) => void; notify: (kind: ToastKind, title: string, detail?: string) => void;
    openModal: (state: NonNullable<ModalState>) => void; closeModal: () => void; confirm: (title: string, message: string, actionLabel?: string, destructive?: boolean) => Promise<boolean>;
  };

  function LoginPage({ onAuthenticated, notify }: { onAuthenticated: (identity: Identity) => void; notify: (kind: ToastKind, title: string, detail?: string) => void }) {
    const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [totp, setTotp] = useState(''); const [totpRequired, setTotpRequired] = useState(false); const [busy, setBusy] = useState(false);
    const submit = async (event: any) => { event.preventDefault(); setBusy(true); try { const result = await api<Dict<any>>('/api/v1/auth/login', { method: 'POST', body: jsonBody({ username: username.trim(), password, otp: totp.trim() }) }); if (result.totp_required) { setTotpRequired(true); notify('info', '请输入两步验证码'); return; } onAuthenticated(result); } catch (cause) { if (cause instanceof ApiError && cause.code === 'totp_required') { setTotpRequired(true); notify('info', '请输入两步验证码'); } else notify('error', '登录失败', errorDetail(cause)); } finally { setBusy(false); } };
    const passkey = async () => { setBusy(true); try {
      const begin = await api<Dict<any>>('/api/v1/auth/passkey/login/begin', { method: 'POST', body: jsonBody({ username: username.trim() }) });
      const publicKey = normalizeRequestOptions(begin.public_key || begin.options || begin);
      const credential = await navigator.credentials.get({ publicKey }) as PublicKeyCredential | null; if (!credential) throw new Error('未取得 Passkey 凭据');
      const result = await api<Identity>('/api/v1/auth/passkey/login/finish', { method: 'POST', body: jsonBody({ flow_id: begin.flow_id, credential: serializeCredential(credential) }) }); onAuthenticated(result);
    } catch (cause) { notify('error', 'Passkey 登录失败', errorDetail(cause)); } finally { setBusy(false); } };
    return <main className="login-layout"><section className="login-brand"><div className="brand-mark"><img src="/assets/lukepanel-icon-192.png" alt="" /><div><strong>LukePanel</strong><span>SERVER CONTROL</span></div></div><div className="login-hero"><span className="eyebrow">LIGHTWEIGHT SERVER CONTROL</span><h1>把服务器管理，收进一个清晰的界面。</h1><p>LukePanel 专注于 VPS 与家庭服务器的日常运维，所有高风险操作都要求二次确认。</p></div><footer>LukePanel {VERSION} · React 18</footer></section><section className="login-panel"><form className="login-card" onSubmit={submit}><div className="login-card-title"><img src="/assets/favicon-64.png" alt="" /><div><h2>登录 LukePanel</h2><p>使用面板账户继续</p></div></div><Field label="用户名"><TextInput name="username" autoCapitalize="none" autoCorrect="off" autoComplete="username" value={username} onChange={(event: any) => setUsername(event.target.value)} required /></Field><Field label="密码"><TextInput name="password" type="password" autoComplete="current-password" value={password} onChange={(event: any) => setPassword(event.target.value)} required /></Field>{totpRequired ? <Field label="两步验证码" hint="输入验证器中的 6 位验证码或恢复码"><TextInput name="otp" inputMode="numeric" autoComplete="one-time-code" value={totp} onChange={(event: any) => setTotp(event.target.value)} autoFocus required /></Field> : null}<Button type="submit" tone="primary" busy={busy} className="full-width">登录</Button>{typeof PublicKeyCredential !== 'undefined' ? <><div className="login-divider"><span>或</span></div><Button icon="key" busy={busy} onClick={passkey} className="full-width">使用 Passkey</Button></> : null}<p className="login-note"><Icon name="security" size={16} />登录状态仅保存在当前浏览器安全会话中</p></form></section></main>;
  }

  function normalizeRequestOptions(options: any): PublicKeyCredentialRequestOptions {
    const allow = options.allowCredentials || options.allow_credentials || [];
    return {
      challenge: typeof options.challenge === 'string' ? base64urlToBuffer(options.challenge) : options.challenge,
      rpId: options.rpId || options.rp_id,
      timeout: options.timeout || 60000,
      userVerification: options.userVerification || options.user_verification || 'preferred',
      allowCredentials: allow.map((item: any) => ({ type: item.type || 'public-key', id: typeof item.id === 'string' ? base64urlToBuffer(item.id) : item.id, transports: item.transports }))
    };
  }
  function normalizeCreationOptions(options: any): PublicKeyCredentialCreationOptions {
    const user = options.user || {}; const selection = options.authenticatorSelection || options.authenticator_selection;
    const excluded = options.excludeCredentials || options.exclude_credentials || [];
    return {
      challenge: typeof options.challenge === 'string' ? base64urlToBuffer(options.challenge) : options.challenge,
      rp: options.rp,
      user: { id: typeof user.id === 'string' ? base64urlToBuffer(user.id) : user.id, name: user.name, displayName: user.displayName || user.display_name || user.name },
      pubKeyCredParams: options.pubKeyCredParams || options.pub_key_cred_params || [{ type: 'public-key', alg: -7 }],
      timeout: options.timeout || 60000,
      attestation: options.attestation || 'none',
      authenticatorSelection: selection ? { residentKey: selection.residentKey || selection.resident_key, userVerification: selection.userVerification || selection.user_verification } : undefined,
      excludeCredentials: excluded.map((item: any) => ({ type: item.type || 'public-key', id: typeof item.id === 'string' ? base64urlToBuffer(item.id) : item.id, transports: item.transports }))
    };
  }
  function base64urlToBuffer(value: string): ArrayBuffer { const normalized = value.replace(/-/g, '+').replace(/_/g, '/'); const padded = normalized + '='.repeat((4 - normalized.length % 4) % 4); const binary = atob(padded); const bytes = new Uint8Array(binary.length); for (let index = 0; index < binary.length; index++) bytes[index] = binary.charCodeAt(index); return bytes.buffer; }
  function bufferToBase64url(value: ArrayBuffer | null): string { if (!value) return ''; const bytes = new Uint8Array(value); let binary = ''; bytes.forEach(byte => { binary += String.fromCharCode(byte); }); return btoa(binary).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, ''); }
  function serializeCredential(credential: PublicKeyCredential): Dict<any> {
    const response: any = credential.response;
    return {
      id: credential.id,
      raw_id: bufferToBase64url(credential.rawId),
      response: {
        client_data_json: bufferToBase64url(response.clientDataJSON),
        authenticator_data: response.authenticatorData ? bufferToBase64url(response.authenticatorData) : undefined,
        signature: response.signature ? bufferToBase64url(response.signature) : undefined,
        user_handle: response.userHandle ? bufferToBase64url(response.userHandle) : undefined,
        attestation_object: response.attestationObject ? bufferToBase64url(response.attestationObject) : undefined,
        transports: typeof response.getTransports === 'function' ? response.getTransports() : undefined
      }
    };
  }

  function DashboardPage(props: PageProps) {
    const resource = useResource(async () => {
      const overview = asObject(await api('/api/v1/system/overview'));
      const [dockerResult, securityResult] = await Promise.allSettled([
        api('/api/v1/docker/status'),
        api('/api/v1/security/status')
      ]);
      const docker = dockerResult.status === 'fulfilled'
        ? asObject(dockerResult.value)
        : { _load_error: errorDetail(dockerResult.reason) };
      const security = securityResult.status === 'fulfilled'
        ? asObject(securityResult.value)
        : { _load_error: errorDetail(securityResult.reason) };
      return { overview, docker, security };
    }, []);
    const data = resource.data || { overview: {}, docker: {}, security: {} }; const overview = data.overview;
    const cpu = number(overview.cpu_percent ?? overview.cpu?.percent); const memoryUsed = number(overview.memory?.used ?? overview.memory?.Used); const memoryTotal = number(overview.memory?.total ?? overview.memory?.Total); const memoryPct = number(overview.memory?.percent, memoryTotal ? memoryUsed / memoryTotal * 100 : 0); const swapUsed = number(overview.swap?.used ?? overview.swap?.Used); const swapTotal = number(overview.swap?.total ?? overview.swap?.Total); const swapPct = number(overview.swap?.percent, swapTotal ? swapUsed / swapTotal * 100 : 0); const disk = listOf<any>(overview.disks)[0] || overview.disk || {}; const diskPct = number(disk.percent, number(disk.total ?? disk.Total) ? number(disk.used ?? disk.Used) / number(disk.total ?? disk.Total) * 100 : 0);
    return <div className="page-stack"><PageHeader {...props} busy={resource.loading} onRefresh={() => resource.reload()} />{resource.error ? <ErrorState error={resource.error} retry={() => resource.reload()} /> : resource.loading && !resource.data ? <LoadingState rows={6} /> : <><div className="status-line"><span className="live-dot" /><strong>实时状态</strong><span>{text(overview.hostname, '服务器')}</span><time>{formatDate(overview.collected_at)}</time></div><section className="metric-grid"><Metric title="运行时间" value={formatDuration(overview.uptime_seconds)} detail={text(overview.platform || overview.os, 'Linux')} icon="server" /><Metric title="CPU 使用率" value={`${cpu.toFixed(1)}%`} detail={`${text(overview.cpu_cores ?? overview.cpu?.cores, '—')} 核 · 负载 ${number(overview.load_1 ?? overview.load?.[0]).toFixed(2)}`} icon="process" tone={cpu > 90 ? 'danger' : cpu > 70 ? 'warning' : 'normal'} /><Metric title="内存使用率" value={`${memoryPct.toFixed(1)}%`} detail={`${formatBytes(memoryUsed)} / ${formatBytes(memoryTotal)}`} icon="storage" tone={memoryPct > 90 ? 'danger' : memoryPct > 75 ? 'warning' : 'normal'} /><Metric title="系统盘" value={`${diskPct.toFixed(1)}%`} detail={`${formatBytes(disk.used ?? disk.Used)} / ${formatBytes(disk.total ?? disk.Total)}`} icon="storage" tone={diskPct > 90 ? 'danger' : diskPct > 80 ? 'warning' : 'normal'} /></section><section className="dashboard-grid"><Card><SectionTitle title="资源使用" subtitle="服务器当前资源占用" actions={<Button tone="ghost" onClick={() => props.navigate('/system/processes')}>查看进程</Button>} /><ResourceRow label="CPU" value={`${cpu.toFixed(1)}%`} detail={`${text(overview.cpu_cores ?? overview.cpu?.cores, '—')} 核心`} percent={cpu} /><ResourceRow label="内存" value={`${memoryPct.toFixed(1)}%`} detail={`${formatBytes(memoryUsed)} / ${formatBytes(memoryTotal)}`} percent={memoryPct} /><ResourceRow label="系统盘" value={`${diskPct.toFixed(1)}%`} detail={text(disk.mount ?? disk.mountpoint, '/')} percent={diskPct} /><ResourceRow label="Swap" value={swapTotal ? `${swapPct.toFixed(1)}%` : '未配置'} detail={swapTotal ? `${formatBytes(swapUsed)} / ${formatBytes(swapTotal)}` : '可在主机设置中创建'} percent={swapPct} /></Card><Card><SectionTitle title="运行环境" subtitle="基础系统与安全状态" /><dl className="key-value-list"><KeyValue label="主机名" value={text(overview.hostname)} /><KeyValue label="系统" value={text(overview.os || overview.platform)} /><KeyValue label="内核" value={text(overview.kernel)} mono /><KeyValue label="架构" value={text(overview.architecture || overview.arch)} /><KeyValue label="Docker" value={data.docker._load_error
              ? <Badge tone="danger" title={text(data.docker._load_error)}>读取失败</Badge>
              : data.docker.available === true
                ? <Badge tone="success">可用 · {text(data.docker.version, '版本未知')}</Badge>
                : data.docker.available === false
                  ? <Badge tone="warning" title={text(data.docker.error)}>不可用</Badge>
                  : <Badge tone="neutral">状态未知</Badge>} /><KeyValue label="安全评分" value={data.security._load_error
              ? <Badge tone="danger" title={text(data.security._load_error)}>读取失败</Badge>
              : data.security.score === undefined || data.security.score === null
                ? <Badge tone="neutral">未评估</Badge>
                : text(data.security.score)} /></dl></Card></section><Card><SectionTitle title="快捷入口" subtitle="常用管理能力" /><div className="shortcut-grid">{[['文件管理', '浏览、编辑与安全上传', '/files', 'files'], ['软件管理', '更新系统和管理软件源', '/system/updates', 'package'], ['日志中心', '审计操作和系统日志', '/audit', 'audit'], ['安全中心', '防火墙与登录防护', '/security', 'security']].map(item => <button className="shortcut" key={item[2]} onClick={() => props.navigate(item[2])}><span><Icon name={item[3]} /></span><div><strong>{item[0]}</strong><small>{item[1]}</small></div><Icon name="chevron" size={17} /></button>)}</div></Card></>}</div>;
  }
  function Metric({ title, value, detail, icon, tone = 'normal' }: { title: string; value: string; detail: string; icon: string; tone?: 'normal' | 'warning' | 'danger' }) { return <Card className={`metric metric-${tone}`}><div className="metric-icon"><Icon name={icon} /></div><div><span>{title}</span><strong>{value}</strong><small>{detail}</small></div></Card>; }
  function ResourceRow({ label, value, detail, percent }: { label: string; value: string; detail: string; percent: number }) { return <div className="resource-row"><div><strong>{label}</strong><span>{value}</span></div><Progress value={percent} tone={percent > 90 ? 'danger' : percent > 75 ? 'warning' : 'primary'} /><small>{detail}</small></div>; }

  function SystemPage(props: PageProps) {
    const entries = ROUTES.filter(route => route.parent === '/system');
    return <div className="page-stack"><PageHeader {...props} /><Card><SectionTitle title="系统管理" subtitle="进入具体模块后才显示返回按钮；当前一级页面不显示返回" /><div className="module-grid">{entries.map(route => <button className="module-card" key={route.path} onClick={() => props.navigate(route.path)}><span className="module-icon"><Icon name={route.icon} /></span><div><strong>{route.title}</strong><p>{route.subtitle}</p></div><Icon name="chevron" size={18} /></button>)}</div></Card></div>;
  }

  function ServicesPage(props: PageProps) {
    const [search, setSearch] = useState(''); const [working, setWorking] = useState('');
    const resource = useResource(() => api(`/api/v1/system/services${query({ query: search })}`), [search]); const services = listOf<any>(resource.data, 'services', 'items');
    const act = async (service: any, action: string) => { const key = `${service.name}:${action}`; setWorking(key); try { await secureApi('/api/v1/system/services/action', { method: 'POST', body: jsonBody({ name: service.name, action }) }); props.notify('success', `${service.name} 已${action === 'restart' ? '重启' : action === 'start' ? '启动' : '停止'}`); await resource.reload(true); } catch (cause) { props.notify('error', '服务操作失败', errorDetail(cause)); } finally { setWorking(''); } };
    const logs = async (service: any) => { try { const out = await api<any>(`/api/v1/system/services/logs${query({ name: service.name, lines: 400 })}`); props.openModal({ title: `${service.name} · 日志`, size: 'large', content: <Terminal maxHeight={620}>{out.logs || out.output}</Terminal> }); } catch (cause) { props.notify('error', '读取日志失败', errorDetail(cause)); } };
    return <div className="page-stack"><PageHeader {...props} busy={resource.loading} onRefresh={() => resource.reload()} actions={<div className="search-box"><Icon name="search" size={17} /><input value={search} onChange={(event: any) => setSearch(event.target.value)} placeholder="搜索服务" /></div>} />{resource.error ? <ErrorState error={resource.error} retry={() => resource.reload()} /> : resource.loading && !resource.data ? <LoadingState /> : services.length ? <div className="card-list">{services.map(service => { const active = String(service.active_state || service.active || '').toLowerCase() === 'active' || service.running; return <Card className="service-card" key={service.name}><div className="list-main"><span className={`state-indicator ${active ? 'is-up' : 'is-down'}`} /><div><strong>{service.name}</strong><p>{text(service.description)}</p><div className="inline-meta"><Badge tone={active ? 'success' : 'neutral'}>{text(service.active_state || service.active || service.status, active ? 'active' : 'inactive')}</Badge><span>{text(service.sub_state || service.sub)}</span><span>{text(service.enabled)}</span></div></div></div><div className="list-actions"><Button icon="play" busy={working === `${service.name}:start`} disabled={active} onClick={() => act(service, 'start')}>启动</Button><Button icon="restart" busy={working === `${service.name}:restart`} onClick={() => act(service, 'restart')}>重启</Button><Button icon="stop" tone="danger" busy={working === `${service.name}:stop`} disabled={!active} onClick={() => act(service, 'stop')}>停止</Button><Button tone="ghost" icon="audit" onClick={() => logs(service)}>日志</Button></div></Card>; })}</div> : <EmptyState icon="services" title="没有匹配的服务" description="调整搜索条件后重试" />}</div>;
  }

  function ProcessesPage(props: PageProps) {
    const [search, setSearch] = useState(''); const [sort, setSort] = useState('cpu'); const resource = useResource(() => api('/api/v1/system/processes'), []); const source = listOf<any>(resource.data, 'processes', 'items');
    const processes = useMemo(() => { const value = (item: any) => sort === 'cpu' ? number(item.cpu_percent ?? item.cpu) : sort === 'memory' ? number(item.memory_bytes ?? item.memory_percent ?? item.memory) : number(item.pid); return source.filter(item => `${item.pid} ${item.user} ${item.command} ${item.name}`.toLowerCase().includes(search.toLowerCase())).sort((a, b) => value(b) - value(a)); }, [source, search, sort]);
    const signal = async (item: any, name: 'term' | 'kill') => { if (!await props.confirm(name === 'kill' ? '强制结束进程' : '结束进程', `PID ${item.pid} · ${text(item.command || item.name)}`, name === 'kill' ? '强制结束' : '发送 SIGTERM', name === 'kill')) return; try { await secureApi('/api/v1/system/processes/action', { method: 'POST', body: jsonBody({ pid: item.pid, signal: name }) }); props.notify('success', '信号已发送'); await resource.reload(true); } catch (cause) { props.notify('error', '进程操作失败', errorDetail(cause)); } };
    return <div className="page-stack"><PageHeader {...props} busy={resource.loading} onRefresh={() => resource.reload()} actions={<><div className="search-box"><Icon name="search" size={17} /><input value={search} onChange={(event: any) => setSearch(event.target.value)} placeholder="PID、用户或命令" /></div><SelectInput aria-label="进程排序方式" value={sort} onChange={(event: any) => setSort(event.target.value)}><option value="cpu">CPU 排序</option><option value="memory">内存排序</option><option value="pid">PID 排序</option></SelectInput></>} />{resource.error ? <ErrorState error={resource.error} retry={() => resource.reload()} /> : <Card className="table-card"><div className="responsive-table process-table"><div className="table-head"><span>PID</span><span>用户</span><span>进程</span><span>CPU</span><span>内存</span><span>操作</span></div>{processes.map(item => <div className="table-row" key={item.pid}><span data-label="PID" className="mono">{item.pid}</span><span data-label="用户">{text(item.user)}</span><span data-label="进程"><strong>{text(item.name || String(item.command || '').split(' ')[0])}</strong><small className="mono">{truncate(item.command, 120)}</small></span><span data-label="CPU">{number(item.cpu ?? item.cpu_percent).toFixed(1)}%</span><span data-label="内存">{item.memory_bytes ? formatBytes(item.memory_bytes) : `${number(item.memory ?? item.memory_percent).toFixed(1)}%`}</span><span data-label="操作" className="row-buttons"><Button tone="ghost" onClick={() => signal(item, 'term')}>结束</Button><Button tone="danger" onClick={() => signal(item, 'kill')}>强制</Button></span></div>)}{!processes.length && !resource.loading ? <EmptyState icon="process" title="没有匹配的进程" /> : null}</div></Card>}</div>;
  }

  function NetworkPage(props: PageProps) {
    const resource = useResource(() => api('/api/v1/system/network'), []); const interfaces = listOf<any>(resource.data, 'interfaces'); const object = asObject(resource.data);
    return <div className="page-stack"><PageHeader {...props} busy={resource.loading} onRefresh={() => resource.reload()} />{resource.error ? <ErrorState error={resource.error} retry={() => resource.reload()} /> : resource.loading && !resource.data ? <LoadingState /> : <><section className="interface-grid">{interfaces.map(item => <Card className="interface-card" key={item.name}><div className="card-topline"><strong>{text(item.name)}</strong><Badge tone={String(item.flags || '').toUpperCase().includes('UP') || item.up ? 'success' : 'neutral'}>{item.up ? 'UP' : text(item.flags)}</Badge></div><div className="address-list">{listOf<string>(item.addresses).map(address => <code key={address}>{address}</code>)}{!listOf(item.addresses).length ? <span>无地址</span> : null}</div><div className="split-meta"><span>MTU {text(item.mtu)}</span><span>↓ {formatBytes(item.received_bytes ?? item.rx_bytes)}</span><span>↑ {formatBytes(item.sent_bytes ?? item.tx_bytes)}</span></div></Card>)}</section><Card><SectionTitle title="监听端口" subtitle="来自系统当前监听状态" /><Terminal>{text(object.listening || object.listeners, '未读取到监听端口')}</Terminal></Card>{object.connections ? <Card><SectionTitle title="活动连接" /><Terminal>{typeof object.connections === 'string' ? object.connections : JSON.stringify(object.connections, null, 2)}</Terminal></Card> : null}</>}</div>;
  }

  function StoragePage(props: PageProps) {
    const [showVirtual, setShowVirtual] = useState(false); const resource = useResource(() => api('/api/v1/system/storage'), []); const all = listOf<any>(resource.data, 'mounts', 'filesystems'); const mounts = showVirtual ? all : all.filter(item => !item.virtual);
    return <div className="page-stack"><PageHeader {...props} busy={resource.loading} onRefresh={() => resource.reload()} actions={<Toggle checked={showVirtual} onChange={setShowVirtual} label="显示虚拟挂载" />} />{resource.error ? <ErrorState error={resource.error} retry={() => resource.reload()} /> : <section className="storage-grid">{mounts.map(item => { const total = number(item.total); const used = number(item.used); const percent = number(item.percent, total ? used / total * 100 : 0); return <Card className="storage-card" key={`${item.mountpoint || item.mount}-${item.device}`}><div className="card-topline"><strong>{text(item.mountpoint || item.mount)}</strong>{item.virtual ? <Badge>虚拟</Badge> : <Badge tone={percent > 90 ? 'danger' : percent > 80 ? 'warning' : 'success'}>{percent.toFixed(1)}%</Badge>}</div><code>{text(item.device)}</code><Progress value={percent} tone={percent > 90 ? 'danger' : percent > 80 ? 'warning' : 'primary'} /><div className="split-meta"><span>{text(item.filesystem)}</span><span>{formatBytes(used)} / {formatBytes(total)}</span></div></Card>; })}{!mounts.length && !resource.loading ? <EmptyState icon="storage" title="暂无挂载点" /> : null}</section>}</div>;
  }

  function TaskCreateForm({ props, reload }: { props: PageProps; reload: () => Promise<void> }) {
    const [type, setType] = useState('service-restart'); const [frequency, setFrequency] = useState('daily'); const [working, setWorking] = useState(false);
    const needsTarget = type === 'service-restart' || type === 'docker-restart';
    return <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); setWorking(true); try { await secureApi('/api/v1/system/tasks/create', { method: 'POST', body: jsonBody({ name: values.get('name'), type, target: needsTarget ? values.get('target') : '', frequency, hour: number(values.get('hour')), minute: number(values.get('minute')), weekday: number(values.get('weekday')) }) }); props.closeModal(); props.notify('success', '计划任务已创建并启用'); await reload(); } catch (cause) { props.notify('error', '创建失败', errorDetail(cause)); } finally { setWorking(false); } }}>
      <Field label="名称"><TextInput name="name" required maxLength="80" placeholder="例如：每日配置备份" /></Field>
      <Field label="任务类型"><SelectInput value={type} onChange={(event:any) => setType(event.target.value)}><option value="service-restart">重启 systemd 服务</option><option value="docker-restart">重启 Docker 容器</option><option value="docker-cleanup-safe">安全清理 Docker</option><option value="panel-backup">创建面板备份</option></SelectInput></Field>
      {needsTarget ? <Field label={type === 'service-restart' ? '服务名称' : '容器名称'} hint={type === 'service-restart' ? '必须是完整的 .service 单元名' : '填写现有 Docker 容器名称'}><TextInput name="target" required placeholder={type === 'service-restart' ? 'nginx.service' : 'nginx'} /></Field> : <div className="notice">{type === 'docker-cleanup-safe' ? '只执行 docker system prune -f，不会删除正在使用的资源。' : '由 LukePanel 创建配置备份并按定时备份策略保留。'}</div>}
      <Field label="执行频率"><SelectInput value={frequency} onChange={(event:any) => setFrequency(event.target.value)}><option value="hourly">每小时</option><option value="daily">每天</option><option value="weekly">每周</option></SelectInput></Field>
      <div className="form-grid">{frequency !== 'hourly' ? <Field label="小时（0-23）"><TextInput name="hour" type="number" min="0" max="23" defaultValue="3" required /></Field> : <input type="hidden" name="hour" value="0" />}{frequency === 'weekly' ? <Field label="星期"><SelectInput name="weekday" defaultValue="1"><option value="0">周日</option><option value="1">周一</option><option value="2">周二</option><option value="3">周三</option><option value="4">周四</option><option value="5">周五</option><option value="6">周六</option></SelectInput></Field> : <input type="hidden" name="weekday" value="0" />}<Field label="分钟（0-59）"><TextInput name="minute" type="number" min="0" max="59" defaultValue="0" required /></Field></div>
      <div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>创建并启用</Button></div>
    </form>;
  }

  function TasksPage(props: PageProps) {
    const [data, setData] = useState<any>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const [tasks, timers] = await Promise.all([api('/api/v1/system/tasks'), api('/api/v1/system/timers')]); setData({ tasks, timers }); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []); useEffect(() => { void reload(); }, [reload]);
    const taskList = listOf<any>(data?.tasks, 'tasks'); const timerOutput = text(asObject(data?.timers).timers, '未读取到 systemd timer 输出');
    const createDialog = () => props.openModal({ title: '创建计划任务', content: <TaskCreateForm props={props} reload={reload} /> });
    const act = async (item: any, action: string) => { if (action === 'delete' && !await props.confirm('删除计划任务', '对应的 systemd service 和 timer 会一起移除。', '删除', true)) return; try { await secureApi('/api/v1/system/tasks/action', { method: 'POST', body: jsonBody({ id: item.id, action }) }); props.notify('success', action === 'run' ? '任务已开始' : action === 'delete' ? '任务已删除' : '任务状态已更新'); await reload(); } catch (cause) { props.notify('error', '任务操作失败', errorDetail(cause)); } };
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} actions={<Button tone="primary" icon="plus" onClick={createDialog}>新建任务</Button>} />{error ? <ErrorState error={error} retry={reload} /> : <><Card><SectionTitle title="计划任务" subtitle="由 LukePanel 管理并生成独立的 systemd service 与 timer" />{taskList.length ? <div className="card-list compact-list">{taskList.map(item => <div className="task-row" key={item.id}><div><strong>{text(item.name || item.id)}</strong><code>{taskScheduleLabel(item)}</code><small>{taskTypeLabel(item.type)} · {taskTargetLabel(item)}</small>{item.next_run ? <small>下次：{text(item.next_run)}</small> : null}</div><div className="inline-meta"><Badge tone={item.enabled ? 'success' : 'neutral'}>{boolText(item.enabled)}</Badge><span>{item.last_run ? `上次：${text(item.last_run)}` : '尚未运行'}</span></div><div className="row-buttons"><Button icon="play" onClick={() => act(item, 'run')}>运行</Button><Button tone="ghost" onClick={() => act(item, item.enabled ? 'disable' : 'enable')}>{item.enabled ? '停用' : '启用'}</Button><Button tone="danger" icon="trash" onClick={() => act(item, 'delete')}>删除</Button></div></div>)}</div> : !loading ? <EmptyState icon="tasks" title="暂无计划任务" description="LukePanel 仅允许预定义的安全任务类型，不支持任意命令执行" action={<Button tone="primary" icon="plus" onClick={createDialog}>创建任务</Button>} /> : <LoadingState />}</Card><Card><SectionTitle title="系统 Timers" subtitle="后端返回 systemctl list-timers 的原始输出，不伪造结构化状态" /><Terminal>{timerOutput}</Terminal></Card></>}</div>;
  }

  function UpdatesPage(props: PageProps) {
    const [preflight, setPreflight] = useState<any>({}); const [sources, setSources] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [search, setSearch] = useState(''); const [results, setResults] = useState<any[]>([]); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const [check, sourceData] = await Promise.all([api('/api/v1/system/apt/preflight'), api('/api/v1/system/apt/sources')]); setPreflight(asObject(check)); setSources(listOf<any>(sourceData, 'sources')); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []); useEffect(() => { void reload(); }, [reload]);
    const aptBlocked = preflight.available === false || !!preflight.locked;
    const find = async () => { if (search.trim().length < 2) return; setWorking(true); try { const out = await api(`/api/v1/system/apt/search${query({ q: search.trim() })}`); setResults(listOf<any>(out, 'packages', 'results')); } catch (cause) { props.notify('error', '搜索失败', errorDetail(cause)); } finally { setWorking(false); } };
    const action = async (name: string, packages: string[] = []) => { const label: Dict<string> = { download: '只下载更新', upgrade: '执行系统升级', install: '安装软件包', remove: '卸载软件包' }; if (aptBlocked) { props.notify('warning', preflight.available === false ? '当前系统没有 apt-get' : 'APT 正被其他进程占用', text(preflight.lock_detail)); return; } if (!await props.confirm(label[name] || name, packages.length ? packages.join(', ') : '该操作可能持续数分钟，期间请勿关闭服务器。', '确认', name === 'remove')) return; setWorking(true); try { const endpoint = name === 'download' ? '/api/v1/system/apt/download' : name === 'upgrade' ? '/api/v1/system/apt/upgrade' : '/api/v1/system/apt/package'; const result = asObject(await secureApi(endpoint, { method: 'POST', body: jsonBody(name === 'install' || name === 'remove' ? { action: name, packages } : {}) })); props.notify('success', `${label[name]}已完成`); if (result.output) props.openModal({ title: `${label[name]}结果`, size: 'large', content: <Terminal maxHeight={620}>{String(result.output)}</Terminal> }); await reload(); if (search) await find(); } catch (cause) { props.notify('error', '软件操作失败', errorDetail(cause)); } finally { setWorking(false); } };
    const sourceAction = async (name: string, source?: any) => { if (name === 'delete' && !await props.confirm('删除软件源', text(source?.path), '删除', true)) return; try { await secureApi('/api/v1/system/apt/sources', { method: 'POST', body: jsonBody({ action: name, path: source?.path }) }); props.notify('success', '软件源已更新'); await reload(); } catch (cause) { props.notify('error', '软件源操作失败', errorDetail(cause)); } };
    const addSource = () => props.openModal({ title: '添加软件源', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); try { await secureApi('/api/v1/system/apt/sources', { method: 'POST', body: jsonBody({ action: 'add', content: values.get('content'), name: values.get('name') }) }); props.closeModal(); props.notify('success', '软件源已添加'); await reload(); } catch (cause) { props.notify('error', '添加失败', errorDetail(cause)); } }}><Field label="文件名" hint="仅填写文件名，例如 custom.sources"><TextInput name="name" placeholder="custom.sources" required /></Field><Field label="软件源内容"><TextArea name="content" rows={6} placeholder="deb https://... stable main" required /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">添加</Button></div></form> });
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} />{error ? <ErrorState error={error} retry={reload} /> : <>{preflight.available === false ? <div className="notice warning">当前系统未检测到 apt-get，软件更新与安装功能不可用。</div> : null}{preflight.locked ? <div className="notice warning">APT 正被其他进程占用：{text(preflight.lock_detail, '请稍后刷新')}</div> : null}{preflight.reboot_required ? <div className="notice warning">系统检测到需要重启才能完成已安装更新。</div> : null}<section className="metric-grid small"><Metric title="可升级" value={text(preflight.upgrade_count, '0')} detail="个软件包" icon="package" tone={number(preflight.upgrade_count) ? 'warning' : 'normal'} /><Metric title="新安装" value={text(preflight.install_count, '0')} detail="升级过程中新增" icon="plus" tone={number(preflight.install_count) ? 'warning' : 'normal'} /><Metric title="将删除" value={text(preflight.remove_count, '0')} detail="升级过程中移除" icon="trash" tone={number(preflight.remove_count) ? 'danger' : 'normal'} /><Metric title="预计下载" value={formatBytes(preflight.download_bytes)} detail={`磁盘变化 ${formatBytes(Math.abs(number(preflight.disk_delta_bytes)))}`} icon="download" /></section><Card><SectionTitle title="系统更新" subtitle="数据来自 apt-get 模拟执行结果；操作返回后才表示完成" actions={<><Button busy={working} disabled={aptBlocked} icon="download" onClick={() => action('download')}>只下载</Button><Button busy={working} disabled={aptBlocked} tone="primary" icon="package" onClick={() => action('upgrade')}>执行升级</Button></>} />{Array.isArray(preflight.packages) && preflight.packages.length ? <Terminal>{preflight.packages.join('\n')}</Terminal> : <p className="muted">当前没有待更新软件包，或尚未取得预检结果。</p>}{preflight.output ? <JsonDetails data={{ output: preflight.output }} title="APT 预检原始输出" /> : null}</Card><Card><SectionTitle title="搜索软件包" subtitle="至少输入两个字符" /><form className="search-form" onSubmit={(event: any) => { event.preventDefault(); void find(); }}><div className="search-box grow"><Icon name="search" size={17} /><input value={search} onChange={(event: any) => setSearch(event.target.value)} placeholder="例如 nginx、curl" /></div><Button type="submit" tone="primary" busy={working} disabled={preflight.available === false}>搜索</Button></form><div className="package-list">{results.map(item => <div className="package-row" key={item.name}><div><strong>{item.name}</strong><p>{text(item.description)}</p><small>{text(item.version || item.candidate)}</small></div><div>{item.installed ? <Button tone="danger" disabled={aptBlocked} onClick={() => action('remove', [item.name])}>卸载</Button> : <Button tone="primary" disabled={aptBlocked} onClick={() => action('install', [item.name])}>安装</Button>}</div></div>)}</div></Card><Card><SectionTitle title="软件源" subtitle="启停或维护 APT 源文件" actions={<Button tone="primary" icon="plus" disabled={preflight.available === false} onClick={addSource}>添加软件源</Button>} />{sources.length ? <div className="card-list compact-list">{sources.map(source => <div className="source-row" key={source.path}><div><strong>{text(source.name || source.path)}</strong><code>{text(source.path)}</code><small>{truncate(source.content, 180)}</small></div><div className="row-buttons"><Badge tone={source.enabled ? 'success' : 'neutral'}>{boolText(source.enabled)}</Badge><Button tone="ghost" onClick={() => sourceAction(source.enabled ? 'disable' : 'enable', source)}>{source.enabled ? '停用' : '启用'}</Button><Button tone="danger" icon="trash" onClick={() => sourceAction('delete', source)}>删除</Button></div></div>)}</div> : <EmptyState icon="package" title="没有检测到可管理的软件源" />}</Card></>}</div>;
  }

  function HostPage(props: PageProps) {
    const [data, setData] = useState<any>({}); const [ntp, setNtp] = useState<any>({}); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const [host, ntpData] = await Promise.all([api('/api/v1/system/host'), api('/api/v1/system/host/ntp')]); setData(asObject(host)); setNtp(asObject(ntpData)); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []); useEffect(() => { void reload(); }, [reload]);
    const mutate = async (endpoint: string, body: unknown, success = '主机设置已更新') => { setWorking(true); try { await secureApi(`/api/v1/system/host/${endpoint}`, { method: 'POST', body: jsonBody(body) }); props.notify('success', success); await reload(); } catch (cause) { props.notify('error', '修改失败', errorDetail(cause)); } finally { setWorking(false); } };
    const deleteSwap = async () => { if (!data.swap?.managed) { props.notify('warning', '当前 Swap 不是 LukePanel 管理的 /swapfile，不能自动删除'); return; } if (!await props.confirm('删除 Swap', '将关闭并永久删除 LukePanel 管理的 /swapfile。', '删除 Swap', true)) return; setWorking(true); try { await secureApi('/api/v1/system/host/swap', { method: 'DELETE' }); props.notify('success', 'Swap 已删除'); await reload(); } catch (cause) { props.notify('error', '删除 Swap 失败', errorDetail(cause)); } finally { setWorking(false); } };
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} />{error ? <ErrorState error={error} retry={reload} /> : loading && !Object.keys(data).length ? <LoadingState /> : <div className="settings-grid"><Card><SectionTitle title="基础信息" subtitle="主机名与系统时区" /><form key={`${data.hostname}|${data.timezone}`} className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); setWorking(true); try { const hostname = values.get('hostname'); const timezone = values.get('timezone'); if (hostname !== data.hostname) await secureApi('/api/v1/system/host/hostname', { method: 'POST', body: jsonBody({ hostname }) }); if (timezone !== data.timezone) await secureApi('/api/v1/system/host/timezone', { method: 'POST', body: jsonBody({ timezone }) }); props.notify('success', '基础设置已更新'); await reload(); } catch (cause) { props.notify('error', '修改失败', errorDetail(cause)); } finally { setWorking(false); } }}><Field label="主机名"><TextInput name="hostname" defaultValue={text(data.hostname, '')} required /></Field><Field label="时区"><TextInput name="timezone" defaultValue={text(data.timezone, 'UTC')} required /></Field><Button type="submit" tone="primary" busy={working}>保存基础设置</Button></form></Card><Card><SectionTitle title="DNS 服务器" subtitle={data.systemd_resolved ? '由 systemd-resolved 管理，每行一个地址' : '当前未运行 systemd-resolved，LukePanel 不会接管 resolv.conf'} />{!data.systemd_resolved ? <div className="notice warning">当前系统没有运行 systemd-resolved，此表单不可用。</div> : null}<form key={JSON.stringify(data.dns || [])} className="form-stack" onSubmit={(event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); void mutate('dns', { servers: lines(String(values.get('servers') || '')) }, 'DNS 已更新并完成解析验证'); }}><Field label="DNS 地址"><TextArea name="servers" rows={5} defaultValue={Array.isArray(data.dns) ? data.dns.join('\n') : text(data.dns, '')} placeholder={'1.1.1.1\n8.8.8.8'} disabled={!data.systemd_resolved} /></Field><Button type="submit" tone="primary" busy={working} disabled={!data.systemd_resolved}>保存 DNS</Button></form></Card><Card><SectionTitle title="NTP 时间同步" subtitle="仅控制系统 NTP 开关；刷新状态不会修改服务器" /><dl className="key-value-list"><KeyValue label="服务" value={text(ntp.service)} /><KeyValue label="状态" value={<Badge tone={ntp.synchronized ? 'success' : ntp.enabled ? 'warning' : 'neutral'}>{ntp.synchronized ? '已同步' : ntp.enabled ? '等待同步' : '已关闭'}</Badge>} /><KeyValue label="服务器" value={text(ntp.server_name || ntp.server_address)} /><KeyValue label="服务器地址" value={text(ntp.server_address)} mono /><KeyValue label="上次同步" value={text(ntp.last_sync)} /><KeyValue label="轮询间隔" value={text(ntp.poll_interval)} /><KeyValue label="时区" value={text(ntp.timezone || data.timezone)} /></dl><div className="row-buttons"><Button onClick={reload} busy={loading}>刷新状态</Button><Button tone="ghost" busy={working} onClick={() => mutate('ntp', { enabled: !ntp.enabled }, ntp.enabled ? 'NTP 已关闭' : 'NTP 已开启')}>{ntp.enabled ? '关闭 NTP' : '开启 NTP'}</Button></div></Card><Card><SectionTitle title="Swap" subtitle="只会自动删除 LukePanel 创建的 /swapfile" /><dl className="key-value-list"><KeyValue label="状态" value={<Badge tone={data.swap?.enabled ? 'success' : 'neutral'}>{data.swap?.enabled ? '已启用' : '未启用'}</Badge>} /><KeyValue label="总容量" value={formatBytes(data.swap?.total)} /><KeyValue label="已使用" value={formatBytes(data.swap?.used)} /><KeyValue label="路径" value={text(data.swap?.path)} mono /><KeyValue label="LukePanel 管理" value={boolText(data.swap?.managed, '是', '否')} /></dl><form className="inline-form" onSubmit={(event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); void mutate('swap', { size_mb: number(values.get('size_mb'), 2048) }, 'Swap 已创建'); }}><TextInput name="size_mb" type="number" min="256" max="32768" step="256" defaultValue="2048" aria-label="Swap 大小 MB" disabled={!!data.swap?.enabled} /><Button type="submit" tone="primary" busy={working} disabled={!!data.swap?.enabled}>创建 Swap</Button><Button tone="danger" busy={working} disabled={!data.swap?.managed} onClick={() => void deleteSwap()}>删除</Button></form>{data.swap?.enabled && !data.swap?.managed ? <div className="notice">检测到系统已有 Swap，但不是 LukePanel 管理的 /swapfile，因此不会提供删除或调整操作。</div> : null}</Card><Card className="span-2"><SectionTitle title="内核优化方案" subtitle="仅应用后端明确支持并可回滚的预设，不允许任意写入 sysctl" /><dl className="key-value-list"><KeyValue label="当前方案" value={text(data.sysctl?.label, '系统默认')} /><KeyValue label="由 LukePanel 管理" value={boolText(data.sysctl?.managed, '是', '否')} /><KeyValue label="BBR" value={boolText(data.sysctl?.bbr, '已启用', '未启用')} /><KeyValue label="拥塞控制" value={text(data.sysctl?.congestion_control)} mono /><KeyValue label="默认队列" value={text(data.sysctl?.default_qdisc)} mono /><KeyValue label="Swappiness" value={text(data.sysctl?.swappiness)} /></dl><form className="inline-form" onSubmit={(event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); void mutate('sysctl', { preset: values.get('preset') }, '内核优化方案已应用'); }}><SelectInput key={text(data.sysctl?.preset, 'reset')} name="preset" defaultValue={text(data.sysctl?.preset, 'reset')}><option value="reset">系统默认（移除 LukePanel 配置）</option><option value="balanced">均衡</option><option value="network">网络吞吐 / BBR</option><option value="low-memory">小内存 VPS</option></SelectInput><Button type="submit" tone="primary" busy={working}>应用方案</Button></form></Card></div>}</div>;
  }

  function SnapshotsPage(props: PageProps) {
    const [data, setData] = useState<any>({}); const [scheduled, setScheduled] = useState<any>({}); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const [snapshots, scheduledBackups] = await Promise.all([api('/api/v1/system/snapshots'), api('/api/v1/backup/scheduled')]); setData(asObject(snapshots)); setScheduled(asObject(scheduledBackups)); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []); useEffect(() => { void reload(); }, [reload]); const snapshots = listOf<any>(data, 'snapshots', 'items'); const scheduledBackups = listOf<any>(scheduled, 'backups');
    const action = async (item: any, operation: 'restore' | 'delete') => { if (!await props.confirm(operation === 'restore' ? '恢复配置快照' : '删除配置快照', operation === 'restore' ? '恢复会覆盖快照涉及的配置文件；若恢复失败，后端会尝试回滚。' : '确认永久删除这个快照？', operation === 'restore' ? '恢复' : '删除', true)) return; setWorking(true); try { await secureApi('/api/v1/system/snapshots', { method: 'POST', body: jsonBody({ id: item.id, action: operation }) }); props.notify('success', operation === 'restore' ? '快照已恢复' : '快照已删除'); await reload(); } catch (cause) { props.notify('error', '操作失败', errorDetail(cause)); } finally { setWorking(false); } };
    const importFile = async (file: File) => { if (!await props.confirm('导入并恢复备份', '该操作会覆盖当前面板配置，请确认备份来源可信。', '导入恢复', true)) return; setWorking(true); try { const form = new FormData(); form.append('file', file); await secureApi('/api/v1/backup/import', { method: 'POST', body: form }); props.notify('success', '备份已导入，请重新登录检查配置'); } catch (cause) { props.notify('error', '导入失败', errorDetail(cause)); } finally { setWorking(false); } };
    const createScheduledBackup = async () => { setWorking(true); try { await secureApi('/api/v1/backup/scheduled', { method: 'POST', body: jsonBody({ action: 'create' }) }); props.notify('success', '面板备份已创建'); await reload(); } catch (cause) { props.notify('error', '备份失败', errorDetail(cause)); } finally { setWorking(false); } };
    const deleteScheduledBackup = async (item: any) => { if (!await props.confirm('删除面板备份', text(item.name), '删除', true)) return; setWorking(true); try { await secureApi('/api/v1/backup/scheduled', { method: 'POST', body: jsonBody({ action: 'delete', name: item.name }) }); props.notify('success', '面板备份已删除'); await reload(); } catch (cause) { props.notify('error', '删除失败', errorDetail(cause)); } finally { setWorking(false); } };
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} />{error ? <ErrorState error={error} retry={reload} /> : <><Card><SectionTitle title="配置快照" subtitle="由 LukePanel 在修改 SSH、UFW、APT、DNS、Swap、sysctl 和 Compose 前自动创建" />{snapshots.length ? <div className="card-list compact-list">{snapshots.map(item => <div className="snapshot-row" key={item.id}><div><strong>{text(item.name || item.id)}</strong><p>{text(item.note, `${text(item.kind, '配置')} 自动快照`)}</p><small>{formatDate(item.created_at)} · {formatBytes(item.size)} · {listOf(item.items).length} 项配置</small></div><div className="row-buttons"><Button busy={working} onClick={() => action(item, 'restore')}>恢复</Button><Button busy={working} tone="danger" icon="trash" onClick={() => action(item, 'delete')}>删除</Button></div></div>)}</div> : !loading ? <EmptyState icon="backup" title="暂无配置快照" description="执行受支持的高风险配置修改前会自动生成，这里不提供虚假的手动创建按钮。" /> : <LoadingState />}</Card><div className="settings-grid"><Card><SectionTitle title="完整备份" subtitle="导出或导入面板配置" /><div className="stacked-actions"><a className="button button-default" href="/api/v1/backup/export" download><Icon name="download" size={17} /><span>导出备份</span></a><label className="button button-default file-button"><Icon name="upload" size={17} /><span>导入备份</span><input type="file" accept=".tar.gz,.tgz" onChange={(event: any) => { const file = event.target.files?.[0]; if (file) void importFile(file); event.currentTarget.value = ''; }} /></label></div></Card><Card><SectionTitle title="定时备份文件" subtitle={`后端固定保留最近 ${number(scheduled.retention, 7)} 份；执行计划在“计划任务”中管理`} actions={<Button tone="primary" busy={working} onClick={() => void createScheduledBackup()}>立即创建</Button>} />{scheduledBackups.length ? <div className="compact-list">{scheduledBackups.map(item => <div className="compact-row" key={item.name}><div><strong>{text(item.name)}</strong><small>{formatDate(item.modified_at)} · {formatBytes(item.size)}</small></div><div className="row-buttons"><a className="button button-default" href={`/api/v1/backup/scheduled?download=${encodeURIComponent(item.name)}`} download><Icon name="download" size={17} /><span>下载</span></a><Button tone="danger" icon="trash" busy={working} onClick={() => void deleteScheduledBackup(item)}>删除</Button></div></div>)}</div> : <EmptyState icon="backup" title="暂无面板备份" description="可立即创建，或在计划任务中添加“创建面板备份”任务。" />}<div className="form-actions"><Button tone="ghost" onClick={() => props.navigate('/system/tasks')}>前往计划任务</Button></div></Card></div></>}</div>;
  }

  function ComposeEditorForm({ project, files, close, notify, reload }: { project: string; files: any[]; close: () => void; notify: PageProps['notify']; reload: () => Promise<void> }) {
    const initialFiles = useMemo(() => Object.fromEntries(files.map(file => [String(file.path), String(file.content || '')])), [files]);
    const paths = useMemo(() => Object.keys(initialFiles), [initialFiles]);
    const [selectedPath, setSelectedPath] = useState(paths[0] || '');
    const [contents, setContents] = useState<Dict<string>>(initialFiles);
    const [deploy, setDeploy] = useState(true);
    const [saving, setSaving] = useState(false);
    const submit = async (event: any) => {
      event.preventDefault();
      if (!selectedPath || paths.length === 0) { notify('error', '没有可编辑的 Compose 配置文件'); return; }
      setSaving(true);
      try {
        const result = await secureApi<any>('/api/v1/docker/compose/config', { method: 'PUT', body: jsonBody({ project, files: contents, deploy }) });
        notify('success', 'Compose 配置已校验并保存', result.output || undefined);
        await reload();
        close();
      } catch (cause) { notify('error', '保存 Compose 配置失败', errorDetail(cause)); }
      finally { setSaving(false); }
    };
    return <form className="form-stack" onSubmit={submit}>
      <Field label="配置文件"><SelectInput value={selectedPath} onChange={(event: any) => setSelectedPath(event.target.value)}>{paths.map(path => <option value={path} key={path}>{path}</option>)}</SelectInput></Field>
      <Field label="YAML 内容"><TextArea className="code-input" rows={22} value={contents[selectedPath] || ''} onChange={(event: any) => setContents(current => ({ ...current, [selectedPath]: event.target.value }))} required /></Field>
      <label className="check-field"><input type="checkbox" checked={deploy} onChange={(event: any) => setDeploy(event.target.checked)} />保存后执行部署</label>
      <p className="muted">保存时会提交并校验该项目的全部 Compose 配置文件；校验失败会自动恢复快照。</p>
      <div className="form-actions"><Button onClick={close}>取消</Button><Button type="submit" tone="primary" busy={saving}>校验并保存</Button></div>
    </form>;
  }

  function DockerImageForm({ close, mutate, working, notify }: { close: () => void; mutate: (endpoint: string, body: unknown, success: string) => Promise<any | null>; working: boolean; notify: (kind: ToastKind, title: string, detail?: string) => void }) {
    const [mode, setMode] = useState<'pull' | 'build'>('pull');
    return <form className="form-stack" onSubmit={async (event: any) => {
      event.preventDefault();
      const values = new FormData(event.currentTarget);
      try {
        if (mode === 'pull') {
          const reference = String(values.get('reference') || '').trim();
          if (!reference) throw new Error('请输入镜像引用');
          if (!await mutate('/api/v1/docker/images/pull', { reference }, '镜像拉取完成')) return;
        } else {
          const contextDir = String(values.get('context_dir') || '').trim();
          const tag = String(values.get('tag') || '').trim();
          if (!contextDir || !tag) throw new Error('构建上下文和镜像标签不能为空');
          if (!await mutate('/api/v1/docker/images/build', {
            context_dir: contextDir,
            dockerfile: String(values.get('dockerfile') || 'Dockerfile').trim() || 'Dockerfile',
            tag,
            no_cache: values.get('no_cache') === 'on',
            pull: values.get('pull') === 'on'
          }, '镜像构建完成')) return;
        }
        close();
      } catch (cause) {
        notify('error', '镜像操作失败', errorDetail(cause));
      }
    }}>
      <Field label="操作"><SelectInput name="mode" value={mode} onChange={(event: any) => setMode(event.target.value)}><option value="pull">拉取镜像</option><option value="build">从服务器目录构建</option></SelectInput></Field>
      {mode === 'pull' ? <Field label="镜像引用"><TextInput name="reference" placeholder="nginx:latest" autoCapitalize="none" required autoFocus /></Field> : <>
        <Field label="构建上下文" hint="服务器上的绝对目录"><TextInput name="context_dir" placeholder="/opt/project" required autoFocus /></Field>
        <div className="form-grid"><Field label="Dockerfile"><TextInput name="dockerfile" defaultValue="Dockerfile" required /></Field><Field label="镜像标签"><TextInput name="tag" placeholder="myapp:latest" required /></Field></div>
        <div className="inline-checks"><label className="check-field"><input type="checkbox" name="pull" defaultChecked />拉取基础镜像</label><label className="check-field"><input type="checkbox" name="no_cache" />不使用缓存</label></div>
      </>}
      <div className="form-actions"><Button onClick={close}>取消</Button><Button type="submit" tone="primary" busy={working}>{mode === 'pull' ? '拉取镜像' : '开始构建'}</Button></div>
    </form>;
  }

  function DockerPage(props: PageProps) {
    type Tab = 'containers' | 'compose' | 'images' | 'networks' | 'volumes';
    const [tab, setTab] = useState<Tab>('containers'); const [state, setState] = useState<any>({ status: {}, containers: [], images: [], networks: [], volumes: [], projects: [], stats: {}, statsError: '' }); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const status = asObject(await api('/api/v1/docker/status')); if (status.available !== true) { setState({ status, containers: [], images: [], networks: [], volumes: [], projects: [], stats: {}, statsError: '' }); return; } const [containers, images, networks, volumes, compose] = await Promise.all([api('/api/v1/docker/containers'), api('/api/v1/docker/images'), api('/api/v1/docker/networks'), api('/api/v1/docker/volumes'), api('/api/v1/docker/compose')]); const containerList = listOf<any>(containers, 'containers'); const ids = containerList.filter(item => item.state === 'running').slice(0, 40).map(item => item.id); let stats: Dict<any> = {}; let statsError = ''; if (ids.length) { try { const result = await api(`/api/v1/docker/stats?${ids.map(id => `id=${encodeURIComponent(id)}`).join('&')}`); stats = Object.fromEntries(listOf<any>(result, 'stats').map(item => [String(item.id), item])); } catch (cause) { stats = {}; statsError = errorDetail(cause); } } setState({ status, containers: containerList, images: listOf<any>(images, 'images'), networks: listOf<any>(networks, 'networks'), volumes: listOf<any>(volumes, 'volumes'), projects: listOf<any>(compose, 'projects'), stats, statsError }); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []);
    useEffect(() => { void reload(); const timer = window.setInterval(() => { if (!document.hidden) void reload(); }, 15000); return () => window.clearInterval(timer); }, [reload]);
    const mutate = async (endpoint: string, body: unknown, success: string): Promise<any | null> => { setWorking(true); try { const result = await secureApi<any>(endpoint, { method: 'POST', body: jsonBody(body) }); props.notify('success', success, result?.output || result?.warning); await reload(); return result || { ok: true }; } catch (cause) { props.notify('error', 'Docker 操作失败', errorDetail(cause)); return null; } finally { setWorking(false); } };
    const containerName = (item: any) => String(item.names?.[0] || item.name || item.id || '').replace(/^\//, '').slice(0, 80);
    const containerAction = async (item: any, action: string) => { if (['stop', 'kill', 'remove'].includes(action)) { const ok = await props.confirm(action === 'remove' ? '删除容器' : action === 'kill' ? '强制终止容器' : '停止容器', containerName(item), '确认', action !== 'stop'); if (!ok) return; } await mutate('/api/v1/docker/action', { id: item.id, action }, action === 'remove' ? '容器已删除' : '容器状态已更新'); };
    const showLogs = async (item: any) => { try { const result = await api<any>(`/api/v1/docker/logs${query({ id: item.id, tail: 500 })}`); props.openModal({ title: `${containerName(item)} · 日志`, size: 'large', content: <Terminal maxHeight={650}>{result.logs}</Terminal> }); } catch (cause) { props.notify('error', '读取日志失败', errorDetail(cause)); } };
    const runDiagnostic = async (item: any) => { const commands = [['identity', '身份信息'], ['working-directory', '工作目录'], ['environment', '环境变量'], ['disk', '磁盘空间'], ['processes', '进程列表'], ['network', '网络统计'], ['os-release', '系统版本'], ['list-root', '根目录文件']]; props.openModal({ title: `${containerName(item)} · 安全诊断`, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); try { const out = await secureApi<any>('/api/v1/docker/exec', { method: 'POST', body: jsonBody({ id: item.id, command: values.get('command') }) }); props.openModal({ title: `${containerName(item)} · 诊断结果`, size: 'large', content: <Terminal maxHeight={650}>{out.output}</Terminal> }); } catch (cause) { props.notify('error', '诊断失败', errorDetail(cause)); } }}><Field label="固定诊断命令"><SelectInput name="command">{commands.map(([value, label]) => <option value={value} key={value}>{label}</option>)}</SelectInput></Field><p className="muted">仅允许后端预定义的安全诊断命令，不接受任意 Shell。</p><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" icon="terminal">执行</Button></div></form> }); };
    const editContainer = async (item: any) => { try { const edit = asObject(await api(`/api/v1/docker/inspect${query({ id: item.id })}`)); const ports = listOf<any>(edit.ports).map(port => [port.host_ip, port.host_port, `${port.container_port}/${port.protocol || 'tcp'}`].filter(Boolean).join('|')).join('\n'); const mounts = listOf<any>(edit.mounts).map(mount => `${mount.type || 'bind'}|${mount.source}|${mount.target}|${mount.read_only ? 'ro' : 'rw'}`).join('\n'); props.openModal({ title: `编辑容器 · ${containerName(item)}`, size: 'large', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const parsePorts = (value: string) => lines(value).map(line => { const fields = line.split('|').map(part => part.trim()); if (fields.length < 2 || fields.length > 3) throw new Error(`端口格式错误：${line}`); const target = fields[fields.length - 1].split('/'); return { host_ip: fields.length === 3 ? fields[0] : '', host_port: fields.length === 3 ? fields[1] : fields[0], container_port: target[0], protocol: target[1] || 'tcp' }; }); const parseMounts = (value: string) => lines(value).map(line => { const [type = 'bind', source = '', target = '', mode = 'rw'] = line.split('|').map(part => part.trim()); if (!source || !target.startsWith('/')) throw new Error(`挂载格式错误：${line}`); return { type, source, target, read_only: mode === 'ro' }; }); try { const body = { id: edit.id || item.id, name: values.get('name'), image: values.get('image'), env: lines(String(values.get('env') || '')), cmd: lines(String(values.get('cmd') || '')), entrypoint: lines(String(values.get('entrypoint') || '')), working_dir: values.get('working_dir'), user: values.get('user'), hostname: values.get('hostname'), restart_policy: values.get('restart_policy'), restart_maximum_retry_count: Number(values.get('restart_maximum_retry_count') || 0), network_mode: values.get('network_mode'), privileged: values.get('privileged') === 'on', start: !!edit.running, ports: parsePorts(String(values.get('ports') || '')), mounts: parseMounts(String(values.get('mounts') || '')) }; if (await mutate('/api/v1/docker/recreate', body, '容器已安全重建')) props.closeModal(); } catch (cause) { props.notify('error', '重建失败', errorDetail(cause)); } }}><div className="form-grid"><Field label="容器名称"><TextInput name="name" defaultValue={text(edit.name, containerName(item))} /></Field><Field label="镜像"><TextInput name="image" defaultValue={text(edit.image)} required /></Field><Field label="重启策略"><SelectInput name="restart_policy" defaultValue={text(edit.restart_policy, 'unless-stopped')}>{['no', 'always', 'on-failure', 'unless-stopped'].map(value => <option value={value} key={value}>{value}</option>)}</SelectInput></Field><Field label="最大重试次数" hint="仅 on-failure 策略生效"><TextInput name="restart_maximum_retry_count" type="number" min="0" defaultValue={String(number(edit.restart_maximum_retry_count))} /></Field><Field label="网络模式"><TextInput name="network_mode" defaultValue={text(edit.network_mode, 'default')} /></Field><Field label="主机名"><TextInput name="hostname" defaultValue={text(edit.hostname, '')} /></Field><Field label="运行用户"><TextInput name="user" defaultValue={text(edit.user, '')} /></Field><Field label="工作目录"><TextInput name="working_dir" defaultValue={text(edit.working_dir, '')} /></Field><label className="check-field"><input name="privileged" type="checkbox" defaultChecked={!!edit.privileged} />特权模式</label></div><Field label="环境变量" hint="每行一个 KEY=VALUE"><TextArea name="env" rows={6} defaultValue={listOf<string>(edit.env).join('\n')} /></Field><Field label="启动参数" hint="每行一个参数"><TextArea name="cmd" rows={4} defaultValue={listOf<string>(edit.cmd).join('\n')} /></Field><Field label="Entrypoint" hint="每行一个参数"><TextArea name="entrypoint" rows={3} defaultValue={listOf<string>(edit.entrypoint).join('\n')} /></Field><Field label="端口映射" hint="hostPort|containerPort/protocol；绑定 IP 时 hostIP|hostPort|containerPort/protocol"><TextArea name="ports" rows={5} defaultValue={ports} /></Field><Field label="挂载" hint="bind|源路径|目标路径|rw 或 volume|卷名|目标路径|ro"><TextArea name="mounts" rows={5} defaultValue={mounts} /></Field>{edit.compose_managed ? <div className="notice warning"><Icon name="warning" /><span>该容器由 Compose 项目 {text(edit.compose_project)} 管理，建议通过 Compose YAML 修改。</span></div> : null}<div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working} disabled={!!edit.compose_managed}>安全重建</Button></div></form> }); } catch (cause) { props.notify('error', '读取容器配置失败', errorDetail(cause)); } };
    const composeAction = async (project: any, action: string) => { if (action === 'down' && !await props.confirm('停止并移除 Compose 项目', project.name, '停止并移除', true)) return; await mutate('/api/v1/docker/compose/action', { project: project.name, action }, 'Compose 操作完成'); };
    const editCompose = async (project: any) => { try {
      const config = asObject(await api(`/api/v1/docker/compose/config${query({ project: project.name })}`));
      const files = listOf<any>(config, 'files');
      if (!files.length) throw new Error('项目没有可编辑的 Compose 配置文件');
      props.openModal({ title: `Compose YAML · ${project.name}`, size: 'large', content: <ComposeEditorForm project={project.name} files={files} close={props.closeModal} notify={props.notify} reload={reload} /> });
    } catch (cause) { props.notify('error', '读取 Compose 配置失败', errorDetail(cause)); } };
    const createCompose = () => props.openModal({ title: '新建 Compose 项目', size: 'large', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const service = { name: values.get('service'), image: values.get('image'), container_name: values.get('container_name'), restart: values.get('restart'), environment: lines(String(values.get('environment') || '')), ports: lines(String(values.get('ports') || '')), volumes: lines(String(values.get('volumes') || '')) }; try { if (await mutate('/api/v1/docker/compose/create', { project: values.get('project'), directory: values.get('directory'), services: [service], start: values.get('start') === 'on' }, 'Compose 项目已创建')) props.closeModal(); } catch { /* reported */ } }}><div className="form-grid"><Field label="项目名称"><TextInput name="project" required /></Field><Field label="保存目录"><TextInput name="directory" defaultValue="/opt/compose" required /></Field><Field label="服务名称"><TextInput name="service" defaultValue="app" required /></Field><Field label="镜像"><TextInput name="image" placeholder="nginx:latest" required /></Field><Field label="容器名称"><TextInput name="container_name" /></Field><Field label="重启策略"><SelectInput name="restart" defaultValue="unless-stopped">{['no', 'always', 'on-failure', 'unless-stopped'].map(value => <option key={value}>{value}</option>)}</SelectInput></Field></div><Field label="环境变量"><TextArea name="environment" rows={3} /></Field><Field label="端口"><TextArea name="ports" rows={3} placeholder="8080:80" /></Field><Field label="挂载"><TextArea name="volumes" rows={3} placeholder="./data:/data" /></Field><label className="check-field"><input type="checkbox" name="start" defaultChecked />创建后立即启动</label><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>创建</Button></div></form> });
    const imageDialog = () => props.openModal({ title: '镜像管理', content: <DockerImageForm close={props.closeModal} mutate={mutate} working={working} notify={props.notify} /> });
    const createResource = (kind: 'network' | 'volume') => props.openModal({ title: kind === 'network' ? '新建网络' : '新建存储卷', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const endpoint = kind === 'network' ? '/api/v1/docker/networks/create' : '/api/v1/docker/volumes/create'; const body = kind === 'network' ? { name: values.get('name'), driver: values.get('driver'), subnet: values.get('subnet'), gateway: values.get('gateway'), internal: values.get('internal') === 'on' } : { name: values.get('name'), driver: values.get('driver') }; try { if (await mutate(endpoint, body, kind === 'network' ? '网络已创建' : '存储卷已创建')) props.closeModal(); } catch { /* reported */ } }}><Field label="名称"><TextInput name="name" required /></Field><Field label="驱动"><TextInput name="driver" defaultValue={kind === 'network' ? 'bridge' : 'local'} /></Field>{kind === 'network' ? <><Field label="子网"><TextInput name="subnet" placeholder="172.30.0.0/16" /></Field><Field label="网关"><TextInput name="gateway" placeholder="172.30.0.1" /></Field><label className="check-field"><input type="checkbox" name="internal" />仅内部网络</label></> : null}<div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>创建</Button></div></form> });
    const volumeTools = async (selected?: any) => { try { const usage = listOf<any>(await api('/api/v1/docker/volumes/usage'), 'volumes'); props.openModal({ title: '存储卷用量与备份', size: 'large', content: <div className="form-stack"><div className="responsive-table"><div className="table-head"><span>卷名</span><span>用量</span><span>引用</span></div>{usage.map(item => <div className="table-row" key={item.name}><span data-label="卷名">{item.name}</span><span data-label="用量">{formatBytes(item.size)}</span><span data-label="引用">{number(item.ref_count)}</span></div>)}</div><form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const action = String(values.get('action')); if (action === 'restore' && !await props.confirm('恢复存储卷', '恢复会覆盖卷内同名文件，请确认备份来源可信。', '确认恢复', true)) return; try { if (await mutate('/api/v1/docker/volumes/archive', { action, name: values.get('name'), path: values.get('path') }, action === 'backup' ? '存储卷备份完成' : '存储卷恢复完成')) props.closeModal(); } catch { /* reported */ } }}><div className="form-grid"><Field label="存储卷"><SelectInput name="name" defaultValue={selected?.name || usage[0]?.name}>{usage.map(item => <option key={item.name}>{item.name}</option>)}</SelectInput></Field><Field label="操作"><SelectInput name="action" defaultValue="backup"><option value="backup">备份为 tar.gz</option><option value="restore">从 tar.gz 恢复</option></SelectInput></Field></div><Field label="服务器绝对路径"><TextInput name="path" defaultValue="/var/backups/lukepanel-volume.tar.gz" required /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">执行</Button></div></form></div> }); } catch (cause) { props.notify('error', '读取卷用量失败', errorDetail(cause)); } };
    const cleanup = async () => { try { const preview = asObject(await api('/api/v1/docker/cleanup/preview')); props.openModal({ title: 'Docker 清理', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); if (!await props.confirm('执行 Docker 清理', '清理不可恢复，请确认没有仍需使用的镜像、缓存或卷。', '开始清理', true)) return; try { if (await mutate('/api/v1/docker/cleanup', { mode: values.get('mode'), include_volumes: values.get('volumes') === 'on' }, 'Docker 清理完成')) props.closeModal(); } catch { /* reported */ } }}><JsonDetails data={preview} title="预计清理内容" /><Field label="清理模式"><SelectInput name="mode"><option value="safe">安全清理</option><option value="deep">深度清理</option></SelectInput></Field><label className="check-field"><input type="checkbox" name="volumes" />同时清理未使用存储卷</label><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="danger">执行清理</Button></div></form> }); } catch (cause) { props.notify('error', '读取清理预览失败', errorDetail(cause)); } };
    const status = state.status || {}; const available = status.available === true;
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} actions={available ? <Button tone="ghost" icon="trash" onClick={cleanup}>清理</Button> : null} />{error ? <ErrorState error={error} retry={reload} /> : !available ? <Card><EmptyState icon="docker" title="Docker 服务不可用" description={text(status.error, 'Docker Engine 未运行或当前用户无法访问 Docker Socket')} action={<Button tone="primary" busy={working} onClick={() => mutate('/api/v1/docker/install', {}, 'Docker 安装或修复完成')}>安装或修复 Docker</Button>} /></Card> : <>{state.statsError ? <div className="notice warning"><Icon name="warning" /><div><strong>容器实时统计读取失败</strong><span>基础状态仍可管理；CPU 和内存暂时显示为“—”。{state.statsError}</span></div></div> : null}<div className="tab-bar" role="tablist">{([['containers', `容器 ${state.containers.length}`], ['compose', `Compose ${state.projects.length}`], ['images', `镜像 ${state.images.length}`], ['networks', `网络 ${state.networks.length}`], ['volumes', `存储卷 ${state.volumes.length}`]] as [Tab, string][]).map(([value, label]) => <button role="tab" aria-selected={tab === value} className={tab === value ? 'active' : ''} key={value} onClick={() => setTab(value)}>{label}</button>)}</div>{tab === 'containers' ? <div className="card-list">{state.containers.map((item: any) => { const running = item.state === 'running'; const stats = state.stats[item.id] || {}; return <Card className="container-card" key={item.id}><div className="list-main"><span className={`state-indicator ${running ? 'is-up' : 'is-down'}`} /><div><div className="card-topline"><strong>{containerName(item)}</strong><Badge tone={running ? 'success' : 'neutral'}>{text(item.state)}</Badge></div><p>{text(item.image)}</p><small>{text(item.status)}</small><div className="container-stats"><span>CPU {stats.cpu_percent === undefined || stats.cpu_percent === null ? '—' : `${number(stats.cpu_percent).toFixed(1)}%`}</span><span>内存 {stats.memory_usage ? formatBytes(stats.memory_usage) : '—'}</span><span>{listOf<any>(item.ports).map(port => number(port.PublicPort) > 0 ? `${port.IP ? `${port.IP}:` : ''}${port.PublicPort}:${port.PrivatePort}/${port.Type || 'tcp'}` : `${port.PrivatePort}/${port.Type || 'tcp'}（仅容器内）`).join(' · ') || '无端口声明'}</span></div></div></div><div className="list-actions">{running ? <><Button onClick={() => containerAction(item, 'restart')} icon="restart">重启</Button><Button onClick={() => containerAction(item, 'stop')} icon="stop">停止</Button></> : <Button tone="primary" onClick={() => containerAction(item, 'start')} icon="play">启动</Button>}<Button tone="ghost" onClick={() => showLogs(item)}>日志</Button><Button tone="ghost" onClick={() => runDiagnostic(item)}>诊断</Button><Button tone="ghost" onClick={() => editContainer(item)}>编辑</Button><Button tone="danger" onClick={() => containerAction(item, 'remove')} icon="trash">删除</Button></div></Card>; })}{!state.containers.length ? <EmptyState icon="docker" title="暂无容器" /> : null}</div> : null}{tab === 'compose' ? <><div className="toolbar-row"><Button tone="primary" icon="plus" onClick={createCompose}>新建 Compose</Button></div><div className="card-list">{state.projects.map((project: any) => <Card className="compose-card" key={project.name}><div><div className="card-topline"><strong>{project.name}</strong><Badge tone={number(project.running) === number(project.total) && number(project.total) > 0 ? 'success' : 'warning'}>{number(project.running)}/{number(project.total)} 运行</Badge></div><p className="mono">{text(project.working_dir)}</p><small>{listOf<string>(project.config_files).join(', ')}</small></div><div className="list-actions"><Button onClick={() => composeAction(project, 'up')} icon="play">启动</Button><Button onClick={() => composeAction(project, 'restart')} icon="restart">重启</Button><Button tone="ghost" onClick={() => editCompose(project)} icon="edit">YAML</Button><Button tone="danger" onClick={() => composeAction(project, 'down')} icon="stop">Down</Button></div></Card>)}{!state.projects.length ? <EmptyState icon="docker" title="暂无 Compose 项目" action={<Button tone="primary" icon="plus" onClick={createCompose}>创建项目</Button>} /> : null}</div></> : null}{tab === 'images' ? <><div className="toolbar-row"><Button tone="primary" icon="plus" onClick={imageDialog}>拉取/构建镜像</Button></div><div className="docker-card-grid">{state.images.map((item: any) => { const name = item.repo_tags?.[0] || String(item.id).replace(/^sha256:/, '').slice(0, 12); return <Card className="docker-resource-card" key={item.id}><div className="card-topline"><strong>{name}</strong><Badge>{formatBytes(item.size)}</Badge></div><code>{String(item.id).replace(/^sha256:/, '').slice(0, 24)}</code><small>{formatDate(number(item.created) * 1000)} · {number(item.containers)} 个容器引用</small><Button tone="danger" icon="trash" onClick={async () => { if (await props.confirm('删除镜像', name, '删除', true)) await mutate('/api/v1/docker/images/delete', { id: item.id }, '镜像已删除'); }}>删除镜像</Button></Card>; })}</div></> : null}{tab === 'networks' ? <><div className="toolbar-row"><Button tone="primary" icon="plus" onClick={() => createResource('network')}>新建网络</Button></div><div className="docker-card-grid">{state.networks.map((item: any) => <Card className="docker-resource-card" key={item.id}><div className="card-topline"><strong>{item.name}</strong><Badge>{text(item.driver)}</Badge></div><code>{String(item.id).slice(0, 24)}</code><small>{text(item.scope)} · {item.internal ? '内部网络' : '可访问外部'} · {number(item.containers)} 个容器</small>{!['bridge', 'host', 'none'].includes(item.name) ? <Button tone="danger" icon="trash" onClick={async () => { if (await props.confirm('删除网络', item.name, '删除', true)) await mutate('/api/v1/docker/networks/delete', { id: item.id }, '网络已删除'); }}>删除网络</Button> : null}</Card>)}</div></> : null}{tab === 'volumes' ? <><div className="toolbar-row"><Button tone="primary" icon="plus" onClick={() => createResource('volume')}>新建存储卷</Button><Button onClick={() => volumeTools()}>用量与备份</Button></div><div className="docker-card-grid">{state.volumes.map((item: any) => <Card className="docker-resource-card" key={item.name}><div className="card-topline"><strong>{item.name}</strong><Badge>{text(item.driver)}</Badge></div><code>{text(item.mountpoint)}</code><small>{text(item.scope)}</small><div className="row-buttons"><Button onClick={() => volumeTools(item)}>用量/备份</Button><Button tone="danger" icon="trash" onClick={async () => { if (await props.confirm('删除存储卷', `${item.name}\n确认其中不再包含需要的数据。`, '删除', true)) await mutate('/api/v1/docker/volumes/delete', { name: item.name }, '存储卷已删除'); }}>删除</Button></div></Card>)}</div></> : null}</>}</div>;
  }

  function FilesPage(props: PageProps) {
    type Mode = 'files' | 'favorites' | 'recent' | 'recycle';
    const [listing, setListing] = useState<any>({ path: '/', parent: '', entries: [] }); const [preferences, setPreferences] = useState<any>({ favorites: [], recent: [] }); const [recycle, setRecycle] = useState<any[]>([]); const [mode, setMode] = useState<Mode>('files'); const [filter, setFilter] = useState(''); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState(''); const [uploadMenuOpen, setUploadMenuOpen] = useState(false); const fileInput = useRef<any>(null); const folderInput = useRef<any>(null); const zipInput = useRef<any>(null);
    const normalizePreferences = (value: any) => ({ favorites: listOf<any>(value, 'favorites'), recent: listOf<any>(value, 'recent') });
    const load = useCallback(async (path = listing.path || '/') => { setLoading(true); setError(''); try { const [next, prefs] = await Promise.all([api(`/api/v1/files${query({ path })}`), api('/api/v1/files/preferences')]); setListing({ ...asObject(next), entries: listOf<any>(next, 'entries') }); setPreferences(normalizePreferences(prefs)); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, [listing.path]);
    const loadRecycle = useCallback(async () => { setLoading(true); setError(''); try { setRecycle(listOf<any>(await api('/api/v1/files/recycle'), 'entries')); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, []);
    useEffect(() => { void load('/'); }, []);
    const switchMode = async (next: Mode) => { setMode(next); setFilter(''); if (next === 'recycle') await loadRecycle(); else if (next === 'files') await load(listing.path); else { setLoading(true); try { setPreferences(normalizePreferences(await api('/api/v1/files/preferences'))); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } } };
    const favorites = new Set(listOf<any>(preferences, 'favorites').map(item => item.path));
    const sourceEntries = mode === 'favorites' ? listOf<any>(preferences, 'favorites') : mode === 'recent' ? listOf<any>(preferences, 'recent') : listOf<any>(listing, 'entries');
    const entries = sourceEntries.map((item: any) => ({ ...item, name: item.name || String(item.path || '').split('/').pop(), is_dir: item.is_dir ?? item.directory })).filter((item: any) => !filter || `${item.name} ${item.path}`.toLowerCase().includes(filter.toLowerCase()));
    const breadcrumbs = useMemo(() => { const rows = [{ label: '根目录', path: '/' }]; let current = ''; for (const part of String(listing.path || '/').split('/').filter(Boolean)) { current += `/${part}`; rows.push({ label: part, path: current }); } return rows; }, [listing.path]);
    const joinPath = (base: string, name: string) => `${base === '/' ? '' : base}/${name}`.replace(/\/+/g, '/') || '/'; const parentOf = (path: string) => path.slice(0, path.lastIndexOf('/')) || '/';
    const mutate = async (endpoint: string, body: Dict<any>, success: string): Promise<boolean> => { setWorking(true); try {
      await secureApi(endpoint, { method: 'POST', body: jsonBody(body) });
      const sourcePath = String(body.source || body.path || '');
      const favoriteEntry = listOf<any>(preferences, 'favorites').find(item => item.path === sourcePath);
      if (favoriteEntry && endpoint === '/api/v1/files/delete') {
        await api('/api/v1/files/preferences', { method: 'POST', body: jsonBody({ action: 'unfavorite', path: sourcePath, is_dir: !!favoriteEntry.is_dir }) });
      } else if (favoriteEntry && (endpoint === '/api/v1/files/rename' || endpoint === '/api/v1/files/move')) {
        const destination = String(body.destination || '');
        await api('/api/v1/files/preferences', { method: 'POST', body: jsonBody({ action: 'unfavorite', path: sourcePath, is_dir: !!favoriteEntry.is_dir }) });
        await api('/api/v1/files/preferences', { method: 'POST', body: jsonBody({ action: 'favorite', path: destination, is_dir: !!favoriteEntry.is_dir }) });
      }
      props.notify('success', success); await load(listing.path); return true;
    } catch (cause) { props.notify('error', '文件操作失败', errorDetail(cause)); return false; } finally { setWorking(false); } };
    const openEditor = async (item: any) => { try { const content = asObject(await secureApi(`/api/v1/files/content${query({ path: item.path })}`)); const value = text(content.content, ''); const editor = <FileEditorModal item={item} initial={value} onClose={props.closeModal} onSave={async next => { await secureApi('/api/v1/files/content', { method: 'PUT', body: jsonBody({ path: item.path, content: next }) }); await load(listing.path); props.notify('success', '文件已保存，并生成历史版本'); }} onHistory={() => showHistory(item.path)} confirm={props.confirm} />; props.openModal({ title: item.name, size: 'large', content: editor }); } catch (cause) { props.notify('error', '读取文件失败', errorDetail(cause)); } };
    const downloadEntry = async (item: any) => { try { await secureApi(`/api/v1/files/preview${query({ path: item.path })}`); location.assign(`/api/v1/files/download${query({ path: item.path })}`); } catch (cause) { props.notify('error', '下载准备失败', errorDetail(cause)); } };
    const openEntry = async (item: any) => { if (item.is_dir) { setMode('files'); await load(item.path); return; } setWorking(true); try { const preview = asObject(await secureApi(`/api/v1/files/preview${query({ path: item.path })}`)); const kind = text(preview.kind, 'download'); if (kind === 'text' || kind === 'markdown') { await openEditor(item); } else if (kind === 'image') { props.openModal({ title: item.name, size: 'large', content: <div className="preview-wrap"><img className="image-preview" src={`/api/v1/files/preview/raw${query({ path: item.path })}`} alt={item.name} /><a className="button button-default" href={`/api/v1/files/download${query({ path: item.path })}`}><Icon name="download" size={17} /><span>下载</span></a></div> }); } else if (kind === 'pdf') { props.openModal({ title: item.name, size: 'large', content: <div className="preview-wrap"><iframe className="pdf-preview" src={`/api/v1/files/preview/raw${query({ path: item.path })}`} title={item.name} /><a className="button button-default" href={`/api/v1/files/download${query({ path: item.path })}`}><Icon name="download" size={17} /><span>下载</span></a></div> }); } else if (kind === 'archive') { const archive = await secureApi(`/api/v1/files/archive/list${query({ path: item.path })}`); props.openModal({ title: `${item.name} · 压缩包内容`, size: 'large', content: <><JsonDetails data={archive} title="归档条目" /><a className="button button-default" href={`/api/v1/files/download${query({ path: item.path })}`}><Icon name="download" size={17} /><span>下载</span></a></> }); } else { await downloadEntry(item); } } catch (cause) { props.notify('error', '无法打开文件', errorDetail(cause)); } finally { setWorking(false); } };
    const showHistory = async (path: string) => { try { const result = await secureApi(`/api/v1/files/backups${query({ path })}`); const versions = listOf<any>(result, 'backups', 'versions'); props.openModal({ title: '历史版本', size: 'large', content: versions.length ? <div className="card-list compact-list">{versions.map(version => <div className="history-row" key={version.id}><div><strong>{formatDate(version.created_at)}</strong><small>{formatBytes(version.size)} · {version.id}</small></div><div className="row-buttons"><Button onClick={async () => { try { const diff = asObject(await secureApi(`/api/v1/files/backups/diff${query({ path: version.path || path, id: version.id })}`)); props.openModal({ title: '版本差异', size: 'large', content: <div className="form-stack"><Terminal maxHeight={600}>{diff.diff || '没有差异'}</Terminal><div className="form-actions"><Button onClick={props.closeModal}>关闭</Button><Button tone="primary" onClick={async () => { if (await props.confirm('恢复历史版本', `恢复到 ${formatDate(version.created_at)}？当前内容会先自动备份。`, '恢复')) { await secureApi('/api/v1/files/backups/restore', { method: 'POST', body: jsonBody({ path: version.path || path, id: version.id }) }); await load(listing.path); props.notify('success', '历史版本已恢复'); props.closeModal(); } }}>恢复此版本</Button></div></div> }); } catch (cause) { props.notify('error', '版本对比失败', errorDetail(cause)); } }}>对比</Button><Button tone="primary" onClick={async () => { if (await props.confirm('恢复历史版本', `恢复到 ${formatDate(version.created_at)}？`, '恢复')) { try { await secureApi('/api/v1/files/backups/restore', { method: 'POST', body: jsonBody({ path: version.path || path, id: version.id }) }); await load(listing.path); props.notify('success', '历史版本已恢复'); props.closeModal(); } catch (cause) { props.notify('error', '恢复失败', errorDetail(cause)); } } }}>恢复</Button></div></div>)}</div> : <EmptyState icon="backup" title="还没有历史版本" description="在线保存文件前会自动生成备份" /> }); } catch (cause) { props.notify('error', '历史版本读取失败', errorDetail(cause)); } };
    const promptMutation = (title: string, label: string, initial: string, endpoint: string, body: (value: string) => Dict<any>, success: string, pattern?: RegExp) => props.openModal({ title, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const value = String(new FormData(event.currentTarget).get('value') || '').trim(); if (!value || (pattern && !pattern.test(value))) { props.notify('warning', '输入内容不符合要求'); return; } if (await mutate(endpoint, body(value), success)) props.closeModal(); }}><Field label={label}><TextInput name="value" defaultValue={initial} autoFocus required /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">确认</Button></div></form> });
    const createEntry = (kind: 'file' | 'folder') => promptMutation(kind === 'file' ? '新建文件' : '新建文件夹', '名称', '', kind === 'file' ? '/api/v1/files/create' : '/api/v1/files/mkdir', value => ({ path: joinPath(listing.path, value) }), kind === 'file' ? '文件已创建' : '文件夹已创建');
    const actionMenu = (item: any) => props.openModal({ title: item.name, content: <div className="action-menu"><button onClick={() => { props.closeModal(); void openEntry(item); }}><Icon name="edit" /><span>打开</span></button><button onClick={() => { void copyText(item.path).then(() => props.notify('success', '路径已复制')).catch(() => props.openModal({ title: '完整路径', content: <Terminal>{item.path}</Terminal> })); }}><Icon name="copy" /><span>复制完整路径</span></button>{!item.is_dir ? <><button onClick={() => void downloadEntry(item)}><Icon name="download" /><span>下载</span></button><button onClick={() => showHistory(item.path)}><Icon name="backup" /><span>历史版本</span></button></> : null}<button onClick={() => promptMutation('重命名', '目标绝对路径', joinPath(parentOf(item.path), item.name), '/api/v1/files/rename', value => ({ source: item.path, destination: value }), '已重命名', /^\//)}><Icon name="edit" /><span>重命名</span></button><button onClick={() => promptMutation('复制到', '目标绝对路径', `${item.path}-副本`, '/api/v1/files/copy', value => ({ source: item.path, destination: value }), '复制完成', /^\//)}><Icon name="copy" /><span>复制到…</span></button><button onClick={() => promptMutation('移动到', '目标绝对路径', item.path, '/api/v1/files/move', value => ({ source: item.path, destination: value }), '移动完成', /^\//)}><Icon name="files" /><span>移动到…</span></button><button onClick={() => promptMutation('修改权限', '权限模式', item.mode?.match(/[0-7]{3,4}$/)?.[0] || (item.is_dir ? '755' : '644'), '/api/v1/files/chmod', value => ({ path: item.path, mode: value }), '权限已更新', /^[0-7]{3,4}$/)}><Icon name="security" /><span>修改权限</span></button><button onClick={() => props.openModal({ title: '修改所有者', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); if (await mutate('/api/v1/files/chown', { path: item.path, owner: values.get('owner'), group: values.get('group') }, '所有者已更新')) props.closeModal(); }}><Field label="用户"><TextInput name="owner" required /></Field><Field label="用户组"><TextInput name="group" /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">保存</Button></div></form> })}><Icon name="user" /><span>修改所有者</span></button><button onClick={() => promptMutation('压缩包保存路径', '目标绝对路径', joinPath(parentOf(item.path), `${item.name.replace(/\.(tar\.gz|zip)$/i, '')}.tar.gz`), '/api/v1/files/archive/create', value => ({ destination: value, sources: [item.path], format: value.endsWith('.zip') ? 'zip' : 'tar.gz' }), '压缩完成', /^\//)}><Icon name="package" /><span>创建压缩包</span></button><button className="danger" onClick={async () => { if (await props.confirm('移到回收站', `确认删除“${item.name}”？可稍后恢复。`, '删除', true)) { if (await mutate('/api/v1/files/delete', { path: item.path }, '已移到回收站')) props.closeModal(); } }}><Icon name="trash" /><span>移到回收站</span></button></div> });
    const toggleFavorite = async (item: any) => { const enabled = !favorites.has(item.path); setWorking(true); try { setPreferences(normalizePreferences(await api('/api/v1/files/preferences', { method: 'POST', body: jsonBody({ action: enabled ? 'favorite' : 'unfavorite', path: item.path, is_dir: item.is_dir }) }))); props.notify('success', enabled ? '已加入收藏' : '已取消收藏'); } catch (cause) { props.notify('error', '收藏操作失败', errorDetail(cause)); } finally { setWorking(false); } };
    const upload = async (event: any, extract = false) => { const input = event.target as HTMLInputElement; const files = Array.from(input.files || []); if (!files.length) return; setWorking(true); let completed = 0; try { if (extract) { const form = new FormData(); form.set('directory', listing.path); form.set('overwrite', 'false'); form.set('file', files[0]); const out = asObject(await secureApi('/api/v1/files/archive/extract', { method: 'POST', body: form })); props.notify('success', `已解压 ${number(out.files)} 个文件和 ${number(out.dirs)} 个目录`); } else { for (const file of files as File[]) { const form = new FormData(); form.set('directory', listing.path); form.set('relative_path', (file as any).webkitRelativePath || file.name); form.set('overwrite', 'false'); form.set('file', file); await secureApi('/api/v1/files/upload', { method: 'POST', body: form }); completed++; } props.notify('success', `已上传 ${completed} 个文件`); } await load(listing.path); } catch (cause) { props.notify('error', extract ? 'ZIP 解压失败' : `上传中断（${completed}/${files.length}）`, errorDetail(cause)); } finally { input.value = ''; setWorking(false); } };
    const searchAll = () => props.openModal({ title: `在 ${listing.path} 中搜索`, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const term = String(new FormData(event.currentTarget).get('term') || '').trim(); try { const result = await api(`/api/v1/files/search${query({ root: listing.path, q: term })}`); const rows = listOf<any>(result, 'entries', 'results'); props.openModal({ title: `搜索结果（${rows.length}）`, size: 'large', content: rows.length ? <div className="card-list compact-list">{rows.map(item => <button className="search-result" key={item.path} onClick={() => { props.closeModal(); void openEntry(item); }}><Icon name={item.is_dir ? 'files' : 'edit'} /><span><strong>{item.name}</strong><small>{item.path}</small></span><Icon name="chevron" /></button>)}</div> : <EmptyState icon="search" title="没有匹配项" /> }); } catch (cause) { props.notify('error', '搜索失败', errorDetail(cause)); } }}><Field label="文件名或路径"><TextInput name="term" required autoFocus /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" icon="search">搜索</Button></div></form> });
    const recycleAction = async (item: any, action: 'restore' | 'purge') => { if (action === 'purge' && !await props.confirm('永久删除', '此操作不可恢复。', '永久删除', true)) return; setWorking(true); try { await secureApi('/api/v1/files/recycle', { method: 'POST', body: jsonBody({ id: item.id, action, destination: '' }) }); props.notify('success', action === 'restore' ? '文件已恢复' : '已永久删除'); await loadRecycle(); } catch (cause) { props.notify('error', '回收站操作失败', errorDetail(cause)); } finally { setWorking(false); } };
    return <div className="page-stack files-page"><PageHeader {...props} busy={loading} onRefresh={() => mode === 'recycle' ? loadRecycle() : load(listing.path)} actions={<>{mode !== 'recycle' ? <Button tone="ghost" icon="search" onClick={searchAll}>搜索</Button> : null}{mode === 'files' ? <><Button tone="ghost" icon="plus" onClick={() => createEntry('file')}>新建文件</Button><Button tone="ghost" icon="plus" onClick={() => createEntry('folder')}>新建文件夹</Button><Button tone="primary" icon="upload" busy={working} onClick={() => fileInput.current?.click()}>上传</Button><input ref={fileInput} className="visually-hidden" type="file" multiple onChange={(event: any) => upload(event)} /><input ref={folderInput} className="visually-hidden" type="file" multiple {...({ webkitdirectory: '' } as any)} onChange={(event: any) => upload(event)} /><input ref={zipInput} className="visually-hidden" type="file" accept=".zip,application/zip" onChange={(event: any) => upload(event, true)} /><div className={`dropdown ${uploadMenuOpen ? 'is-open' : ''}`}><Button tone="ghost" icon="more" title="更多上传方式" onClick={() => setUploadMenuOpen(current => !current)} aria-expanded={uploadMenuOpen}>更多</Button><div className="dropdown-menu"><button onClick={() => { setUploadMenuOpen(false); folderInput.current?.click(); }}>上传文件夹</button><button onClick={() => { setUploadMenuOpen(false); zipInput.current?.click(); }}>上传 ZIP 并解压</button></div></div></> : null}</>} /><div className="tab-bar" role="tablist">{([['files', '文件'], ['favorites', `收藏 ${listOf(preferences, 'favorites').length}`], ['recent', `最近 ${listOf(preferences, 'recent').length}`], ['recycle', `回收站 ${recycle.length || ''}`]] as [Mode, string][]).map(([value, label]) => <button key={value} className={mode === value ? 'active' : ''} onClick={() => void switchMode(value)}>{label}</button>)}</div>{error ? <ErrorState error={error} retry={() => mode === 'recycle' ? loadRecycle() : load(listing.path)} /> : mode === 'recycle' ? <Card className="file-list-card">{recycle.length ? recycle.map(item => <div className="file-row" key={item.id}><span className="file-icon"><Icon name={item.is_dir ? 'files' : 'edit'} /></span><div className="file-main"><strong>{item.name}</strong><small>{item.original_path} · {formatDate(item.deleted_at)}</small></div><div className="row-buttons"><Button tone="primary" onClick={() => recycleAction(item, 'restore')}>恢复</Button><Button tone="danger" onClick={() => recycleAction(item, 'purge')}>永久删除</Button></div></div>) : !loading ? <EmptyState icon="trash" title="回收站是空的" /> : <LoadingState />}</Card> : <><Card className="file-toolbar"><Button tone="ghost" icon="back" disabled={!listing.parent} onClick={() => listing.parent && load(listing.parent)} title="返回上级目录" /><nav className="breadcrumbs" aria-label="当前目录">{breadcrumbs.map(crumb => <button key={crumb.path} onClick={() => load(crumb.path)}>{crumb.label}</button>)}</nav><Button tone="ghost" icon="copy" onClick={() => copyText(listing.path).then(() => props.notify('success', '路径已复制')).catch(() => props.openModal({ title: '当前路径', content: <Terminal>{listing.path}</Terminal> }))}>复制路径</Button></Card><div className="file-filter"><div className="search-box"><Icon name="search" size={17} /><input value={filter} onChange={(event: any) => setFilter(event.target.value)} placeholder={mode === 'files' ? '筛选当前目录' : mode === 'favorites' ? '筛选收藏' : '筛选最近访问'} /></div><Badge>{entries.length}</Badge></div><Card className="file-list-card">{entries.length ? entries.map((item: any) => <div className="file-row" key={item.path}><button className="file-open" onClick={() => openEntry(item)}><span className="file-icon"><Icon name={item.is_dir ? 'files' : 'edit'} /></span><span className="file-main"><strong>{item.name}</strong><small>{item.path}{mode === 'files' ? ` · ${item.is_dir ? '文件夹' : formatBytes(item.size)} · ${formatDate(item.modified_at)}` : ''}</small></span></button><button className={`favorite-button ${favorites.has(item.path) ? 'active' : ''}`} onClick={() => toggleFavorite(item)} aria-label={favorites.has(item.path) ? '取消收藏' : '加入收藏'}>★</button><button className="icon-button" onClick={() => actionMenu(item)} aria-label="文件操作"><Icon name="more" /></button></div>) : !loading ? <EmptyState icon="files" title="没有可显示的文件" /> : <LoadingState />}</Card></>}</div>;
  }

  function FileEditorModal({ item, initial, onClose, onSave, onHistory, confirm }: { item: any; initial: string; onClose: () => void; onSave: (value: string) => Promise<void>; onHistory: () => void; confirm: ConfirmFn }) {
    const [value, setValue] = useState(initial); const [saved, setSaved] = useState(initial); const [busy, setBusy] = useState(false);
    const close = async () => { if (value !== saved && !await confirm('放弃未保存内容', '编辑器中仍有未保存的修改，关闭后这些修改会丢失。', '放弃修改', true)) return; onClose(); };
    const history = async () => { if (value !== saved && !await confirm('打开历史版本', '当前修改尚未保存。打开历史版本会关闭编辑器并丢弃这些修改。', '继续打开', true)) return; onHistory(); };
    return <div className="editor-layout"><div className="editor-toolbar"><span className="mono">{item.path}</span><div><Button tone="ghost" onClick={history}>历史版本</Button><Button tone="primary" busy={busy} disabled={value === saved} onClick={async () => { setBusy(true); try { await onSave(value); setSaved(value); } finally { setBusy(false); } }}>保存</Button><Button tone="ghost" onClick={close}>关闭</Button></div></div><textarea className="code-editor" value={value} onChange={(event: any) => setValue(event.target.value)} spellCheck={false} /></div>;
  }

  function ToolsPage(props: PageProps) {
    const [tool, setTool] = useState('ping'); const [target, setTarget] = useState(''); const [port, setPort] = useState(443); const [result, setResult] = useState<any>(null); const [running, setRunning] = useState(false);
    const options: Dict<any> = { ping: ['Ping', '检查 ICMP 延迟与丢包'], dns: ['DNS', '解析域名的 A/AAAA 地址'], tcp: ['TCP', '测试目标端口可达性'], http: ['HTTP', '检查网址响应和重定向'], diagnostic: ['一键诊断', '汇总负载、磁盘、服务和端口'] };
    const run = async (event?: any) => { event?.preventDefault(); setRunning(true); setResult(null); try { setResult(await api('/api/v1/tools/run', { method: 'POST', body: jsonBody({ tool, target: tool === 'diagnostic' ? '' : target, port: tool === 'tcp' ? port : 0 }) })); } catch (cause) { props.notify('error', '工具执行失败', errorDetail(cause)); } finally { setRunning(false); } };
    return <div className="page-stack"><PageHeader {...props} /><section className="tools-grid"><Card><SectionTitle title="网络诊断" subtitle="在服务器侧执行，不经过浏览器网络" /><form className="form-stack" onSubmit={run}><Field label="工具"><SelectInput value={tool} onChange={(event: any) => setTool(event.target.value)}>{Object.entries(options).map(([value, item]) => <option value={value} key={value}>{item[0]}</option>)}</SelectInput></Field>{tool !== 'diagnostic' ? <Field label={tool === 'http' ? '网址或域名' : '目标域名或 IP'}><TextInput value={target} onChange={(event: any) => setTarget(event.target.value)} placeholder="example.com" required /></Field> : null}{tool === 'tcp' ? <Field label="端口"><TextInput type="number" min="1" max="65535" value={port} onChange={(event: any) => setPort(number(event.target.value, 443))} /></Field> : null}<p className="field-hint">{options[tool][1]}</p><Button type="submit" tone="primary" busy={running} className="full-width">开始执行</Button></form></Card><Card><SectionTitle title="执行结果" subtitle={result ? `${text(result.target)} · ${text(result.duration_ms, '—')} ms` : '结果会完整显示在这里'} actions={result?.output ? <Button tone="ghost" icon="copy" onClick={() => copyText(result.output).then(() => props.notify('success', '结果已复制'))}>复制</Button> : null} />{running ? <LoadingState rows={5} /> : result ? <Terminal maxHeight={520}>{result.output || listOf<string>(result.addresses).join('\n') || '命令没有返回内容'}</Terminal> : <EmptyState icon="terminal" title="等待执行" description="选择工具并填写目标后执行" />}</Card></section><Card><SectionTitle title="扩展工具" /><button className="module-card full-module" onClick={() => props.navigate('/tools/github')}><span className="module-icon"><Icon name="github" /></span><div><strong>GitHub 助手</strong><p>设备登录、仓库、Actions、Release 和 ZIP 推送</p></div><Icon name="chevron" /></button></Card></div>;
  }

  function GitHubPage(props: PageProps) {
    const [auth, setAuth] = useState<any>({}); const [flow, setFlow] = useState<any>(null); const [summary, setSummary] = useState<any>(null); const [owner, setOwner] = useState(localStorage.getItem('lukepanel:github-owner') || ''); const [repo, setRepo] = useState(localStorage.getItem('lukepanel:github-repo') || ''); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState(''); const pollRef = useRef<number>(0); const zipRef = useRef<any>(null); const assetRef = useRef<any>(null); const [importPlan, setImportPlan] = useState<any>(null); const [importBranch, setImportBranch] = useState('main'); const [importMessage, setImportMessage] = useState('chore: import files from LukePanel');
    const loadAuth = async () => { const next = asObject(await api('/api/v1/github/auth/status')); setAuth(next); return next; };
    const loadRepo = async () => { if (!owner.trim() || !repo.trim()) return; setWorking(true); try { const next = asObject(await api(`/api/v1/github/summary${query({ owner: owner.trim(), repo: repo.trim() })}`)); setSummary(next); localStorage.setItem('lukepanel:github-owner', owner.trim()); localStorage.setItem('lukepanel:github-repo', repo.trim()); setImportBranch(text(next.default_branch, 'main')); } catch (cause) { props.notify('error', '读取仓库失败', errorDetail(cause)); } finally { setWorking(false); } };
    const reload = useCallback(async () => { setLoading(true); setError(''); try { await loadAuth(); if (owner.trim() && repo.trim()) { const next = asObject(await api(`/api/v1/github/summary${query({ owner: owner.trim(), repo: repo.trim() })}`)); setSummary(next); } } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, [owner, repo]); useEffect(() => { void reload(); return () => window.clearTimeout(pollRef.current); }, []);
    const connectToken = () => props.openModal({ title: '连接 GitHub Token', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const token = String(new FormData(event.currentTarget).get('token') || '').trim(); setWorking(true); try { const next = asObject(await api('/api/v1/github/auth/token', { method: 'POST', body: jsonBody({ token }) })); setAuth(next); props.closeModal(); props.notify('success', `已连接 GitHub @${text(next.login)}`); } catch (cause) { props.notify('error', '连接 GitHub 失败', errorDetail(cause)); } finally { setWorking(false); } }}><Field label="Fine-grained Personal Access Token" hint="Token 只保存在当前登录会话内存，退出或重启后失效"><TextInput name="token" type="password" required autoFocus /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>验证并连接</Button></div></form> });
    const pollDevice = async (current: any) => { try { const out = asObject(await api('/api/v1/github/auth/device/poll', { method: 'POST', body: jsonBody({ flow_id: current.flow_id }) })); if (out.status === 'authorized') { setFlow(null); await loadAuth(); props.notify('success', 'GitHub 设备登录完成'); return; } if (out.status === 'expired' || out.status === 'denied') throw new Error(text(out.message, '设备登录已过期或被拒绝')); pollRef.current = window.setTimeout(() => void pollDevice(current), Math.max(2, number(out.retry_after || current.interval, 5)) * 1000); } catch (cause) { setFlow(null); props.notify('error', '设备登录失败', errorDetail(cause)); } };
    const startDevice = async () => { setWorking(true); try { const next = asObject(await api('/api/v1/github/auth/device/start', { method: 'POST', body: '{}' })); setFlow(next); window.open(text(next.verification_uri), '_blank', 'noopener'); pollRef.current = window.setTimeout(() => void pollDevice(next), Math.max(2, number(next.interval, 5)) * 1000); } catch (cause) { props.notify('error', '无法启动设备登录', errorDetail(cause)); } finally { setWorking(false); } };
    const disconnect = async () => { if (!await props.confirm('断开 GitHub', '当前会话中的 Token 会立即从内存删除。', '断开', true)) return; try { await api('/api/v1/github/auth/disconnect', { method: 'POST', body: '{}' }); setAuth({ connected: false }); setSummary(null); props.notify('success', '已断开 GitHub'); } catch (cause) { props.notify('error', '断开失败', errorDetail(cause)); } };
    const repoMutation = async (endpoint: string, body: Dict<any>, success: string) => { setWorking(true); try { const out = asObject(await secureApi(endpoint, { method: 'POST', body: jsonBody({ owner: summary?.owner || owner, repo: summary?.name || repo, ...body }) })); props.notify('success', success, out.message || out.output); await loadRepo(); return out; } catch (cause) { props.notify('error', 'GitHub 操作失败', errorDetail(cause)); throw cause; } finally { setWorking(false); } };
    const simpleForm = (title: string, fields: any[], submit: (values: FormData) => Promise<void>, submitLabel = '确认') => props.openModal({ title, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); await submit(new FormData(event.currentTarget)); }} >{fields.map((field, index) => <Field label={field.label} hint={field.hint} key={index}>{field.kind === 'textarea' ? <TextArea name={field.name} rows={field.rows || 5} defaultValue={field.value || ''} required={field.required} /> : field.kind === 'select' ? <SelectInput name={field.name} defaultValue={field.value}>{field.options.map((option: string) => <option key={option}>{option}</option>)}</SelectInput> : <TextInput name={field.name} defaultValue={field.value || ''} required={field.required} />}</Field>)}<div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>{submitLabel}</Button></div></form> });
    const openRun = async (run: any) => { try { const jobs = listOf<any>(await api(`/api/v1/github/actions/jobs${query({ owner: summary.owner, repo: summary.name, run_id: run.id })}`), 'jobs'); props.openModal({ title: `${run.name} · Jobs`, size: 'large', content: jobs.length ? <div className="card-list compact-list">{jobs.map(job => <div className="history-row" key={job.id}><div><strong>{job.name}</strong><small>{text(job.conclusion || job.status)} · {formatDate(job.started_at)}</small></div><Button onClick={async () => { try { const out = asObject(await api(`/api/v1/github/actions/job-logs${query({ owner: summary.owner, repo: summary.name, job_id: job.id })}`)); props.openModal({ title: `${job.name} · 日志`, size: 'large', content: <div className="form-stack"><Terminal maxHeight={650}>{out.logs}</Terminal><Button icon="copy" onClick={() => copyText(text(out.logs, '')).then(() => props.notify('success', '日志已复制'))}>复制日志</Button></div> }); } catch (cause) { props.notify('error', '读取 Job 日志失败', errorDetail(cause)); } }}>查看日志</Button></div>)}</div> : <EmptyState icon="audit" title="没有可显示的 Jobs" /> }); } catch (cause) { props.notify('error', '读取 Actions 详情失败', errorDetail(cause)); } };
    const previewImport = async (event: any) => { const file = event.target.files?.[0]; if (!file || !summary) return; setWorking(true); try { const form = new FormData(); form.set('owner', summary.owner); form.set('repo', summary.name); form.set('branch', importBranch); form.set('file', file); setImportPlan(await api('/api/v1/github/import/preview', { method: 'POST', body: form })); props.notify('success', 'ZIP 差异已生成'); } catch (cause) { props.notify('error', 'ZIP 预览失败', errorDetail(cause)); } finally { setWorking(false); event.target.value = ''; } };
    const commitImport = async () => { if (!importPlan) return; if (!await props.confirm('Commit 并 Push', `新增 ${number(importPlan.added)} · 修改 ${number(importPlan.modified)} · 删除 ${number(importPlan.deleted)}`, '提交推送')) return; try { const out = asObject(await secureApi('/api/v1/github/import/commit', { method: 'POST', body: jsonBody({ plan_id: importPlan.id, message: importMessage }) })); setImportPlan(null); props.notify('success', '文件已推送'); if (out.html_url) window.open(out.html_url, '_blank', 'noopener'); await loadRepo(); } catch (cause) { props.notify('error', '推送失败', errorDetail(cause)); } };
    const openAssets = async () => { if (!summary?.latest_release) return; const tag = summary.latest_release.tag_name; try { const assets = listOf<any>(await api(`/api/v1/github/release/assets${query({ owner: summary.owner, repo: summary.name, tag })}`), 'assets'); props.openModal({ title: `Release 附件 · ${tag}`, size: 'large', content: <div className="form-stack"><label className="button button-primary file-button"><Icon name="upload" size={17} /><span>上传附件</span><input ref={assetRef} type="file" onChange={async (event: any) => { const file = event.target.files?.[0]; if (!file) return; const form = new FormData(); form.set('owner', summary.owner); form.set('repo', summary.name); form.set('tag', tag); form.set('file', file); try { await secureApi('/api/v1/github/release/assets/upload', { method: 'POST', body: form }); props.notify('success', `${file.name} 已上传`); props.closeModal(); await openAssets(); } catch (cause) { props.notify('error', '附件上传失败', errorDetail(cause)); } }} /></label><div className="card-list compact-list">{assets.map(asset => <div className="history-row" key={asset.id}><div><strong>{asset.name}</strong><small>{formatBytes(asset.size)} · 下载 {number(asset.download_count)} 次 · {formatDate(asset.created_at)}</small></div>{asset.browser_download_url ? <a className="button button-default" href={asset.browser_download_url} target="_blank" rel="noopener">下载</a> : null}</div>)}</div></div> }); } catch (cause) { props.notify('error', '读取 Release 附件失败', errorDetail(cause)); } };
    const branches = listOf<any>(summary, 'branches').map(item => item.name); const runs = listOf<any>(summary, 'workflow_runs'); const pulls = listOf<any>(summary, 'pull_requests');
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} />{error ? <ErrorState error={error} retry={reload} /> : <><Card className="github-auth-card"><div className="github-identity"><span className="github-mark"><Icon name="github" /></span><div><h2>{auth.connected ? `@${text(auth.login)}` : '连接 GitHub'}</h2><p>{auth.connected ? `${text(auth.name || auth.scope)} · Token 仅保存在当前登录会话内存` : '推荐设备代码登录，也可以使用 Fine-grained Token。'}</p></div></div><div className="row-buttons">{auth.connected ? <Button tone="danger" onClick={disconnect}>断开</Button> : <><Button onClick={connectToken}>Token 登录</Button>{auth.device_login_available !== false ? <Button tone="primary" busy={working} onClick={startDevice}>设备登录</Button> : null}</>}</div></Card>{flow ? <div className="notice info"><Icon name="info" /><div><strong>设备代码：{text(flow.user_code)}</strong><span>已打开 GitHub 验证页，完成授权后会自动更新。</span></div><Button icon="copy" onClick={() => copyText(flow.user_code).then(() => props.notify('success', '设备代码已复制'))}>复制</Button><Button tone="danger" onClick={async () => { window.clearTimeout(pollRef.current); try { await api('/api/v1/github/auth/device/cancel', { method: 'POST', body: jsonBody({ flow_id: flow.flow_id }) }); } finally { setFlow(null); } }}>取消</Button></div> : null}<Card><SectionTitle title="选择仓库" subtitle="LukePanel 不预设任何仓库" /><form className="repo-picker" onSubmit={(event: any) => { event.preventDefault(); void loadRepo(); }}><Field label="所有者"><TextInput value={owner} onChange={(event: any) => setOwner(event.target.value)} required /></Field><Field label="仓库"><TextInput value={repo} onChange={(event: any) => setRepo(event.target.value)} required /></Field><Button type="submit" tone="primary" busy={working}>读取仓库</Button></form></Card>{summary ? <><section className="github-summary-grid"><Card><SectionTitle title={text(summary.full_name)} subtitle={text(summary.description, '无仓库说明')} actions={<Badge>{text(summary.visibility)}</Badge>} /><dl className="key-value-list"><KeyValue label="默认分支" value={text(summary.default_branch)} /><KeyValue label="最新提交" value={<code>{text(summary.main_sha, '').slice(0, 12)}</code>} /><KeyValue label="分支" value={branches.length} /><KeyValue label="最新 Tag" value={text(summary.tags?.[0]?.name, '暂无')} /><KeyValue label="最新 Release" value={text(summary.latest_release?.tag_name, '暂无')} /></dl></Card><Card><SectionTitle title="最近 Actions" subtitle="失败任务可直接重试" />{runs.length ? <div className="workflow-list">{runs.map(run => <div className="workflow-row" key={run.id}><span className={`workflow-dot ${run.conclusion === 'success' ? 'ok' : run.status !== 'completed' ? 'running' : 'bad'}`} /><div><strong>{text(run.name)}</strong><small>{text(run.head_branch || run.event)} · {formatDate(run.created_at)}</small></div><div className="row-buttons"><Badge tone={run.conclusion === 'success' ? 'success' : run.status !== 'completed' ? 'warning' : 'danger'}>{text(run.conclusion || run.status)}</Badge><Button tone="ghost" onClick={() => openRun(run)}>详情</Button>{['failure', 'cancelled', 'timed_out'].includes(run.conclusion) ? <Button onClick={() => repoMutation('/api/v1/github/rerun', { run_id: run.id }, '已请求重试失败任务')}>重试</Button> : null}</div></div>)}</div> : <EmptyState icon="audit" title="暂无 Actions 记录" />}</Card></section><section className="github-summary-grid"><Card><SectionTitle title="Pull Requests" subtitle="合并仍遵守分支保护和检查" />{pulls.length ? <div className="workflow-list">{pulls.map(item => <div className="workflow-row" key={item.number}><span className={`workflow-dot ${item.draft ? 'running' : 'ok'}`} /><div><strong>#{item.number} · {text(item.title)}</strong><small>{text(item.head)} → {text(item.base)}{item.draft ? ' · 草稿' : ''}</small></div><div className="row-buttons">{item.html_url ? <a className="button button-ghost" href={item.html_url} target="_blank" rel="noopener">打开</a> : null}{auth.connected && item.state === 'open' && !item.draft ? <Button tone="primary" onClick={() => props.openModal({ title: '合并 Pull Request', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const method = String(new FormData(event.currentTarget).get('method') || 'squash'); if (!await props.confirm('确认合并 Pull Request', `#${item.number} · ${text(item.title)}\n合并方式：${method}`, '合并')) return; await repoMutation('/api/v1/github/pull/merge', { number: item.number, expected_sha: item.head_sha || '', method }, 'Pull Request 已合并'); props.closeModal(); }}><Field label="合并方式"><SelectInput name="method" defaultValue="squash"><option value="squash">Squash merge（推荐）</option><option value="merge">Merge commit</option><option value="rebase">Rebase merge</option></SelectInput></Field><p className="form-help">合并仍会遵守仓库的分支保护、检查状态和最新 Head SHA。</p><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">继续</Button></div></form> })}>合并</Button> : null}</div></div>)}</div> : <EmptyState icon="github" title="暂无开放的 Pull Request" />}</Card><Card><SectionTitle title="Release 附件" subtitle={text(summary.latest_release?.tag_name, '尚未创建 Release')} />{summary.latest_release ? <div className="setting-card"><p>{text(summary.latest_release.name || summary.latest_release.tag_name)} · {formatDate(summary.latest_release.published_at)}</p><Button tone="primary" onClick={openAssets}>管理附件</Button></div> : <EmptyState icon="package" title="尚无 Release" />}</Card></section><div className="settings-grid"><Card><SectionTitle title="创建分支" subtitle="从现有分支或 SHA 创建" /><Button tone="primary" onClick={() => simpleForm('创建分支', [{ label: '新分支名称', name: 'name', required: true }, { label: '来源', name: 'source', kind: 'select', options: branches, value: summary.default_branch }], async values => { await repoMutation('/api/v1/github/branch', { name: values.get('name'), source: values.get('source') }, '分支已创建'); props.closeModal(); })}>创建分支</Button></Card><Card><SectionTitle title="创建版本 Tag" subtitle="默认指向默认分支最新提交" /><Button tone="primary" onClick={() => simpleForm('创建 Tag', [{ label: '版本 Tag', name: 'tag', value: VERSION, required: true }], async values => { await repoMutation('/api/v1/github/tag', { tag: values.get('tag'), target_sha: summary.main_sha }, `Tag ${values.get('tag')} 已创建`); props.closeModal(); })}>创建 Tag</Button></Card><Card><SectionTitle title="创建 Pull Request" subtitle="不会自动合并" /><Button tone="primary" onClick={() => simpleForm('创建 Pull Request', [{ label: '标题', name: 'title', required: true }, { label: 'Head', name: 'head', kind: 'select', options: branches }, { label: 'Base', name: 'base', kind: 'select', options: branches, value: summary.default_branch }, { label: '说明', name: 'body', kind: 'textarea' }], async values => { const out = await repoMutation('/api/v1/github/pull', { title: values.get('title'), body: values.get('body'), head: values.get('head'), base: values.get('base') }, 'Pull Request 已创建'); props.closeModal(); if (out.html_url) window.open(out.html_url, '_blank', 'noopener'); })}>创建 PR</Button></Card><Card><SectionTitle title="创建 Release" subtitle="Tag 必须已经存在" /><Button tone="primary" onClick={() => simpleForm('创建 Release', [{ label: 'Tag', name: 'tag', value: VERSION, required: true }, { label: '标题', name: 'name', value: `LukePanel ${VERSION}`, required: true }, { label: '发布说明', name: 'body', kind: 'textarea' }], async values => { const out = await repoMutation('/api/v1/github/release', { tag: values.get('tag'), name: values.get('name'), body: values.get('body'), draft: false, prerelease: false }, 'Release 已创建'); props.closeModal(); if (out.html_url) window.open(out.html_url, '_blank', 'noopener'); })}>创建 Release</Button></Card></div><Card><SectionTitle title="ZIP 差异导入" subtitle="先上传比较，再经二次验证 Commit + Push" actions={<Button tone="primary" onClick={() => zipRef.current?.click()} disabled={!auth.connected}>选择 ZIP</Button>} /><input ref={zipRef} className="visually-hidden" type="file" accept=".zip,application/zip" onChange={previewImport} /><div className="form-grid"><Field label="目标分支"><SelectInput value={importBranch} onChange={(event: any) => setImportBranch(event.target.value)}>{branches.map(branch => <option key={branch}>{branch}</option>)}</SelectInput></Field><Field label="Commit 信息"><TextInput value={importMessage} onChange={(event: any) => setImportMessage(event.target.value)} /></Field></div>{importPlan ? <div className="notice info"><Icon name="check" /><div><strong>差异已生成</strong><span>新增 {number(importPlan.added)} · 修改 {number(importPlan.modified)} · 不变 {number(importPlan.unchanged)} · 删除 {number(importPlan.deleted)}</span></div><Button tone="primary" busy={working} onClick={commitImport}>Commit 并 Push</Button></div> : null}</Card></> : <Card><EmptyState icon="github" title="尚未选择仓库" description="填写仓库所有者和名称后读取；未连接时也可以查看公开仓库。" /></Card>}</>}</div>;
  }

  function SSHPage(props: PageProps) {
    const [status, setStatus] = useState<any>({}); const [users, setUsers] = useState<any[]>([]); const [keys, setKeys] = useState<any[]>([]); const [selectedUser, setSelectedUser] = useState(''); const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const enabledValue = (value: unknown, fallback = false) => {
      const normalized = String(value ?? '').trim().toLowerCase();
      if (['yes', 'true', 'on', '1', 'enabled'].includes(normalized)) return true;
      if (['no', 'false', 'off', '0', 'disabled'].includes(normalized)) return false;
      return fallback;
    };
    const reload = useCallback(async (user = selectedUser) => { setLoading(true); setError(''); try { const [statusResult, userResult] = await Promise.all([api('/api/v1/ssh/status'), api('/api/v1/ssh/users')]); const nextStatus = asObject(statusResult); const userList = listOf<any>(userResult, 'users'); const nextUser = user || userList[0]?.name || ''; setStatus(nextStatus); setUsers(userList); setSelectedUser(nextUser); if (nextStatus.available === true && nextUser) { setKeys(listOf<any>(await api(`/api/v1/ssh/keys${query({ user: nextUser })}`), 'keys')); } else { setKeys([]); } } catch (cause) { setError(errorDetail(cause)); setKeys([]); } finally { setLoading(false); } }, [selectedUser]); useEffect(() => { void reload(''); }, []);
    const mutate = async (endpoint: string, body: unknown, success: string, user = selectedUser) => { setWorking(true); try { const out = asObject(await secureApi(endpoint, { method: 'POST', body: jsonBody(body) })); props.notify(out.pending_port_confirmation ? 'warning' : 'success', out.message || success); await reload(user); return out; } catch (cause) { props.notify('error', 'SSH 操作失败', errorDetail(cause)); throw cause; } finally { setWorking(false); } };
    const addKey = () => props.openModal({ title: `添加公钥 · ${selectedUser}`, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); try { await mutate('/api/v1/ssh/keys/add', { user: selectedUser, key: new FormData(event.currentTarget).get('key') }, 'SSH 公钥已添加'); props.closeModal(); } catch { /* reported */ } }}><Field label="OpenSSH 公钥"><TextArea name="key" rows={6} placeholder="ssh-ed25519 AAAA..." required /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>添加</Button></div></form> });
    const generateKey = () => props.openModal({ title: `生成密钥 · ${selectedUser}`, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); try { const generated = await mutate('/api/v1/ssh/keys/generate', { user: selectedUser, comment: values.get('comment'), passphrase: values.get('passphrase') }, '密钥已生成'); props.openModal({ title: '保存私钥', size: 'large', content: <div className="form-stack"><div className="notice warning"><Icon name="warning" /><span>私钥只显示一次。请立即保存，关闭后无法再次查看。</span></div><Field label="指纹"><TextInput readOnly value={text(generated.fingerprint)} /></Field><Terminal maxHeight={420}>{generated.private_key}</Terminal><div className="form-actions"><Button icon="copy" onClick={() => copyText(text(generated.private_key, '')).then(() => props.notify('success', '私钥已复制'))}>复制私钥</Button><Button tone="primary" icon="download" onClick={() => { const blob = new Blob([text(generated.private_key, '')], { type: 'application/octet-stream' }); const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = text(generated.filename, 'lukepanel_ed25519'); anchor.click(); setTimeout(() => URL.revokeObjectURL(url), 1000); }}>下载私钥</Button><Button onClick={props.closeModal}>我已保存</Button></div></div> }); } catch { /* reported */ } }}><Field label="注释"><TextInput name="comment" placeholder={`${selectedUser}@lukepanel`} /></Field><Field label="私钥口令" hint="建议设置；留空则不加密"><TextInput name="passphrase" type="password" autoComplete="new-password" /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>生成</Button></div></form> });
    const createUser = () => props.openModal({ title: '创建 Linux 用户', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const name = String(values.get('name') || '').trim(); if (!/^[a-z_][a-z0-9_-]{0,31}$/.test(name)) { props.notify('warning', '用户名只能包含小写字母、数字、下划线和短横线'); return; } try { await mutate('/api/v1/ssh/users/manage', { action: 'create', name, sudo: values.get('sudo') === 'on' }, 'Linux 用户已创建', name); props.closeModal(); } catch { /* reported */ } }}><Field label="用户名"><TextInput name="name" autoCapitalize="none" required /></Field><label className="check-field"><input type="checkbox" name="sudo" />授予 sudo 权限</label><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary" busy={working}>创建</Button></div></form> });
    const saveSettings = async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); await mutate('/api/v1/ssh/settings', { port: number(values.get('port'), 22), permit_root_login: values.get('permit_root_login'), allow_tcp_forwarding: values.get('allow_tcp_forwarding') === 'on', allow_agent_forwarding: values.get('allow_agent_forwarding') === 'on', x11_forwarding: values.get('x11_forwarding') === 'on' }, 'SSH 设置已应用'); };
    const sshAvailable = status.available === true;
    const passwordEnabled = enabledValue(status.password_authentication, false); const totalKeys = users.reduce((sum, user) => sum + number(user.key_count), 0);
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={() => reload()} actions={<Button tone="primary" icon="plus" onClick={createUser}>创建用户</Button>} />{error ? <ErrorState error={error} retry={() => reload()} /> : <>{!sshAvailable ? <div className="notice danger"><Icon name="warning" /><div><strong>OpenSSH Server 当前不可用</strong><span>{text(status.error, '无法读取 sshd 有效配置。SSH 设置、密码登录和公钥操作已禁用，避免把未知状态显示成已开启。')}</span></div></div> : null}{status.pending_new_port ? <div className="notice warning"><Icon name="warning" /><div><strong>SSH 新端口等待确认</strong><span>先在另一个终端连接端口 {text(status.pending_new_port)}，确认成功后再保留。</span></div><Button tone="primary" onClick={() => mutate('/api/v1/ssh/port/confirm', { keep_new: true }, '新端口已确认')}>保留新端口</Button><Button tone="danger" onClick={() => mutate('/api/v1/ssh/port/confirm', { keep_new: false }, '已恢复旧端口')}>恢复旧端口</Button></div> : null}<section className="metric-grid small"><Metric title="SSH 服务" value={sshAvailable ? text(status.service, '可用') : '不可用'} detail={sshAvailable ? `端口 ${text(status.port, '未知')}` : text(status.error, '未检测到可用 sshd')} icon="ssh" tone={sshAvailable ? 'normal' : 'danger'} /><Metric title="Linux 用户" value={String(users.length)} detail="可管理账户" icon="user" /><Metric title="已授权公钥" value={String(totalKeys)} detail="所有用户合计" icon="key" /><Metric title="密码登录" value={passwordEnabled ? '已开启' : '已关闭'} detail={passwordEnabled ? '建议确认密钥后关闭' : '仅密钥登录更安全'} icon="security" tone={passwordEnabled ? 'warning' : 'normal'} /></section><div className="settings-grid"><Card><SectionTitle title="SSH 安全设置" subtitle="端口修改含回滚确认窗口" /><fieldset className="form-fieldset" disabled={!sshAvailable}><form key={`${status.port}|${status.permit_root_login}|${status.allow_tcp_forwarding}|${status.allow_agent_forwarding}|${status.x11_forwarding}`} className="form-stack" onSubmit={saveSettings}><div className="form-grid"><Field label="监听端口"><TextInput name="port" type="number" min="1" max="65535" defaultValue={number(String(status.port || '22').split(/\s+/)[0], 22)} /></Field><Field label="Root 登录"><SelectInput name="permit_root_login" defaultValue={text(status.permit_root_login, 'prohibit-password')}><option value="prohibit-password">只允许密钥（推荐）</option><option value="no">完全禁止</option><option value="yes">允许</option><option value="forced-commands-only">仅强制命令</option></SelectInput></Field></div><label className="check-field"><input type="checkbox" name="allow_tcp_forwarding" defaultChecked={enabledValue(status.allow_tcp_forwarding, false)} />允许 TCP 转发</label><label className="check-field"><input type="checkbox" name="allow_agent_forwarding" defaultChecked={enabledValue(status.allow_agent_forwarding, false)} />允许 Agent 转发</label><label className="check-field"><input type="checkbox" name="x11_forwarding" defaultChecked={enabledValue(status.x11_forwarding, false)} />允许 X11 转发</label><Button type="submit" tone="primary" busy={working}>应用 SSH 设置</Button></form></fieldset></Card><Card><SectionTitle title="密码登录" subtitle="关闭前必须已测试公钥登录" /><div className={`notice ${passwordEnabled ? 'warning' : 'success'}`}><Icon name={passwordEnabled ? 'warning' : 'check'} /><div><strong>{passwordEnabled ? '密码登录当前开启' : '密码登录已关闭'}</strong><span>{passwordEnabled ? '暴露在公网时更容易受到撞库攻击。' : 'Linux 用户必须使用公钥或其他安全方式登录。'}</span></div></div><Button tone={passwordEnabled ? 'danger' : 'primary'} busy={working} disabled={!sshAvailable} onClick={async () => { const enabled = !passwordEnabled; if (!await props.confirm(enabled ? '恢复密码登录' : '关闭密码登录', enabled ? '恢复后，Linux 账户密码可再次用于远程登录。' : '必须已在另一个终端成功测试公钥登录，避免失联。', enabled ? '恢复' : '确认关闭', !enabled)) return; await mutate('/api/v1/ssh/password', { enabled }, enabled ? '密码登录已恢复' : '密码登录已关闭'); }}>{passwordEnabled ? '关闭密码登录' : '恢复密码登录'}</Button></Card></div><Card><SectionTitle title="用户与公钥" subtitle="选择用户后管理 authorized_keys" actions={<div className="row-buttons"><SelectInput aria-label="选择 SSH 用户" value={selectedUser} onChange={(event: any) => { setSelectedUser(event.target.value); void reload(event.target.value); }}>{users.map(user => <option key={user.name}>{user.name}</option>)}</SelectInput><Button tone="primary" icon="plus" onClick={addKey} disabled={!selectedUser || !sshAvailable}>添加公钥</Button><Button icon="key" onClick={generateKey} disabled={!selectedUser || !sshAvailable}>生成密钥</Button></div>} /><div className="user-key-layout"><div className="user-list">{users.map(user => <button className={selectedUser === user.name ? 'active' : ''} key={user.name} onClick={() => { setSelectedUser(user.name); void reload(user.name); }}><span><strong>{user.name}</strong><small>UID {text(user.uid)} · {text(user.home)}</small></span><Badge tone={user.sudo ? 'warning' : 'neutral'}>{user.sudo ? 'sudo' : '普通用户'}</Badge></button>)}</div><div className="key-list">{keys.length ? keys.map(item => <div className="key-row" key={item.id}><span className="credential-icon"><Icon name="key" /></span><div><strong>{text(item.comment, '未命名公钥')}</strong><p className="mono">{text(item.fingerprint)}</p><small>{text(item.type)} · {text(item.preview)}</small></div><Button tone="danger" icon="trash" disabled={!sshAvailable} onClick={async () => { if (await props.confirm('删除 SSH 公钥', '请确认仍有其他可用登录方式，避免失联。', '删除', true)) await mutate('/api/v1/ssh/keys/delete', { user: selectedUser, id: item.id }, '公钥已删除'); }}>删除</Button></div>) : <EmptyState icon="key" title="该用户没有公钥" />}</div></div></Card><Card><SectionTitle title="Linux 用户管理" subtitle="谨慎修改 sudo 权限或删除用户" /><div className="responsive-table"><div className="table-head"><span>用户</span><span>UID/GID</span><span>Home / Shell</span><span>权限</span><span>操作</span></div>{users.map(user => <div className="table-row" key={user.name}><span data-label="用户"><strong>{user.name}</strong></span><span data-label="UID/GID">{text(user.uid)}/{text(user.gid)}</span><span data-label="Home / Shell"><small className="mono">{text(user.home)}<br />{text(user.shell)}</small></span><span data-label="权限"><Badge tone={user.sudo ? 'warning' : 'neutral'}>{user.sudo ? 'sudo' : '普通'}</Badge></span><span data-label="操作" className="row-buttons"><Button tone="ghost" onClick={() => mutate('/api/v1/ssh/users/manage', { action: 'sudo', name: user.name, sudo: !user.sudo }, 'sudo 权限已更新')}>{user.sudo ? '移除 sudo' : '授予 sudo'}</Button><Button tone="danger" onClick={async () => { if (await props.confirm('删除 Linux 用户', `确认删除 ${user.name} 并移除其主目录？`, '删除', true)) await mutate('/api/v1/ssh/users/manage', { action: 'delete', name: user.name, remove_home: true }, '用户已删除', ''); }}>删除</Button></span></div>)}</div></Card></>}</div>;
  }

  function AuditPage(props: PageProps) {
    type Tab = 'audit' | 'system'; const [tab, setTab] = useState<Tab>('audit'); const [auditEvents, setAuditEvents] = useState<any[]>([]); const [systemLogs, setSystemLogs] = useState(''); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [filters, setFilters] = useState({ q: '', result: '', unit: '', priority: '', lines: 500 });
    const reload = useCallback(async () => { setLoading(true); setError(''); try { if (tab === 'audit') { const out = await api(`/api/v1/audit${query({ q: filters.q, result: filters.result, limit: 500 })}`); setAuditEvents(listOf<any>(out, 'events')); } else { const out = asObject(await api(`/api/v1/logs/system${query({ unit: filters.unit, priority: filters.priority, lines: filters.lines })}`)); setSystemLogs(text(out.logs, '')); } } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, [tab, filters.q, filters.result, filters.unit, filters.priority, filters.lines]); useEffect(() => { void reload(); }, [reload]);
    useEffect(() => { if (tab !== 'system') return; const timer = window.setInterval(() => { if (!document.hidden) void reload(); }, 10000); return () => clearInterval(timer); }, [tab, reload]);
    return <div className="page-stack"><PageHeader {...props} busy={loading} onRefresh={reload} actions={tab === 'system' && systemLogs ? <Button tone="ghost" icon="copy" onClick={() => copyText(systemLogs).then(() => props.notify('success', '日志已复制'))}>复制</Button> : null} /><div className="tab-bar"><button className={tab === 'audit' ? 'active' : ''} onClick={() => setTab('audit')}>操作审计</button><button className={tab === 'system' ? 'active' : ''} onClick={() => setTab('system')}>系统日志</button></div>{tab === 'audit' ? <Card><SectionTitle title="操作审计" subtitle="记录面板内高风险操作与结果" /><div className="filter-row"><div className="search-box grow"><Icon name="search" size={17} /><input value={filters.q} onChange={(event: any) => setFilters(current => ({ ...current, q: event.target.value }))} placeholder="用户、路径、动作或 IP" /></div><SelectInput aria-label="筛选审计结果" value={filters.result} onChange={(event: any) => setFilters(current => ({ ...current, result: event.target.value }))}><option value="">全部结果</option><option value="success">成功</option><option value="failure">失败</option></SelectInput></div>{error ? <ErrorState error={error} retry={reload} /> : auditEvents.length ? <div className="audit-list">{auditEvents.map((item, index) => <article className="audit-row" key={item.id || index}><span className={`audit-status ${item.result === 'failure' || item.success === false ? 'bad' : 'ok'}`}><Icon name={item.result === 'failure' || item.success === false ? 'warning' : 'check'} size={16} /></span><div><div className="card-topline"><strong>{text(item.action || item.event)}</strong><Badge tone={item.result === 'failure' || item.success === false ? 'danger' : 'success'}>{text(item.result, item.success === false ? 'failure' : 'success')}</Badge></div><p>{text(item.message || item.resource || item.path)}</p><small>{formatDate(item.time || item.created_at)} · {text(item.username || item.user)} · {text(item.ip || item.client_ip)}</small>{item.details ? <details><summary>详情</summary><Terminal>{typeof item.details === 'string' ? item.details : JSON.stringify(item.details, null, 2)}</Terminal></details> : null}</div></article>)}</div> : !loading ? <EmptyState icon="audit" title="没有匹配的审计记录" /> : <LoadingState />}</Card> : <Card><SectionTitle title="系统日志" subtitle="定期自动刷新；页面隐藏时暂停" /><div className="filter-row"><Field label="systemd 单元"><TextInput value={filters.unit} onChange={(event: any) => setFilters(current => ({ ...current, unit: event.target.value }))} placeholder="例如 ssh.service" /></Field><Field label="优先级"><SelectInput value={filters.priority} onChange={(event: any) => setFilters(current => ({ ...current, priority: event.target.value }))}><option value="">全部</option><option value="err">错误及以上</option><option value="warning">警告及以上</option><option value="info">信息及以上</option></SelectInput></Field><Field label="行数"><TextInput type="number" min="50" max="5000" value={filters.lines} onChange={(event: any) => setFilters(current => ({ ...current, lines: number(event.target.value, 500) }))} /></Field></div>{error ? <ErrorState error={error} retry={reload} /> : loading && !systemLogs ? <LoadingState rows={7} /> : <Terminal maxHeight={720}>{systemLogs}</Terminal>}</Card>}</div>;
  }

  function SecurityPage(props: PageProps) {
    type Tab = 'overview' | 'firewall' | 'fail2ban' | 'access';
    const [tab, setTab] = useState<Tab>('overview');
    const [report, setReport] = useState<any>({ checks: [] });
    const [firewall, setFirewall] = useState<any>({ rules: [] });
    const [fail2ban, setFail2ban] = useState<any>({});
    const [allowlist, setAllowlist] = useState<any>({ enabled: false, entries: [] });
    const [notifications, setNotifications] = useState<any>({ enabled: false });
    const [allowlistText, setAllowlistText] = useState('');
    const [notificationForm, setNotificationForm] = useState({ enabled: false, bot_token: '', chat_id: '' });
    const [rule, setRule] = useState({ action: 'allow', direction: 'in', protocol: 'tcp', port: '', source: '', comment: '' });
    const [ignoreEntry, setIgnoreEntry] = useState('');
    const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => {
      setLoading(true); setError('');
      try {
        const [a, b, c, d, e] = await Promise.all([
          api('/api/v1/security/status'),
          api('/api/v1/security/firewall'),
          api('/api/v1/security/fail2ban'),
          api('/api/v1/security/ip-allowlist'),
          api('/api/v1/security/login-notifications')
        ]);
        const access = asObject(d), notify = asObject(e);
        setReport(asObject(a)); setFirewall({ ...asObject(b), rules: listOf<any>(b, 'rules') }); setFail2ban(asObject(c)); setAllowlist(access);
        setAllowlistText(listOf<string>(access.entries).join('\n')); setNotifications(notify);
        setNotificationForm({ enabled: !!notify.enabled, bot_token: '', chat_id: text(notify.chat_id, '') });
      } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); }
    }, []);
    useEffect(() => { void reload(); }, [reload]);
    const securityAction = async (endpoint: string, body: Dict<any> = {}, success = '操作完成') => {
      setWorking(true); try { await secureApi(`/api/v1/security/${endpoint}`, { method: 'POST', body: jsonBody(body) }); await reload(); props.notify('success', success); }
      catch (cause) { props.notify('error', '安全操作失败', errorDetail(cause)); } finally { setWorking(false); }
    };
    const addRule = async (event: any) => {
      event.preventDefault(); const port = rule.port.trim();
      if (!port) { props.notify('warning', '请输入端口或端口范围'); return; }
      if (rule.action === 'limit' && rule.protocol !== 'tcp') { props.notify('warning', 'UFW 限速规则只支持 TCP，请将协议改为 TCP'); return; }
      setWorking(true); try {
        await secureApi('/api/v1/security/firewall/rule', { method: 'POST', body: jsonBody({ operation: 'add', rule: { action: rule.action, direction: rule.direction, protocol: rule.protocol, port, source: rule.source.trim(), comment: rule.comment.trim() } }) });
        setRule({ action: 'allow', direction: 'in', protocol: 'tcp', port: '', source: '', comment: '' }); await reload(); props.notify('success', '防火墙规则已添加');
      } catch (cause) { props.notify('error', '添加规则失败', errorDetail(cause)); } finally { setWorking(false); }
    };
    const deleteRule = async (item: any) => {
      if (!await props.confirm('删除防火墙规则', `确认删除规则 #${item.number}？`, '删除', true)) return;
      setWorking(true); try { await secureApi('/api/v1/security/firewall/rule', { method: 'POST', body: jsonBody({ operation: 'delete', number: Number(item.number) }) }); await reload(); props.notify('success', '防火墙规则已删除'); }
      catch (cause) { props.notify('error', '删除规则失败', errorDetail(cause)); } finally { setWorking(false); }
    };
    const saveAllowlist = async () => {
      setWorking(true); try {
        const result = asObject(await secureApi('/api/v1/security/ip-allowlist', { method: 'POST', body: jsonBody({ enabled: !!allowlist.enabled, entries: lines(allowlistText) }) }));
        await reload();
        if (result.recovery_path) props.openModal({ title: '保存恢复地址', content: <div className="form-stack"><p>访问限制已启用。请立即保存下面的临时恢复地址，失联时可用它关闭限制。</p><Terminal>{`${location.origin}${result.recovery_path}`}</Terminal><div className="form-actions"><Button icon="copy" onClick={() => copyText(`${location.origin}${result.recovery_path}`).then(() => props.notify('success', '恢复地址已复制'))}>复制</Button><Button tone="primary" onClick={props.closeModal}>我已保存</Button></div></div> });
        else props.notify('success', 'IP 允许列表已保存');
      } catch (cause) { props.notify('error', '访问限制保存失败', errorDetail(cause)); } finally { setWorking(false); }
    };
    const saveNotifications = async (test = false) => {
      setWorking(true); try { await secureApi('/api/v1/security/login-notifications', { method: 'POST', body: jsonBody({ enabled: notificationForm.enabled, bot_token: notificationForm.bot_token.trim(), chat_id: notificationForm.chat_id.trim(), test }) }); setNotificationForm(current => ({ ...current, bot_token: '' })); await reload(); props.notify('success', test ? '测试消息已发送' : '登录通知已保存'); }
      catch (cause) { props.notify('error', '通知配置失败', errorDetail(cause)); } finally { setWorking(false); }
    };
    const runCheck = async (id: string) => {
      if (id === 'firewall') { setTab('firewall'); return; }
      if (id === 'auto-updates' && await props.confirm('启用自动安全更新', '将安装 unattended-upgrades 并启用每日安全更新，不会自动重启服务器。', '启用')) await securityAction('auto-updates/enable', {}, '自动安全更新已启用');
      if (id === 'fail2ban' && await props.confirm('安装并启用 Fail2ban', '将保护 SSH 登录，并自动忽略当前访问 IP 和内网地址。', '安装并启用')) await securityAction('fail2ban/install', {}, 'Fail2ban 安装完成');
    };
    const rules = listOf<any>(firewall, 'rules'), checks = listOf<any>(report, 'checks'), banned = listOf<string>(fail2ban, 'banned_ips'), ignored = listOf<string>(fail2ban, 'ignore_ips');
    const checkStatus = (value: unknown) => { const status = text(value, 'unknown').toLowerCase(); return { raw: status, css: status === 'advice' ? 'warning' : status, label: status === 'good' ? '良好' : status === 'warning' || status === 'advice' ? '建议' : status === 'danger' || status === 'bad' ? '风险' : '待检查', tone: status === 'good' ? 'success' : status === 'warning' || status === 'advice' ? 'warning' : 'danger' } as const; };
    return <div className="page-stack security-page"><PageHeader {...props} busy={loading} onRefresh={reload} />
      <div className="tab-bar security-tabs" role="tablist">{([['overview','安全概览'],['firewall','防火墙'],['fail2ban','Fail2ban'],['access','访问保护']] as [Tab,string][]).map(([value,label]) => <button key={value} className={tab === value ? 'active' : ''} onClick={() => setTab(value)}>{label}</button>)}</div>
      {error ? <ErrorState error={error} retry={reload} /> : loading && !report.score && tab === 'overview' ? <LoadingState rows={6} /> : tab === 'overview' ? <>
        <Card className="security-score"><div className="score-ring"><strong>{text(report.score, '—')}</strong><span>安全评分</span></div><div><h2>{number(report.score) >= 80 ? '整体状态良好' : number(report.score) >= 60 ? '仍有改进空间' : '需要尽快处理'}</h2><p>评分来自主机、面板、SSH、认证和更新策略检查。</p></div></Card>
        <div className="security-check-grid">{checks.map(check => { const status = checkStatus(check.status); return <Card className="security-check" key={check.id}><span className={`check-state ${status.css}`} /><div><div className="card-topline"><strong>{text(check.title)}</strong><Badge tone={status.tone}>{status.label}</Badge></div><p>{text(check.detail)}</p>{check.recommendation ? <small>建议：{check.recommendation}</small> : null}{check.status !== 'good' && ['firewall','fail2ban','auto-updates'].includes(String(check.id)) ? <Button tone="ghost" busy={working} onClick={() => runCheck(String(check.id))}>{check.id === 'firewall' ? '前往配置防火墙' : check.id === 'fail2ban' ? '安装并启用' : '启用自动安全更新'}</Button> : null}</div></Card>; })}</div>
      </> : tab === 'firewall' ? <>
        <Card className="firewall-status-card"><div><span className="eyebrow">UFW 状态</span><h2>{!firewall.installed ? '未安装' : firewall.enabled ? '正在保护' : '已安装但未启用'}</h2>{firewall.installed ? <p>默认入站 {text(firewall.default_incoming)} · 默认出站 {text(firewall.default_outgoing)}</p> : <p>安装后可管理入站、出站和端口规则。</p>}</div><div className="row-buttons">{!firewall.installed ? <Button tone="primary" busy={working} onClick={() => securityAction('firewall/install', {}, 'UFW 安装完成')}>安装 UFW</Button> : !firewall.enabled ? <Button tone="primary" busy={working} onClick={async () => { if (await props.confirm('安全启用 UFW', '系统会先放行当前访问 IP 和 SSH 端口，并创建 5 分钟自动恢复窗口。启用后请确认新连接正常。', '安全启用')) await securityAction('firewall/enable', {}, 'UFW 已启用，请验证连接后确认'); }}>安全启用</Button> : <Button tone="danger" busy={working} onClick={async () => { if (await props.confirm('关闭 UFW', '关闭后系统将失去 UFW 入站保护。', '关闭', true)) await securityAction('firewall/disable', {}, 'UFW 已关闭'); }}>关闭 UFW</Button>}</div></Card>
        {firewall.recovery_pending ? <div className="recovery-banner"><div><strong>防失联恢复窗口正在运行</strong><p>确认当前页面和新的 SSH 连接正常后，立即确认保留 UFW。</p></div><Button tone="primary" busy={working} onClick={() => securityAction('firewall/confirm', {}, 'UFW 配置已确认')}>连接正常，确认保留</Button></div> : null}
        {firewall.installed ? <Card><SectionTitle title="添加防火墙规则" subtitle="使用正确 UFW 语义；失败时显示实际命令和输出" /><form className="firewall-form-grid firewall-form" onSubmit={addRule}><Field label="动作"><SelectInput name="action" value={rule.action} onChange={(event:any) => setRule(current => ({ ...current, action: event.target.value }))}><option value="allow">允许</option><option value="deny">拒绝</option><option value="reject">拒绝并回应</option><option value="limit">连接限速</option></SelectInput></Field><Field label="方向"><SelectInput name="direction" value={rule.direction} onChange={(event:any) => setRule(current => ({ ...current, direction: event.target.value }))}><option value="in">入站</option><option value="out">出站</option></SelectInput></Field><Field label="协议"><SelectInput name="protocol" value={rule.protocol} onChange={(event:any) => setRule(current => ({ ...current, protocol: event.target.value }))}><option value="tcp">TCP</option><option value="udp">UDP</option><option value="any">所有协议</option></SelectInput></Field><Field label="端口"><TextInput name="port" value={rule.port} onChange={(event:any) => setRule(current => ({ ...current, port: event.target.value }))} placeholder="443 或 8000:8100" inputMode="numeric" required /></Field><Field label="来源（可选）"><TextInput name="source" value={rule.source} onChange={(event:any) => setRule(current => ({ ...current, source: event.target.value }))} placeholder="IP、CIDR 或留空" /></Field><Field label="备注（可选）"><TextInput name="comment" value={rule.comment} onChange={(event:any) => setRule(current => ({ ...current, comment: event.target.value }))} /></Field><Button type="submit" tone="primary" busy={working} className="firewall-submit">添加规则</Button></form></Card> : null}
        <div className="firewall-rule-list">{rules.length ? rules.map(item => <Card className="firewall-rule-card" key={item.number}><span className="rule-number">#{item.number}</span><div className="rule-main"><div className="rule-title"><Badge tone={String(item.action).toLowerCase().includes('allow') ? 'success' : String(item.action).toLowerCase().includes('deny') || String(item.action).toLowerCase().includes('reject') ? 'danger' : 'warning'}>{text(item.action)}</Badge><strong>{text(item.to || item.destination, 'Anywhere')}</strong></div><p>来源：{text(item.from || item.source, 'Anywhere')}{item.version ? ` · ${item.version}` : ''}</p></div><Button tone="danger" icon="trash" busy={working} onClick={() => deleteRule(item)}>删除</Button></Card>) : <Card><EmptyState icon="security" title="暂无自定义规则" /></Card>}</div>
      </> : tab === 'fail2ban' ? !fail2ban.installed ? <Card><EmptyState icon="security" title="Fail2ban 未安装" description="安装后将自动保护 SSH，并把当前访问 IP 加入忽略列表。" action={<Button tone="primary" busy={working} onClick={() => securityAction('fail2ban/install', {}, 'Fail2ban 安装完成')}>安装并启用</Button>} /></Card> : <><div className="metric-grid compact-metrics"><Metric title="当前失败" value={text(fail2ban.currently_failed, '0')} detail="当前 jail" icon="warning" /><Metric title="累计失败" value={text(fail2ban.total_failed, '0')} detail="历史累计" icon="audit" /><Metric title="当前封禁" value={text(fail2ban.currently_banned, '0')} detail="正在阻止" icon="security" /><Metric title="累计封禁" value={text(fail2ban.total_banned, '0')} detail="历史累计" icon="backup" /></div><div className="security-split"><Card><SectionTitle title="已封禁 IP" subtitle="sshd jail 当前记录" />{banned.length ? <div className="compact-list">{banned.map(ip => <div className="compact-row" key={ip}><code>{ip}</code><Button tone="ghost" onClick={() => securityAction('fail2ban/unban', { ip }, `${ip} 已解封`)}>解封</Button></div>)}</div> : <EmptyState icon="check" title="当前没有封禁地址" />}</Card><Card><SectionTitle title="忽略列表" subtitle="不会被 Fail2ban 封禁的地址" /><form className="inline-form" onSubmit={async (event: any) => { event.preventDefault(); if (!ignoreEntry.trim()) return; await securityAction('fail2ban/ignore', { entry: ignoreEntry.trim(), action: 'add' }, 'Fail2ban 白名单已更新'); setIgnoreEntry(''); }}><TextInput value={ignoreEntry} onChange={(event:any) => setIgnoreEntry(event.target.value)} placeholder="IP 或 CIDR" /><Button type="submit" tone="primary" busy={working}>添加</Button></form><div className="compact-list">{ignored.map(entry => <div className="compact-row" key={entry}><code>{entry}</code><Button tone="danger" icon="trash" title={`从白名单移除 ${entry}`} onClick={() => securityAction('fail2ban/ignore', { entry, action: 'remove' }, '白名单条目已移除')} /></div>)}</div></Card></div></>
      : <div className="settings-grid"><Card className="setting-card"><SectionTitle title="面板 IP 允许列表" subtitle="开启后只有指定 IP 或网段能访问面板" actions={<Toggle checked={!!allowlist.enabled} onChange={enabled => setAllowlist((current:any) => ({ ...current, enabled }))} label={allowlist.enabled ? '已开启' : '未开启'} />} /><Field label="允许地址" hint="每行一个 IP 或 CIDR；开启时自动包含当前 IP"><TextArea rows="7" value={allowlistText} onChange={(event:any) => setAllowlistText(event.target.value)} /></Field><p className="field-hint">当前访问 IP：{text(allowlist.current_ip, '无法识别')}</p><Button tone="primary" busy={working} onClick={saveAllowlist}>保存访问限制</Button></Card><Card className="setting-card"><SectionTitle title="Telegram 登录通知" subtitle="登录成功后发送设备、IP 和登录方式" actions={<Toggle checked={notificationForm.enabled} onChange={enabled => setNotificationForm(current => ({ ...current, enabled }))} label={notificationForm.enabled ? '已开启' : '未开启'} />} /><Field label="Bot Token" hint={notifications.configured ? '已保存 Token，留空保持不变' : '首次启用时必填'}><TextInput type="password" value={notificationForm.bot_token} onChange={(event:any) => setNotificationForm(current => ({ ...current, bot_token: event.target.value }))} autoComplete="off" /></Field><Field label="Chat ID"><TextInput value={notificationForm.chat_id} onChange={(event:any) => setNotificationForm(current => ({ ...current, chat_id: event.target.value }))} /></Field><div className="form-actions"><Button tone="primary" busy={working} onClick={() => saveNotifications(false)}>保存</Button><Button busy={working} onClick={() => saveNotifications(true)}>保存并测试</Button></div></Card></div>}
    </div>;
  }

  function SettingsPage(props: PageProps & { username: string; onLogout: () => Promise<void> }) {
    const [settings, setSettings] = useState<any>({}); const [sessions, setSessions] = useState<any[]>([]); const [currentSession, setCurrentSession] = useState(''); const [passkeys, setPasskeys] = useState<any[]>([]); const [devices, setDevices] = useState<any[]>([]); const [totp, setTotp] = useState<any>({});
    const [account, setAccount] = useState({ username: props.username, current_password: '' }); const [password, setPassword] = useState({ current_password: '', new_password: '', confirm: '' }); const [refreshSeconds, setRefreshSeconds] = useState(10); const [passkeyName, setPasskeyName] = useState('');
    const [loading, setLoading] = useState(true); const [working, setWorking] = useState(false); const [error, setError] = useState('');
    const reload = useCallback(async () => { setLoading(true); setError(''); try { const [a,b,c,d,e] = await Promise.all([api('/api/v1/settings'), api('/api/v1/auth/sessions'), api('/api/v1/auth/passkeys'), api('/api/v1/auth/trusted-devices'), api('/api/v1/auth/totp/status')]); const config = asObject(a), sessionData = asObject(b); setSettings(config); setSessions(listOf<any>(sessionData, 'sessions')); setCurrentSession(text(sessionData.current, '')); setPasskeys(listOf<any>(c, 'passkeys')); setDevices(listOf<any>(d, 'devices')); setTotp(asObject(e)); setAccount(current => ({ ...current, username: text(config.admin_user, props.username) })); setRefreshSeconds(number(config.auto_refresh_seconds, 10)); } catch (cause) { setError(errorDetail(cause)); } finally { setLoading(false); } }, [props.username]);
    useEffect(() => { void reload(); }, [reload]);
    const run = async (title: string, task: () => Promise<void>) => { setWorking(true); try { await task(); } catch (cause) { props.notify('error', title, errorDetail(cause)); } finally { setWorking(false); } };
    const recoveryModal = (codes: string[]) => props.openModal({ title: '保存恢复码', content: <div className="form-stack"><p>每个恢复码只能使用一次，关闭后不会再次显示。</p><Terminal>{codes.join('\n')}</Terminal><div className="form-actions"><Button icon="copy" onClick={() => copyText(codes.join('\n')).then(() => props.notify('success', '恢复码已复制'))}>复制全部</Button><Button tone="primary" onClick={props.closeModal}>我已保存</Button></div></div> });
    const beginTotp = () => run('无法开始两步验证设置', async () => { const setup = asObject(await secureApi('/api/v1/auth/totp/setup', { method: 'POST', body: '{}' })); props.openModal({ title: '设置两步验证', content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const code = String(new FormData(event.currentTarget).get('code') || '').trim(); await run('验证码确认失败', async () => { await secureApi('/api/v1/auth/totp/confirm', { method: 'POST', body: jsonBody({ code }) }); props.closeModal(); recoveryModal(listOf<string>(setup.recovery_codes)); await reload(); props.notify('success', '两步验证已开启'); }); }}><p>在身份验证器中手动添加密钥，或使用下方 OTPAuth URI。</p><Field label="密钥"><div className="input-copy"><TextInput readOnly value={text(setup.secret, '')} /><Button icon="copy" title="复制密钥" onClick={() => copyText(text(setup.secret, '')).then(() => props.notify('success', '密钥已复制'))} /></div></Field><Field label="OTPAuth URI"><TextArea readOnly rows="3" value={text(setup.otpauth_uri, '')} /></Field><Field label="6 位验证码"><TextInput name="code" inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" required autoFocus /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone="primary">验证并开启</Button></div></form> }); });
    const codeAction = (title: string, description: string, endpoint: string, success: string, destructive = false) => props.openModal({ title, content: <form className="form-stack" onSubmit={async (event: any) => { event.preventDefault(); const code = String(new FormData(event.currentTarget).get('code') || '').trim(); await run(`${title}失败`, async () => { const out = asObject(await secureApi(endpoint, { method: 'POST', body: jsonBody({ code }) })); props.closeModal(); if (out.recovery_codes) recoveryModal(listOf<string>(out.recovery_codes)); await reload(); props.notify('success', success); }); }}><p>{description}</p><Field label="验证码或恢复码"><TextInput name="code" required autoFocus autoComplete="one-time-code" /></Field><div className="form-actions"><Button onClick={props.closeModal}>取消</Button><Button type="submit" tone={destructive ? 'danger' : 'primary'}>{destructive ? '确认关闭' : '确认'}</Button></div></form> });
    const registerPasskey = () => run('Passkey 注册失败', async () => { if (!window.PublicKeyCredential || !navigator.credentials) throw new Error('当前浏览器不支持 Passkey'); const name = passkeyName.trim() || navigator.platform || '当前设备'; const begin = asObject(await secureApi('/api/v1/auth/passkey/register/begin', { method: 'POST', body: jsonBody({ name }) })); const credential = await navigator.credentials.create({ publicKey: normalizeCreationOptions(begin.public_key || begin.options || begin) }) as PublicKeyCredential | null; if (!credential) throw new Error('未完成 Passkey 创建'); await secureApi('/api/v1/auth/passkey/register/finish', { method: 'POST', body: jsonBody({ flow_id: begin.flow_id, name, credential: serializeCredential(credential) }) }); setPasskeyName(''); await reload(); props.notify('success', 'Passkey 已添加'); });
    return <div className="page-stack settings-page"><PageHeader {...props} busy={loading} onRefresh={reload} actions={<Button tone="danger" onClick={props.onLogout}>退出登录</Button>} />{error ? <ErrorState error={error} retry={reload} /> : loading && !settings.version ? <LoadingState rows={6} /> : <>
      <div className="settings-grid"><Card className="setting-card"><SectionTitle title="管理员账户" subtitle="修改用户名后会撤销其他登录会话" /><form className="form-stack" onSubmit={(event: any) => { event.preventDefault(); void run('用户名修改失败', async () => { await api('/api/v1/auth/account', { method: 'PATCH', body: jsonBody({ username: account.username.trim(), current_password: account.current_password }) }); setAccount(current => ({ ...current, current_password: '' })); await reload(); props.notify('success', '管理员用户名已更新'); }); }}><Field label="管理员用户名"><TextInput value={account.username} onChange={(event:any) => setAccount(current => ({ ...current, username: event.target.value }))} autoCapitalize="none" required /></Field><Field label="当前密码"><TextInput type="password" value={account.current_password} onChange={(event:any) => setAccount(current => ({ ...current, current_password: event.target.value }))} autoComplete="current-password" required /></Field><Button type="submit" tone="primary" busy={working}>保存用户名</Button></form></Card>
      <Card className="setting-card"><SectionTitle title="修改密码" subtitle="新密码必须满足后端安全强度要求" /><form className="form-stack" onSubmit={(event: any) => { event.preventDefault(); if (password.new_password !== password.confirm) { props.notify('warning', '两次输入的新密码不一致'); return; } void run('密码修改失败', async () => { await api('/api/v1/auth/password', { method: 'POST', body: jsonBody({ current_password: password.current_password, new_password: password.new_password }) }); setPassword({ current_password: '', new_password: '', confirm: '' }); await reload(); props.notify('success', '密码已修改，其他会话已撤销'); }); }}><Field label="当前密码"><TextInput type="password" value={password.current_password} onChange={(event:any) => setPassword(current => ({ ...current, current_password: event.target.value }))} autoComplete="current-password" required /></Field><Field label="新密码"><TextInput type="password" value={password.new_password} onChange={(event:any) => setPassword(current => ({ ...current, new_password: event.target.value }))} autoComplete="new-password" required /></Field><Field label="确认新密码"><TextInput type="password" value={password.confirm} onChange={(event:any) => setPassword(current => ({ ...current, confirm: event.target.value }))} autoComplete="new-password" required /></Field><Button type="submit" tone="primary" busy={working}>修改密码</Button></form></Card>
      <Card className="setting-card"><SectionTitle title="两步验证" subtitle="身份验证器和一次性恢复码" actions={<Badge tone={totp.enabled ? 'success' : 'neutral'}>{totp.enabled ? '已开启' : '未开启'}</Badge>} />{totp.enabled ? <p className="setting-note">剩余恢复码：{number(totp.recovery_codes_remaining)} 个</p> : <p className="setting-note">强烈建议管理员账户开启两步验证。</p>}<div className="form-actions">{!totp.enabled ? <Button tone="primary" busy={working} onClick={beginTotp}>开始设置</Button> : <><Button busy={working} onClick={() => codeAction('重新生成恢复码', '旧恢复码将立即失效。', '/api/v1/auth/totp/recovery', '恢复码已重新生成')}>重新生成恢复码</Button><Button tone="danger" busy={working} onClick={() => codeAction('关闭两步验证', '关闭后账户安全性会降低。', '/api/v1/auth/totp/disable', '两步验证已关闭', true)}>关闭</Button></>}</div></Card>
      <Card className="setting-card"><SectionTitle title="面板偏好" subtitle="统一控制页面自动刷新频率" /><form className="form-stack" onSubmit={(event: any) => { event.preventDefault(); void run('保存失败', async () => { await api('/api/v1/settings', { method: 'PATCH', body: jsonBody({ auto_refresh_seconds: refreshSeconds }) }); await reload(); props.notify('success', '刷新频率已保存'); }); }}><Field label="自动刷新间隔（秒）"><TextInput type="number" min="2" max="300" value={refreshSeconds} onChange={(event:any) => setRefreshSeconds(number(event.target.value, 10))} /></Field><Button type="submit" tone="primary" busy={working}>保存偏好</Button></form><dl className="key-value-list"><KeyValue label="版本" value={text(settings.version, VERSION)} /><KeyValue label="监听" value={text(settings.listen)} mono /><KeyValue label="HTTPS Cookie" value={boolText(settings.secure_cookie)} /></dl></Card></div>
      <Card><SectionTitle title="Passkey" subtitle="使用 Face ID、Touch ID 或安全密钥登录" /><div className="inline-form"><TextInput value={passkeyName} onChange={(event:any) => setPasskeyName(event.target.value)} placeholder="设备名称，例如 iPhone" /><Button tone="primary" busy={working} onClick={registerPasskey}>添加 Passkey</Button></div>{passkeys.length ? <div className="credential-list">{passkeys.map(item => <div className="credential-row" key={item.id}><span className="credential-icon"><Icon name="key" /></span><div><strong>{text(item.name, '未命名 Passkey')}</strong><p>创建：{formatDate(item.created_at)}{item.last_used ? ` · 最近使用：${formatDate(item.last_used)}` : ''}</p></div><Button tone="danger" onClick={async () => { if (await props.confirm('删除 Passkey', `确认删除“${text(item.name, '未命名 Passkey')}”？`, '删除', true)) await run('删除失败', async () => { await secureApi('/api/v1/auth/passkeys', { method: 'DELETE', body: jsonBody({ id: item.id }) }); await reload(); props.notify('success', 'Passkey 已删除'); }); }}>删除</Button></div>)}</div> : <EmptyState icon="key" title="尚未添加 Passkey" />}</Card>
      <div className="settings-two-column"><Card><SectionTitle title="登录会话" subtitle="当前会话不会被批量撤销" actions={<Button tone="danger" disabled={sessions.length <= 1} onClick={() => run('撤销失败', async () => { const out = asObject(await api('/api/v1/auth/sessions', { method: 'DELETE', body: '{}' })); await reload(); props.notify('success', `已撤销 ${number(out.revoked)} 个其他会话`); })}>撤销其他会话</Button>} /><div className="compact-list">{sessions.map(session => <div className="compact-row" key={session.id}><div><strong>{session.id}{session.id === currentSession ? <Badge tone="success">当前</Badge> : null}</strong><small>{formatDate(session.created_at)} → {formatDate(session.expires_at)}</small></div></div>)}</div></Card><Card><SectionTitle title="可信设备" subtitle="跳过两步验证的设备 Cookie" actions={<Button tone="danger" disabled={!devices.length} onClick={() => run('撤销失败', async () => { await secureApi('/api/v1/auth/trusted-devices', { method: 'DELETE', body: jsonBody({ all: true }) }); await reload(); props.notify('success', '全部可信设备已撤销'); })}>全部撤销</Button>} /><div className="compact-list">{devices.length ? devices.map(device => <div className="compact-row" key={device.id}><div><strong>{text(device.name, '未命名设备')}</strong><small>{text(device.last_ip)} · {formatDate(device.last_used)}</small></div><Button tone="danger" icon="trash" title={`撤销可信设备 ${text(device.name, '未命名设备')}`} onClick={() => run('撤销失败', async () => { await secureApi('/api/v1/auth/trusted-devices', { method: 'DELETE', body: jsonBody({ id: device.id }) }); await reload(); props.notify('success', '可信设备已撤销'); })} /></div>) : <EmptyState icon="user" title="没有可信设备" />}</div></Card></div>
    </>}</div>;
  }

  class AppErrorBoundary extends React.Component<any, { error: Error | null }> {
    constructor(props: any) { super(props); this.state = { error: null }; }
    static getDerivedStateFromError(error: Error) { return { error }; }
    componentDidCatch(error: Error, info: any) { console.error('LukePanel UI error', error, info); }
    render() { if (!this.state.error) return this.props.children; return <main className="fatal-layout"><div className="fatal-card"><span className="empty-icon"><Icon name="warning" size={32} /></span><h1>页面发生错误</h1><p>为避免错误状态继续影响操作，界面已停止渲染。</p><Terminal>{this.state.error.message}\n{this.state.error.stack}</Terminal><Button tone="primary" onClick={() => location.reload()}>重新加载</Button></div></main>; }
  }

  function App() {
    const routeState = useRoute(); const route = ROUTE_MAP.get(routeState.path) || ROUTE_MAP.get('/')!;
    const [ready, setReady] = useState(false); const [identity, setIdentity] = useState<Identity | null>(null); const [modal, setModal] = useState<ModalState>(null); const [drawer, setDrawer] = useState(false); const [compactLayout, setCompactLayout] = useState(() => window.matchMedia('(max-width: 900px)').matches); const [toasts, setToasts] = useState<ToastItem[]>([]);
    const sidebarRef = useRef<HTMLElement | null>(null); const confirmResolve = useRef<((value: boolean) => void) | null>(null); const elevationResolve = useRef<(() => void) | null>(null); const elevationReject = useRef<((error: Error) => void) | null>(null); const modalReturnFocus = useRef<HTMLElement | null>(null); const toastId = useRef(0);
    const notify = useCallback((kind: ToastKind, title: string, detail?: string) => { const id = ++toastId.current; setToasts(items => [...items.slice(-3), { id, kind, title, detail }]); window.setTimeout(() => setToasts(items => items.filter(item => item.id !== id)), kind === 'error' ? 10000 : 5000); }, []);
    const presentModal = useCallback((state: NonNullable<ModalState>) => {
      if (!document.querySelector('.modal')) modalReturnFocus.current = document.activeElement instanceof HTMLElement ? document.activeElement : null;
      setModal(state);
    }, []);
    const closeModal = useCallback(() => { if (confirmResolve.current) { confirmResolve.current(false); confirmResolve.current = null; } setModal(null); }, []);
    const openModal = presentModal;
    const confirmAction = useCallback((title: string, message: string, actionLabel = '确认', destructive = false) => new Promise<boolean>(resolve => {
      if (confirmResolve.current) confirmResolve.current(false); confirmResolve.current = resolve;
      presentModal({ title, destructive, content: <div className="confirm-dialog"><p>{message}</p><div className="form-actions"><Button onClick={() => { confirmResolve.current?.(false); confirmResolve.current = null; setModal(null); }}>取消</Button><Button tone={destructive ? 'danger' : 'primary'} onClick={() => { confirmResolve.current?.(true); confirmResolve.current = null; setModal(null); }}>{actionLabel}</Button></div></div> });
    }), [presentModal]);
    const resetAuth = useCallback(() => { csrfToken = ''; setIdentity(null); setModal(null); }, []);
    const applyIdentity = useCallback((next: Identity) => { csrfToken = text(next.csrf_token, ''); setIdentity(next); setReady(true); localStorage.setItem('lukepanel:last-user', text(next.username, '')); }, []);
    useEffect(() => { unauthorizedHandler = resetAuth; return () => { unauthorizedHandler = null; }; }, [resetAuth]);
    useEffect(() => {
      let active = true; void api<Identity>('/api/v1/auth/me').then(next => { if (active) applyIdentity(next); }).catch(() => { if (active) { resetAuth(); setReady(true); } }); return () => { active = false; };
    }, [applyIdentity, resetAuth]);
    useEffect(() => {
      elevationHandler = () => new Promise<void>((resolve, reject) => {
        elevationResolve.current = resolve; elevationReject.current = reject;
        presentModal({ title: '需要二次验证', content: <form className="form-stack elevation-form" onSubmit={async (event: any) => { event.preventDefault(); const values = new FormData(event.currentTarget); const submit = event.currentTarget.querySelector('button[type="submit"]') as HTMLButtonElement | null; if (submit) submit.disabled = true; try { await api('/api/v1/auth/elevate', { method: 'POST', body: jsonBody({ password: values.get('password'), otp: values.get('otp') }) }); setModal(null); elevationResolve.current?.(); elevationResolve.current = null; elevationReject.current = null; } catch (cause) { notify('error', '二次验证失败', errorDetail(cause)); if (submit) submit.disabled = false; } }}><p>此操作会修改系统配置，请再次验证管理员身份。</p><Field label="当前密码"><TextInput name="password" type="password" autoComplete="current-password" required autoFocus /></Field><Field label="两步验证码（已开启时）"><TextInput name="otp" inputMode="numeric" autoComplete="one-time-code" /></Field><div className="form-actions"><Button onClick={() => { setModal(null); elevationReject.current?.(new Error('已取消二次验证')); elevationResolve.current = null; elevationReject.current = null; }}>取消</Button><Button type="submit" tone="primary">验证并继续</Button></div></form> });
      });
      return () => { elevationHandler = null; };
    }, [notify, presentModal]);
    useEffect(() => {
      const query = window.matchMedia('(max-width: 900px)');
      const update = () => setCompactLayout(query.matches);
      update(); query.addEventListener('change', update);
      return () => query.removeEventListener('change', update);
    }, []);
    useEffect(() => { if (sidebarRef.current) (sidebarRef.current as any).inert = compactLayout && !drawer; }, [compactLayout, drawer]);
    useEffect(() => { document.title = `${route.title} · LukePanel`; setDrawer(false); }, [route.path]);
    const logout = useCallback(async () => { try { await api('/api/v1/auth/logout', { method: 'POST', body: '{}' }); } catch { /* Session may already be invalid. */ } finally { resetAuth(); routeState.navigate('/', true); } }, [resetAuth, routeState.navigate]);
    if (!ready) return <main className="boot-screen"><img src="/assets/favicon-64.png" alt="" /><span className="spinner" /><p>正在连接 LukePanel…</p></main>;
    if (!identity) return <LoginPage onAuthenticated={applyIdentity} notify={notify} />;
    const pageProps: PageProps = { route, navigate: routeState.navigate, notify, openModal, closeModal, confirm: confirmAction };
    const renderPage = () => {
      switch (route.path) {
        case '/': return <DashboardPage {...pageProps} />; case '/system': return <SystemPage {...pageProps} />; case '/system/services': return <ServicesPage {...pageProps} />; case '/system/processes': return <ProcessesPage {...pageProps} />; case '/system/network': return <NetworkPage {...pageProps} />; case '/system/storage': return <StoragePage {...pageProps} />; case '/system/tasks': return <TasksPage {...pageProps} />; case '/system/updates': return <UpdatesPage {...pageProps} />; case '/system/host': return <HostPage {...pageProps} />; case '/system/snapshots': return <SnapshotsPage {...pageProps} />; case '/docker': return <DockerPage {...pageProps} />; case '/files': return <FilesPage {...pageProps} />; case '/tools': return <ToolsPage {...pageProps} />; case '/tools/github': return <GitHubPage {...pageProps} />; case '/ssh': return <SSHPage {...pageProps} />; case '/audit': return <AuditPage {...pageProps} />; case '/security': return <SecurityPage {...pageProps} />; case '/settings': return <SettingsPage {...pageProps} username={text(identity.username, '')} onLogout={logout} />; default: return <DashboardPage {...pageProps} />;
      }
    };
    const active = (item: RouteDef) => route.path === item.path || (item.path !== '/' && route.path.startsWith(`${item.path}/`));
    return <div className="app-shell">
      <aside ref={sidebarRef} className={`sidebar ${drawer ? 'is-open' : ''}`} aria-hidden={compactLayout && !drawer ? true : undefined}><div className="sidebar-brand"><img src="/assets/favicon-64.png" alt="LukePanel" /><div><strong>LukePanel</strong><span>{VERSION}</span></div><button className="icon-button sidebar-close" onClick={() => setDrawer(false)} aria-label="关闭菜单"><Icon name="close" /></button></div><nav className="sidebar-nav" aria-label="主导航">{PRIMARY_ROUTES.map(item => <button key={item.path} className={active(item) ? 'active' : ''} onClick={() => routeState.navigate(item.path)}><Icon name={item.icon} /><span>{item.title}</span></button>)}</nav><div className="sidebar-account"><span className="avatar">{text(identity.username, 'L').slice(0, 1).toUpperCase()}</span><div><strong>{text(identity.username, '管理员')}</strong><small>本机管理员</small></div><button className="icon-button" onClick={logout} aria-label="退出登录"><Icon name="back" /></button></div></aside>
      {drawer ? <button className="drawer-scrim" aria-label="关闭菜单" onClick={() => setDrawer(false)} /> : null}
      <section className="workspace"><header className="mobile-topbar"><button className="icon-button" onClick={() => setDrawer(true)} aria-label="打开菜单"><Icon name="menu" /></button><div className="mobile-brand"><img src="/assets/favicon-64.png" alt="" /><strong>LukePanel</strong></div><span className="mobile-user">{text(identity.username, '管理员')}</span></header><main className="content"><AppErrorBoundary key={route.path}>{renderPage()}</AppErrorBoundary></main></section>
      <nav className="mobile-bottom-nav" aria-label="移动端主导航">{MOBILE_ROUTES.map(path => { const item = ROUTE_MAP.get(path)!; return <button key={path} className={active(item) ? 'active' : ''} onClick={() => routeState.navigate(path)}><Icon name={item.icon} size={20} /><span>{item.title}</span></button>; })}<button onClick={() => setDrawer(true)}><Icon name="more" size={20} /><span>更多</span></button></nav>
      <Modal state={modal} returnFocus={modalReturnFocus.current} onClose={() => { if (elevationReject.current) { elevationReject.current(new Error('已取消二次验证')); elevationResolve.current = null; elevationReject.current = null; } closeModal(); }} /><Toasts items={toasts} dismiss={id => setToasts(items => items.filter(item => item.id !== id))} />
    </div>;
  }

  const rootNode = document.getElementById('app');
  if (!rootNode) throw new Error('缺少 #app 根节点');
  ReactDOM.createRoot(rootNode).render(<AppErrorBoundary><App /></AppErrorBoundary>);
})();
